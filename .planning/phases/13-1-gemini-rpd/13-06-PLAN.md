---
phase: 13-1-gemini-rpd
plan: 06
type: execute
wave: 5
depends_on:
  - 13-05
files_modified:
  - .github/workflows/daily.yml
  - .github/workflows/weekly.yml
autonomous: false
requirements:
  - SPEC-4
  - SPEC-6
  - SPEC-7
user_setup: []

must_haves:
  truths:
    - "`.github/workflows/daily.yml` 의 cron === `'0 12 * * 2-7,0'` (화~일 매일 09:00 KST)"
    - "daily.yml 의 run command === `pnpm tsx src/main.ts --mode=daily`"
    - "daily.yml 의 git-auto-commit-action file_pattern === `'state/pending.json state/seen.json'` (공백 구분, atomic — D-23)"
    - "`.github/workflows/weekly.yml` 신규 파일 존재; cron === `'0 12 * * 1'` (월요일 21:00 KST per SPEC §Requirements 6)"
    - "weekly.yml 의 run command === `pnpm tsx src/main.ts --mode=weekly`"
    - "weekly.yml 의 git-auto-commit-action file_pattern === `'state/pending.json state/seen.json archive/**/*.html'`"
    - "양쪽 workflow 모두 `concurrency: { group: digest-pipeline, cancel-in-progress: false }` 공유 (seen+pending race 차단 — SPEC constraint)"
    - "양쪽 workflow 모두 `workflow_dispatch: {}` 유지하여 수동 트리거 가능 (SPEC §Requirement 6)"
    - "commit message 에 `[skip ci]` 포함 (DEDUP-06 — 양쪽)"
    - "weekly.yml 에서도 shin-kim Thawte cert 블록 + Open Issue on failure 블록 유지 (defense-in-depth)"
    - "Human verification checkpoint: 사용자가 manual workflow_dispatch 로 daily.yml 과 weekly.yml 양쪽 한 번씩 실행 후 GHA log 에서 `[METRIC] geminiCallCount=` 마커 grep 확인 (AC-7 partial — full 일주일 검증은 SPEC §AC-7 별도)"
  artifacts:
    - path: ".github/workflows/daily.yml"
      provides: "daily cron (화~일) + --mode=daily entry + pending+seen atomic commit"
      contains: "0 12 * * 2-7,0"
    - path: ".github/workflows/weekly.yml"
      provides: "weekly cron (Mon) + --mode=weekly entry + pending+seen+archive atomic commit"
      contains: "0 12 * * 1"
  key_links:
    - from: ".github/workflows/daily.yml"
      to: "src/main.ts --mode=daily"
      via: "run command"
      pattern: "--mode=daily"
    - from: ".github/workflows/weekly.yml"
      to: "src/main.ts --mode=weekly"
      via: "run command"
      pattern: "--mode=weekly"
    - from: "both workflows"
      to: "digest-pipeline concurrency group"
      via: "race-prevention shared group"
      pattern: "group: digest-pipeline"
---

<objective>
SPEC §Requirement 6 의 workflow 2-file split 을 구현한다. 기존 daily.yml 을 매일 cron 으로 바꾸고 `--mode=daily` entry 로 swap, 신규 weekly.yml 을 월요일 cron + `--mode=weekly` entry 로 생성한다. 양쪽이 `concurrency: digest-pipeline` 그룹을 공유해 seen.json/pending.json 동시 쓰기 race 를 차단 (SPEC constraint). 양쪽 commit step 의 file_pattern 이 pending+seen 을 동시 commit 하여 D-23 atomic invariant 보장.

Human verify checkpoint: manual workflow_dispatch trigger 로 daily + weekly 한 번씩 실행해 GHA UI 에서 step-summary `[METRIC] geminiCallCount=` 마커 확인 (SPEC AC-7 의 일주일 dry 실행 검증은 본 plan 의 scope 외 — 본 plan 은 GHA 인프라 셋업까지).

Purpose: Phase 13 의 production deployment 절반 — cron 분산 자체. Manual dispatch 검증은 본 plan 의 마지막 checkpoint task 가 수행.
Output: daily.yml 수정 + weekly.yml 신규 + human-verify checkpoint.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-1-gemini-rpd/13-SPEC.md
@.planning/phases/13-1-gemini-rpd/13-CONTEXT.md
@.planning/phases/13-1-gemini-rpd/13-PATTERNS.md

@.github/workflows/daily.yml
@src/main.ts

<interfaces>
<!-- Existing daily.yml structure to preserve / mirror. -->

From .github/workflows/daily.yml (current — line 32-38 trigger block):
```yaml
schedule:
  - cron: '0 12 * * 1'  # 매일 09:00 KST  ← will change to '0 12 * * 2-7,0'
workflow_dispatch: {}
concurrency:
  group: digest-pipeline
  cancel-in-progress: false
```

run step (line 118-126):
```yaml
- name: Run daily digest pipeline
  id: run_pipeline
  run: pnpm tsx src/main.ts  # ← will change to "pnpm tsx src/main.ts --mode=daily"
  env: ...
```

commit step (line 127-132):
```yaml
- uses: stefanzweifel/git-auto-commit-action@v7
  with:
    commit_message: '...'
    file_pattern: 'state/seen.json'  # ← will change to 'state/pending.json state/seen.json'
```

Note on cron string semantics:
  - `0 12 * * 2-7,0` = at 12:00 UTC = 21:00 KST on day-of-week 2-7 (Tue-Sun) and 0 (Sun).
    Sunday is listed twice (7 and 0) but cron de-duplicates — effectively Tue-Sun (no Mon).
  - `0 12 * * 1` = at 12:00 UTC = 21:00 KST on Monday.
  - Note: SPEC.md headline cron uses 12:00 UTC; SPEC §Acceptance AC-5/AC-6 confirms 21:00 KST.
    CONTEXT.md D-16 heartbeat KST date format aligns.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Modify .github/workflows/daily.yml — change cron to Tue-Sun, append --mode=daily to entry, expand commit file_pattern to include pending.json</name>
  <files>.github/workflows/daily.yml</files>
  <read_first>
    - .github/workflows/daily.yml (current — 171 lines)
    - src/main.ts (Plan 13-05 — confirms --mode=daily entry)
    - .planning/phases/13-1-gemini-rpd/13-CONTEXT.md D-23
    - .planning/phases/13-1-gemini-rpd/13-SPEC.md §Requirement 6 + AC-5
    - .planning/phases/13-1-gemini-rpd/13-PATTERNS.md ".github/workflows/daily.yml" section
  </read_first>
  <action>
Edit `.github/workflows/daily.yml` with these surgical changes (do NOT touch unrelated blocks):

1. **Header doc comment** (lines 1-25) — replace the title block to reflect daily cadence change:

Find the line `# LegalNewsletter — scheduled daily digest pipeline.` and update the surrounding paragraph to:

```yaml
# .github/workflows/daily.yml
#
# LegalNewsletter — Phase 13 daily scrape pipeline (Tue-Sun).
#
# Runs src/main.ts --mode=daily at 12:00 UTC (21:00 KST) every Tue-Sun (cron
# `0 12 * * 2-7,0` — Mon excluded because weekly.yml owns Mon at the same
# slot). Executes fetch → enrich → filter → dedup → summarize → appendPending
# → writeState; does NOT send email or archive. Weekly.yml on Mon reads the
# accumulated pending.json and dispatches the digest.
#
# State commit groups state/pending.json + state/seen.json into ONE git
# commit (D-23 atomic invariant) so a runner death between summarize and
# write leaves both files un-touched (idempotent recovery on next daily run).
#
# All pre-existing pitfall guards preserved: [skip ci] in commit message,
# concurrency group "digest-pipeline" SHARED with weekly.yml (seen+pending
# race prevention), permissions: contents: write, workflow_dispatch trigger
# for manual smoke tests.
```

2. **schedule.cron** (line 32-33) — change to Tue-Sun:

Find:
```yaml
  schedule:
    - cron: '0 12 * * 1'  # 매일 09:00 KST (config/settings.yaml schedule.cron 참고)
```

Replace with:
```yaml
  schedule:
    - cron: '0 12 * * 2-7,0'  # Phase 13: 화~일 매일 21:00 KST (월요일은 weekly.yml 이 담당)
```

(The Korean header comment at line 29-31 about settings.yaml sync may be left as-is — that decoupling is SPEC §Out-of-scope.)

3. **Run step** (line 118-126) — append --mode=daily:

Find:
```yaml
      - name: Run daily digest pipeline
        id: run_pipeline
        run: pnpm tsx src/main.ts
```

Replace with:
```yaml
      - name: Run daily digest pipeline
        id: run_pipeline
        run: pnpm tsx src/main.ts --mode=daily
```

4. **git-auto-commit-action file_pattern** (line 127-132) — add state/pending.json:

Find:
```yaml
      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          commit_message: 'chore(state): update seen items and archive [skip ci]'
          file_pattern: 'state/seen.json'
```

Replace with:
```yaml
      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          # Phase 13 D-23: atomic commit groups pending.json + seen.json so
          # a mid-run crash leaves both files at the prior-run snapshot —
          # idempotent recovery on the next daily run (same input → same output).
          commit_message: 'chore(state): daily — update seen + pending [skip ci]'
          file_pattern: 'state/pending.json state/seen.json'
```

5. **Open Issue on failure block** (line 133-170) — leave unchanged. The auto-issue body's diagnosis table is still valid for daily failures.

Run `pnpm sync-schedule --dry-run` if applicable to confirm no sync script complains; if the script asserts settings.yaml cron equality, it WILL fail because settings.yaml schedule.cron stays at `'0 12 * * 1'` (SPEC §Out-of-scope). If the script blocks the workflow, comment out the assertion with a TODO pointing to SPEC §Out-of-scope §1.

Verify with `cat .github/workflows/daily.yml | head -45` that cron and headers are correct.
  </action>
  <verify>
    <automated>grep -c "cron: '0 12 \* \* 2-7,0'" .github/workflows/daily.yml && grep -c "tsx src/main.ts --mode=daily" .github/workflows/daily.yml && grep -c "state/pending.json state/seen.json" .github/workflows/daily.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cron: '0 12 \* \* 2-7,0'" .github/workflows/daily.yml` returns 1.
    - `grep -c "0 12 \* \* 1'" .github/workflows/daily.yml` returns 0 (old cron gone — old Mon-only).
    - `grep -c "tsx src/main.ts --mode=daily" .github/workflows/daily.yml` returns 1.
    - `grep -c "file_pattern: 'state/pending.json state/seen.json'" .github/workflows/daily.yml` returns 1.
    - `grep -c "\\[skip ci\\]" .github/workflows/daily.yml` returns at least 1 (DEDUP-06 preserved).
    - `grep -c "group: digest-pipeline" .github/workflows/daily.yml` returns 1.
    - `grep -c "workflow_dispatch:" .github/workflows/daily.yml` returns 1.
    - YAML parse sanity: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/daily.yml'))"` exits 0.
  </acceptance_criteria>
  <done>
    daily.yml cron now Tue-Sun. Entry uses --mode=daily. Commit step's file_pattern includes pending.json + seen.json (atomic per D-23). concurrency group, workflow_dispatch, [skip ci], shin-kim Thawte block, Open Issue on failure block all preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create .github/workflows/weekly.yml — Mon cron, --mode=weekly entry, pending+seen+archive atomic commit, shared concurrency group</name>
  <files>.github/workflows/weekly.yml</files>
  <read_first>
    - .github/workflows/daily.yml (post-Task-1 version — full template)
    - src/main.ts (Plan 13-05 — --mode=weekly entry)
    - .planning/phases/13-1-gemini-rpd/13-CONTEXT.md D-23
    - .planning/phases/13-1-gemini-rpd/13-SPEC.md §Requirement 6 + AC-6
    - .planning/phases/13-1-gemini-rpd/13-PATTERNS.md ".github/workflows/weekly.yml" section
  </read_first>
  <action>
Create `.github/workflows/weekly.yml` as a NEW file. Mirror daily.yml structure VERBATIM with only these differences: name, cron, run command, commit_message, file_pattern, and ON failure issue title.

Full file body:

```yaml
# .github/workflows/weekly.yml
#
# LegalNewsletter — Phase 13 weekly digest dispatch (Mon).
#
# Runs src/main.ts --mode=weekly at 12:00 UTC (21:00 KST) every Monday.
# Reads state/pending.json (accumulated by daily.yml Tue-Sun runs), composes
# the digest (or heartbeat if empty per D-15), sends via Gmail SMTP, archives
# the HTML, then truncates pending.json + writes seen.json lastUpdated.
#
# Concurrency group SHARED with daily.yml (group: digest-pipeline) so a
# Sunday daily run that overshoots into Monday cannot collide with the
# weekly run. cancel-in-progress: false serializes the two; whichever started
# first runs to completion.
#
# Atomic state commit groups state/pending.json + state/seen.json +
# archive/**/*.html into one commit (D-23). [skip ci] in message prevents
# workflow loops.
#
# Defense-in-depth retained: shin-kim Thawte intermediate cert installed
# (even though weekly never fetches — leaving the install step idempotent
# is cheaper than maintaining two divergent install paths), Open Issue on
# failure step with the same bilingual remediation table.

name: Weekly Digest

on:
  # Phase 13 cadence — config/settings.yaml decoupling tracked separately
  # (SPEC §Out-of-scope §1 / §2).
  schedule:
    - cron: '0 12 * * 1'  # 월요일 21:00 KST = 12:00 UTC
  workflow_dispatch: {} # manual trigger for smoke tests / gap closure

concurrency: # OPS-02 — serialize overlapping runs (shared with daily.yml)
  group: digest-pipeline
  cancel-in-progress: false

permissions:
  contents: write # required by stefanzweifel/git-auto-commit-action
  issues: write # required for failure-notification step (gh issue create)

jobs:
  digest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v5
      - uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: 'pnpm'
      - run: pnpm install --frozen-lockfile
      - name: Cache Playwright browser binary
        id: playwright-cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright chromium shell (on cache miss)
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        # weekly does not fetch but keeps the install idempotent: a future
        # plan that adds rendering to the weekly path (e.g., final-pass body
        # re-fetch) inherits the install for free, and the cache step costs
        # ~0s on warm caches.
        run: pnpm exec playwright install chromium --only-shell --with-deps

      - name: Write Thawte TLS RSA CA G1 intermediate (defense-in-depth)
        # weekly does not fetch shin-kim's site; this step is preserved for
        # parity with daily.yml so the two workflows can be diffed safely.
        # Remove only when daily.yml's shin-kim Thawte block is also removed.
        run: |
          cat > /tmp/thawte-tls-rsa-ca-g1.pem << 'EOF'
          -----BEGIN CERTIFICATE-----
          
          TYNYQI+BXG1R1cABlold
          -----END CERTIFICATE-----
          EOF

      - name: Run weekly digest pipeline
        id: run_pipeline
        run: pnpm tsx src/main.ts --mode=weekly
        env:
          # GEMINI_API_KEY deliberately omitted — weekly never calls Gemini
          # (D-22 [METRIC] geminiCallCount=0 always). If a future plan wires
          # re-summarization or hint generation into weekly, re-add the env.
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          RECIPIENT_EMAIL: ${{ secrets.RECIPIENT_EMAIL }}
          NODE_EXTRA_CA_CERTS: /tmp/thawte-tls-rsa-ca-g1.pem

      - uses: stefanzweifel/git-auto-commit-action@v7
        with:
          # Phase 13 D-23: weekly atomic commit covers pending truncate + seen
          # lastUpdated + new archive HTML. Same atomicity rationale as daily.
          commit_message: 'chore(state): weekly — sent digest, truncate pending [skip ci]'
          file_pattern: 'state/pending.json state/seen.json archive/**/*.html'

      - name: Open issue on failure
        if: failure()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh issue create \
            --title "Weekly digest failed — run ${{ github.run_id }}" \
            --body "$(cat <<'EOF'
          **Workflow run:** https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}
          **Commit:** ${{ github.sha }}
          **Triggered by:** ${{ github.event_name }}
          **Started:** ${{ github.event.repository.updated_at }}

          ## Likely causes (check in order)

          | Symptom in logs | Fix |
          |-----------------|-----|
          | \`ZodError ... pending.json\` or \`Unsupported pending version\` | state/pending.json 형식 오류 — daily 가 만든 schema 와 weekly 의 zod schema 불일치 가능성. 코드 변경 없이 \`state/pending.json\` 백업 후 \`[]\`로 초기화하여 다음 weekly 가 empty heartbeat 발송하도록 복구 |
          | \`535 5.7.8\` or \`GMAIL_AUTH_FAILURE\` | App Password 만료/폐기 — https://myaccount.google.com/apppasswords 에서 재발급 후 \`GMAIL_APP_PASSWORD\` secret 업데이트 |
          | \`ZodError ... recipient\` | \`RECIPIENT_EMAIL\` 형식 오류 — Settings → Secrets 에서 수정 |
          | \`ZodError ... firms\` | \`config/firms.yaml\` 형식 오류 |
          | \`ENOENT.*pending.json\` | first-run 또는 pending 파일 부재 — 정상 동작 (heartbeat 발송) 아닌 다른 에러면 daily 가 한 번도 안 돌았다는 신호 |

          ## Next steps

          1. 위 run 링크에서 전체 로그 확인
          2. 원인 파악 후 수정
          3. Actions 탭 → "Weekly Digest" → **Run workflow** 로 수동 재실행
          4. 재실행 성공 확인 후 이 issue close

          ---
          _Auto-created by .github/workflows/weekly.yml failure step._
          EOF
          )"
```

After writing, verify YAML parse and confirm cron is Monday-only.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/weekly.yml'))" && grep -c "cron: '0 12 \* \* 1'" .github/workflows/weekly.yml && grep -c "tsx src/main.ts --mode=weekly" .github/workflows/weekly.yml</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/workflows/weekly.yml` exists; `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/weekly.yml'))"` exits 0.
    - `grep -c "cron: '0 12 \* \* 1'" .github/workflows/weekly.yml` returns 1.
    - `grep -c "tsx src/main.ts --mode=weekly" .github/workflows/weekly.yml` returns 1.
    - `grep -c "file_pattern: 'state/pending.json state/seen.json archive/\*\*/\*.html'" .github/workflows/weekly.yml` returns 1.
    - `grep -c "group: digest-pipeline" .github/workflows/weekly.yml` returns 1 (shared concurrency).
    - `grep -c "workflow_dispatch:" .github/workflows/weekly.yml` returns 1.
    - `grep -c "\\[skip ci\\]" .github/workflows/weekly.yml` returns at least 1.
    - `grep -c "GEMINI_API_KEY" .github/workflows/weekly.yml` returns 0 (weekly does not need it; intentional omission for least-privilege).
    - `grep -c "name: Weekly Digest" .github/workflows/weekly.yml` returns 1.
  </acceptance_criteria>
  <done>
    weekly.yml exists with Mon cron, --mode=weekly entry, atomic commit covering pending+seen+archive, shared concurrency group, workflow_dispatch trigger, Open Issue on failure block. GEMINI_API_KEY intentionally omitted from env (weekly does not call Gemini).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — manual workflow_dispatch trigger for both daily.yml and weekly.yml</name>
  <files>.github/workflows/daily.yml, .github/workflows/weekly.yml (verified via GitHub Actions UI — no local file edits)</files>
  <action>
    Manual verification only — Claude has automated everything possible in Task 1 (daily.yml cron + entry + file_pattern edits) and Task 2 (weekly.yml new file). What remains is the operator-side push + workflow_dispatch trigger because GitHub Actions runs on the GH-hosted runner, not in Claude's environment. The full verification protocol is documented in &lt;how-to-verify&gt; below. Claude pauses until the operator types "approved" or pastes a specific failure.
  </action>
  <what-built>
    Plan 13-01 through 13-05 produced:
      - state/pending.json reader/writer module + COMP-05 enforcement
      - geminiCallCount module-level counter + [METRIC] step-summary line
      - runDaily.ts + runWeekly.ts entry functions
      - main.ts mode dispatch + checkFirm.ts swap
      - Heartbeat composer for empty weeks
    Plan 13-06 Task 1 + 2 produced:
      - .github/workflows/daily.yml — cron now Tue-Sun (`0 12 * * 2-7,0`), entry `tsx src/main.ts --mode=daily`, commit covers state/pending.json + state/seen.json
      - .github/workflows/weekly.yml — NEW file, cron Mon (`0 12 * * 1`), entry `tsx src/main.ts --mode=weekly`, commit covers state/pending.json + state/seen.json + archive/**/*.html
      - Both workflows share `concurrency: digest-pipeline` group (race prevention)
  </what-built>
  <how-to-verify>
    1. **Push the branch to origin** (Plan 13-07 will run after the verification confirms the YAML works end-to-end).
       - The workflows must be on a branch GitHub Actions can see. If the user works on `main`, the push happens after the commit step. If on a feature branch, ensure GH UI's workflow file selector shows both `Daily Digest` and `Weekly Digest`.

    2. **Manual trigger: daily**
       - GitHub UI → Actions tab → `Daily Digest` workflow → `Run workflow` → branch dropdown selects the right branch → click `Run workflow`.
       - Wait for the run to complete (typically ~90s on warm Playwright cache, ~3min on cold).
       - Open the run summary view. Look for:
         - **TOP OF JOB SUMMARY**: a line that starts with `[METRIC] geminiCallCount=` followed by an integer. Expected ≤ 50 (typical 5~15).
         - **No errors in `Run daily digest pipeline` step** (green check).
         - **Commit step**: confirms `state/pending.json` and `state/seen.json` both appear in the commit diff. View the auto-committed commit on the branch — should show both files in the diff.

    3. **Inspect the new commit**
       - `git pull origin <branch>` locally.
       - `cat state/pending.json` — should contain `version: 1`, `windowStart: "<recent ISO>"`, and `items: [...]` with PendingItem shape (firmId, title, url, language, summary_ko, summaryConfidence, summaryModel, summarizedAt). MUST NOT contain `description`, `isClusterMember`, or `isNew` fields (COMP-05 enforcement).
       - `cat state/seen.json` — `lastUpdated` should match the run timestamp.
       - Verify the auto-commit message includes `[skip ci]` and uses the new `chore(state): daily — update seen + pending [skip ci]` format.

    4. **Manual trigger: weekly**
       - GitHub UI → Actions tab → `Weekly Digest` workflow → `Run workflow` → click `Run workflow`.
       - Wait for the run to complete.
       - Look for:
         - **TOP OF JOB SUMMARY**: `[METRIC] geminiCallCount=0` (weekly never calls Gemini).
         - **Email arrives** at the configured RECIPIENT_EMAIL. If pending.json has items from step 2, the subject should be `[법률 다이제스트] YYYY-MM-DD (N firms, M items)`. If pending.json was empty (cleared by a prior weekly run), the subject should be `[법률 다이제스트] YYYY-MM-DD (이번 주 신규 없음)`.
         - **Commit step**: confirms `state/pending.json` (now items=[] post-truncate), `state/seen.json` (lastUpdated bumped), and possibly `archive/YYYY/MM-DD.html` (new archive file) appear in the commit diff.

    5. **Verify race prevention** (optional but recommended)
       - Trigger daily manually again immediately after the weekly run starts (within ~5 seconds). The second trigger should queue per `concurrency: digest-pipeline` and only run after the first completes (look for the "Pending — Waiting for a previous run to complete" indicator in GH UI).

    6. **Type "approved" if all of the following are true**:
       - daily workflow run succeeded, `[METRIC] geminiCallCount=N` appeared at top of summary with integer N
       - pending.json appeared in the daily auto-commit with valid PendingItem shape and NO `description` field
       - weekly workflow run succeeded, `[METRIC] geminiCallCount=0` appeared
       - email arrived with EMAIL-04 prefix and correct subject (digest or heartbeat based on pending state)
       - post-weekly pending.json shows items: []
       - shared concurrency group serialized overlapping triggers

    7. **Type "blocked: <reason>" or paste the specific failure** if anything in steps 2-5 went wrong.

    Note: SPEC §AC-7 also requires a 7-day continuous dry-run (Tue-Sun + Mon) to verify daily-average geminiCallCount ≤ 15 / weekly === 0 with real production cron firing. This checkpoint covers the manual-dispatch infrastructure verification; the 7-day acceptance is tracked separately as a follow-up acceptance after Plan 13-07 lands and the first natural cron cycle completes.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification — see &lt;how-to-verify&gt; steps; operator types 'approved' to resume"</automated>
  </verify>
  <done>
    Operator has triggered both workflows via workflow_dispatch on the GH UI, confirmed [METRIC] markers, atomic commits with the right file_pattern, heartbeat-or-digest email arrived as expected, and post-weekly pending.json is empty.
  </done>
  <resume-signal>Type "approved" if all six bullet points pass, OR paste the specific failure / log excerpt to investigate</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cron→runner | GHA cron schedule fires the workflow; cron drift or overlap with weekly is a race surface |
| runner→state-commit | git-auto-commit-action stages files matching file_pattern; misconfiguration loses commit grouping (D-23) |
| daily↔weekly concurrency | Both workflows share state files; Mon dispatch can race a Sun overshoot — concurrency group is the sole guard |
| YAML→shell | run command receives --mode flag from YAML; YAML typo in mode value is caught by Plan 13-05 parseMode exit-2 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-06-01 | Tampering | daily.yml cron value typo (e.g., `'0 12 * * 2-7,0,1'` accidentally including Mon) → race with weekly | mitigate | Task 1 acceptance grep asserts exact string `'0 12 * * 2-7,0'`; Task 3 human-verify inspects GH UI cron rendering |
| T-13-06-02 | Tampering | weekly.yml file_pattern misses archive/**/*.html → archive file orphaned (in repo working tree but not committed) | mitigate | Task 2 acceptance grep asserts exact triple `'state/pending.json state/seen.json archive/**/*.html'`; Task 3 human-verify checks archive shows up in auto-commit diff |
| T-13-06-03 | Race / DoS | daily.yml's Sun 21:00 KST run extends past weekly.yml's Mon 21:00 KST start | mitigate (HIGH) | Both workflows declare `concurrency: { group: digest-pipeline, cancel-in-progress: false }` — second trigger queues until first completes. Task 3 step 5 verifies queuing behavior in GH UI |
| T-13-06-04 | Information Disclosure | weekly.yml env block leaks GEMINI_API_KEY (weekly doesn't need it) | mitigate (HIGH) | Task 2 acceptance grep asserts `GEMINI_API_KEY` count == 0 in weekly.yml — least-privilege; runner image doesn't see the secret on weekly runs |
| T-13-06-05 | Repudiation | git commit message missing [skip ci] → infinite workflow loop | mitigate | Task 1 + Task 2 acceptance grep `[skip ci]` count ≥ 1 (DEDUP-06 preserved) |
| T-13-06-06 | Tampering | shared concurrency group name typo (e.g., `digest_pipeline` underscore) splits the lock | mitigate | Task 1 + Task 2 acceptance grep asserts exact `group: digest-pipeline` in both files |
| T-13-06-07 | Information Disclosure | weekly auto-issue body echoes pending.json content (potentially leaking firm-specific summaries) | accept | Auto-issue body only references `ZodError ... pending.json` as a symptom; no JSON content interpolated. `EOF` heredoc isolates GitHub Actions context variables from user data |
</threat_model>

<verification>
- Task 1 + Task 2 grep + YAML parse acceptance pass
- Task 3 human-verify checkpoint: both workflows trigger successfully via workflow_dispatch, [METRIC] markers visible, atomic commits include the right file_pattern, race-prevention concurrency observed
</verification>

<success_criteria>
- daily.yml: cron Tue-Sun, --mode=daily entry, pending+seen atomic commit
- weekly.yml: new file, Mon cron, --mode=weekly entry, pending+seen+archive atomic commit, GEMINI_API_KEY omitted from env (least-privilege)
- Both share `concurrency: digest-pipeline` group
- Both include `workflow_dispatch:` and `[skip ci]` in commits
- Human-verify checkpoint passes (or operator documents the specific failure)
</success_criteria>

<output>
After completion (post-checkpoint), create `.planning/phases/13-1-gemini-rpd/13-06-SUMMARY.md` capturing:
- daily.yml deltas (cron, run command, commit file_pattern)
- weekly.yml additions (new file)
- Manual dispatch verification result (approval timestamp + observed [METRIC] values)
- Concurrency group sharing confirmed
- Pattern 2 / shin-kim Thawte block / Open Issue block preservation
- Outstanding SPEC AC-7 7-day acceptance: deferred to natural cron cycle observation after Plan 13-07 closes
</output>
