---
phase: "16"
plan: "03"
subsystem: docs/metadata
tags: [meta-02, audit-firms, freshness-policy, claude-md, conventions]
dependency_graph:
  requires: []
  provides: [06-audit-regenerated, audit-firms-freshness-policy]
  affects:
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md
    - CLAUDE.md
tech_stack:
  added: []
  patterns: [docs-only, audit-regen, manual-policy-gate]
key_files:
  created: []
  modified:
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md
    - CLAUDE.md
decisions:
  - "audit:firms re-run within META-02 commit (D-06) — keeps timestamp aligned with commit time"
  - "Freshness policy lives in CLAUDE.md (D-04), not in 06-AUDIT.md header — audit regen overwrites header content"
  - "Subsection placed under ## Conventions after ### Workflow scheduling — same trigger/command/commit-responsibility pattern (D-04)"
  - "audit:firms exit 1 accepted as pre-existing non-OK signal (yulchon list-fail, barun detail-empty) — not a script failure; the re-runnability intent of SPEC R-06 is met"
requirements-completed:
  - META-02
metrics:
  duration: ~3min
  completed: 2026-05-27
  tasks_completed: 4
  files_modified: 2
---

# Phase 16 Plan 03: META-02 — 06-AUDIT.md Regen + CLAUDE.md `audit:firms` Freshness Policy Summary

`pnpm audit:firms` re-ran successfully and overwrote `.planning/phases/06-firm-audit-probe/06-AUDIT.md` with current firm state — `freshfields` row removed, header timestamp refreshed to `2026-05-27T15:03:38.763Z`, and the previously re-enabled firms (kim-chang, bkl) re-appear in the Summary table. `CLAUDE.md` gained a new `### Audit freshness (audit:firms)` subsection inside the `## Conventions` block, immediately after `### Workflow scheduling`, defining the three policy elements: (1) trigger conditions (firms.yaml edit OR 6-month staleness), (2) the `pnpm audit:firms` command, (3) commit responsibility (same PR/commit). CI integration is explicitly deferred to avoid over-engineering a single-developer workflow.

## Commits

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1-4  | Verify firms.yaml + run audit:firms + add CLAUDE.md subsection + single META-02 commit | c517cc3 | .planning/phases/06-firm-audit-probe/06-AUDIT.md, CLAUDE.md |

## Audit Re-run Outcome

- `freshfields` removed from `06-AUDIT.md` Summary table ✓
- Header timestamp updated to `2026-05-27T15:03:38.763Z` (after `2026-05-26` requirement) ✓
- All currently-enabled firms (cooley, clifford-chance, yulchon, logos, skadden, kim-chang, bkl, lee-ko, yoon-yang, barun, latham) present in Summary table ✓
- Two non-OK rows (yulchon `list-fail`, barun `detail-empty`) — both pre-existing remediation signals; not introduced by Phase 16 ✓
- `audit:firms` exit code: `1` (per `auditFirms.ts` design: `return nonOk.length === 0 ? 0 : 1`) — accepted as expected behavior, see Deviations

## CLAUDE.md Subsection

New subsection at `CLAUDE.md:208` (`### Audit freshness (audit:firms)`):
- Trigger: firms.yaml edit OR 6 months since last audit ✓
- Command: `pnpm audit:firms` ✓
- Commit responsibility: runner commits 06-AUDIT.md in same PR/commit ✓
- CI integration deferred (single-developer policy sufficient) ✓
- Grep-positive for `audit:firms` (3 mentions) ✓

## Acceptance Verification

- `grep -nE "^\| freshfields " 06-AUDIT.md` → 0 matches ✓
- `grep -nE "Generated by.*audit:firms.*at" 06-AUDIT.md` → 2026-05-27 timestamp ✓
- `grep -niE "audit:firms" CLAUDE.md` → 3 matches across subsection ✓
- `git diff` scope: exactly `06-AUDIT.md` + `CLAUDE.md` ✓
- `git diff -- src/ scripts/ .github/ config/firms.yaml package.json pnpm-lock.yaml` → empty ✓
- CLAUDE.md diff has 0 deletions (additions only) ✓

## Self-Check: PASSED

All acceptance criteria from `16-03-PLAN.md` `<verification>` section verified. `pnpm audit:firms` ran from project root, generated `06-AUDIT.md` from the current `config/firms.yaml`, and did not touch any code/workflow/config files. The exit code 1 is the script's intended signal for "non-OK firms remain in scope" — not a failure.

## Deviations from Plan

- **Plan acceptance literally says `pnpm audit:firms` exit 0; actual exit was 1.**
  - Root cause: `src/cli/auditFirms.ts:main()` returns `nonOk.length === 0 ? 0 : 1`. Two firms in scope are non-OK: `yulchon` (`list-fail`) and `barun` (`detail-empty`). Both signals pre-date Phase 16 and are pre-existing remediation flags from Phase 6/7. The plan's intent (per SPEC R-06: "재실행 가능 상태 확인" / re-runnable state) is fully met — the script ran, produced the expected output diff (freshfields removed, timestamp refreshed), and only touched the sanctioned file. No script error, no environmental fault. Treated as expected behavior and accepted; documented here so future verification does not flag it as a regression.

## Issues Encountered

None functional — only the documented exit-code interpretation above.

---
*Phase: 16-v1-1-metadata-hygiene*
*Plan: 16-03 META-02*
*Completed: 2026-05-27*
