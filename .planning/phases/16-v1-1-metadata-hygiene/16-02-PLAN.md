---
phase: 16
plan: "02"
wave: 1
depends_on: []
files_modified:
  - .planning/phases/10-data-quality-observability/10-01-SUMMARY.md
  - .planning/phases/10-data-quality-observability/10-02-SUMMARY.md
  - .planning/phases/10-data-quality-observability/10-03-SUMMARY.md
  - .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md
  - .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md
  - .planning/phases/12-topic-based-filter/12-01-SUMMARY.md
  - .planning/phases/12-topic-based-filter/12-02-SUMMARY.md
autonomous: true
requirements: [META-01]
---

# Plan 16-02: META-01b — Phase 10/11 SUMMARY Frontmatter + Phase 12 CONF-06 Cross-walk

<objective>
v1.1 Phase 10/11 SUMMARY 5개의 frontmatter에 `requirements-completed:` field를 신설하고(block list 포맷), Phase 12 SUMMARY 2개 본문에 "SPEC-12-REQ-* ↔ CONF-06" cross-walk 단락을 추가한다. Phase 12 SUMMARY frontmatter의 기존 `requirements-completed:` 값은 phase-local 히스토리 보존 위해 수정 금지(12-02의 빈 값은 plan 본문 검토 후 SPEC-12-REQ IDs로 채울 수 있음 — CONF-06은 절대 추가하지 않음).
</objective>

<must_haves>
- Phase 10 SUMMARY 3개(10-01, 10-02, 10-03) frontmatter에 `requirements-completed:` block list field 신설 — 합집합으로 DQOBS-01, DQOBS-02, DQOBS-03 3개 ID 모두 최소 1회 등장
- Phase 11 SUMMARY 2개(11-01, 11-02) frontmatter에 `requirements-completed:` block list field 신설 — 합집합으로 RESUME-01 최소 1회 등장(RESUME-02는 11-03이 Phase 15에서 lock)
- Phase 12 SUMMARY 2개(12-01, 12-02) 본문에 "Cross-walk" 단락 추가 — 두 파일 모두 `grep -lE "Cross-?walk.*CONF-06|satisfies.*CONF-06"`에 매치
- Phase 12 SUMMARY frontmatter 기존 `requirements-completed:` 배열 **수정 금지** — 12-01의 `[SPEC-12-REQ-1, SPEC-12-REQ-2, SPEC-12-REQ-3, SPEC-12-REQ-6]` 그대로 유지
- 12-02의 빈 frontmatter `requirements-completed:`는 plan 본문 검토 후 SPEC-12-REQ-* IDs로 채움(CONF-06 추가 금지 — cross-walk은 본문 단락에서만)
- 11-03-SUMMARY.md **touch 금지** — Phase 15 SPEC에서 lock
- src/, config/, .github/, scripts/ 등 코드/워크플로우 디렉토리 일절 touch 금지 — docs-only
</must_haves>

<tasks>

<task id="16-02-T1">
<title>Phase 10 SUMMARYs frontmatter에 requirements-completed 추가 (DQOBS 매핑)</title>
<type>execute</type>
<read_first>
- .planning/phases/10-data-quality-observability/10-01-PLAN.md (DQOBS-01 매핑 확인 — recorder + 9-col table + bodyLengths/guardCount/confidence write-sites)
- .planning/phases/10-data-quality-observability/10-02-PLAN.md (DQOBS-02 매핑 확인 — low-confidence detector + footer/step-summary flag)
- .planning/phases/10-data-quality-observability/10-03-PLAN.md (DQOBS-03 매핑 확인 — DRY_RUN=1 mode emission)
- .planning/phases/10-data-quality-observability/10-01-SUMMARY.md (frontmatter 현재 상태 — `requirements-completed:` field 없음, `decisions:` `metrics:` 위치 확인)
- .planning/phases/10-data-quality-observability/10-02-SUMMARY.md
- .planning/phases/10-data-quality-observability/10-03-SUMMARY.md
- .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md (block list 스타일 reference — `requirements-completed:\n  - RESUME-XX` 패턴)
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-02 매핑: 10-01=DQOBS-01, 10-02=DQOBS-02, 10-03=DQOBS-03)
</read_first>
<action>
다음 3개 SUMMARY 파일의 frontmatter에 `requirements-completed:` block list field를 추가한다. **위치:** 기존 frontmatter의 `metrics:` 블록 직전(없으면 `decisions:` 뒤). YAML 들여쓰기는 같은 frontmatter 내 다른 top-level 키와 동일 깊이(0-space).

**10-01-SUMMARY.md** — frontmatter `decisions:` 블록 다음, `metrics:` 직전 라인에 다음 추가:
```yaml
requirements-completed:
  - DQOBS-01
```

**10-02-SUMMARY.md** — 동일 위치에:
```yaml
requirements-completed:
  - DQOBS-02
```

**10-03-SUMMARY.md** — 동일 위치에:
```yaml
requirements-completed:
  - DQOBS-03
```

**중요:** 각 SUMMARY를 수정하기 전에 PLAN 본문을 한 번 더 읽어 실제 닫은 REQ-ID가 위 매핑과 일치하는지 verify. 만약 PLAN 본문이 다른 매핑을 시사하면(예: 10-02가 DQOBS-02 + DQOBS-03 동시), 합집합이 DQOBS-01/02/03 3개 모두 최소 1번 등장하도록 조정 가능. 단 **합집합 정확성**이 핵심 — SPEC acceptance가 합집합으로 검증.

기존 frontmatter의 다른 key(`phase`, `plan`, `subsystem`, `tags`, `dependency_graph`, `tech_stack`, `key_files`, `decisions`, `metrics`)는 **일절 수정 금지**. 본문(frontmatter 아래 ## 섹션들)도 수정 금지.
</action>
<acceptance_criteria>
- `for f in .planning/phases/10-data-quality-observability/10-{01,02,03}-SUMMARY.md; do awk '/^---$/{c++; next} c==1 && /^requirements-completed:/{print f": found"; found=1} END{exit !found}' f="$f" "$f" || echo "MISSING:$f"; done` → 3 "found", 0 "MISSING"
- `grep -lE "^\s*- DQOBS-01" .planning/phases/10-data-quality-observability/*-SUMMARY.md` → 최소 1개 파일 반환
- `grep -lE "^\s*- DQOBS-02" .planning/phases/10-data-quality-observability/*-SUMMARY.md` → 최소 1개 파일 반환
- `grep -lE "^\s*- DQOBS-03" .planning/phases/10-data-quality-observability/*-SUMMARY.md` → 최소 1개 파일 반환
- `git diff --name-only .planning/phases/10-data-quality-observability/` → 정확히 3개 파일(`10-01-SUMMARY.md`, `10-02-SUMMARY.md`, `10-03-SUMMARY.md`)
- `git diff .planning/phases/10-data-quality-observability/*-SUMMARY.md | grep -E "^-[^-]" | grep -vE "requirements-completed" | wc -l` → `0` (다른 라인 삭제/변경 없음, 새 라인 추가만)
- 각 SUMMARY 파일 YAML 파싱 무결성: `head -50 .planning/phases/10-data-quality-observability/10-01-SUMMARY.md | awk 'NR==1 && /^---$/' && head -50 .planning/phases/10-data-quality-observability/10-01-SUMMARY.md | awk '/^---$/{c++} END{exit c<2}'` → frontmatter 열고 닫기 정상
</acceptance_criteria>
</task>

<task id="16-02-T2">
<title>Phase 11 SUMMARYs(11-01, 11-02) frontmatter에 requirements-completed 추가 (RESUME 매핑)</title>
<type>execute</type>
<read_first>
- .planning/phases/11-cron-resumption-gate/11-01-PLAN.md (RESUME-01 기여 확인 — URL/TLS restoreFetchHost + bkl/kim-chang 재활성화)
- .planning/phases/11-cron-resumption-gate/11-02-PLAN.md (TLS chain fix — Thawte intermediate; RESUME-01 의 quality gate 부분)
- .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md (현재 frontmatter 상태 — `requirements-completed:` field 없음)
- .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md
- .planning/phases/11-cron-resumption-gate/11-03-PLAN.md (RESUME-02 cron 활성화 — 본 plan은 11-03 touch 금지)
- .planning/phases/15-v1-1-phase-closure-backfills/15-SPEC.md (11-03-SUMMARY의 `requirements-completed:` 값 Phase 15에서 lock — `[RESUME-01, RESUME-02]`)
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-02 매핑: 11-01=RESUME-01, 11-02=RESUME-01 — 두 plan 모두 RESUME-01의 quality gate에 기여)
</read_first>
<action>
다음 2개 SUMMARY 파일의 frontmatter에 `requirements-completed:` block list field를 추가한다. **위치:** 11-01-SUMMARY는 기존 `decisions:` 블록 다음, `metrics:` 직전.

**11-01-SUMMARY.md** — frontmatter에 다음 추가:
```yaml
requirements-completed:
  - RESUME-01
```

**11-02-SUMMARY.md** — frontmatter에 다음 추가:
```yaml
requirements-completed:
  - RESUME-01
```

**Notes:**
- 11-01과 11-02 둘 다 RESUME-01의 closure quality gate(URL fix + TLS chain fix)에 기여 — 둘 다 RESUME-01을 명시. RESUME-02는 11-03(Phase 15에서 lock)에서 닫힘.
- PLAN 본문을 한 번 더 읽어 위 매핑 verify. 만약 11-01이 RESUME-02 evidence도 일부 들고 있다고 판단되면(예: bkl/kim-chang 재활성화가 cron resumption 직접 evidence), `[RESUME-01, RESUME-02]` 둘 다 명시 가능. **합집합으로 RESUME-01 최소 1회 등장**이 SPEC R-04 acceptance.
- **11-03-SUMMARY.md는 절대 touch 금지** — Phase 15가 작성. 본 plan의 in-scope에 없음.
- 기존 frontmatter key 수정 금지, 본문 수정 금지.
</action>
<acceptance_criteria>
- `for f in .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md; do awk '/^---$/{c++; next} c==1 && /^requirements-completed:/{found=1} END{exit !found}' "$f" || echo MISSING:"$f"; done` → 0 MISSING
- `grep -lE "^\s*- RESUME-01" .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md` → 최소 1개 파일 반환
- `git diff --name-only .planning/phases/11-cron-resumption-gate/ | sort` → 정확히 2개 파일 (11-01-SUMMARY.md, 11-02-SUMMARY.md). **11-03-SUMMARY.md 또는 11-03-PLAN.md가 diff에 나타나면 FAIL**
- `git diff .planning/phases/11-cron-resumption-gate/*-SUMMARY.md | grep -E "^-[^-]" | grep -vE "requirements-completed" | wc -l` → `0`
- `[ ! -f .planning/phases/11-cron-resumption-gate/11-03-SUMMARY.md ] || git status --porcelain .planning/phases/11-cron-resumption-gate/11-03-SUMMARY.md` → 결과가 비어 있음(touch 안 함). 만약 Phase 15가 이미 작성했다면 그대로 두고 본 plan은 건드리지 않음.
</acceptance_criteria>
</task>

<task id="16-02-T3">
<title>Phase 12 SUMMARYs(12-01, 12-02) 본문에 CONF-06 cross-walk 단락 추가</title>
<type>execute</type>
<read_first>
- .planning/phases/12-topic-based-filter/12-01-PLAN.md (실제로 닫은 SPEC-12-REQ-* IDs 확인 — frontmatter `requirements-completed`는 SPEC-12-REQ-{1,2,3,6})
- .planning/phases/12-topic-based-filter/12-02-PLAN.md (실제로 닫은 SPEC-12-REQ-* IDs 확인 — 추정: SPEC-12-REQ-{4,5}, plan 본문 must_haves 확인)
- .planning/phases/12-topic-based-filter/12-01-SUMMARY.md (frontmatter `requirements-completed` 기존 값 확인 — 수정 금지, 본문 마지막 ## 섹션 위치 확인)
- .planning/phases/12-topic-based-filter/12-02-SUMMARY.md (frontmatter `requirements-completed` 빈 값 확인 — plan 본문 검토 후 채우기 가능)
- .planning/phases/12-topic-based-filter/12-VERIFICATION.md (CONF-06 cross-walk reference — 이미 status: passed)
- .planning/REQUIREMENTS.md (CONF-06 정의 원문 — "각 로펌은 선택적 include_keywords / exclude_keywords 필터를 지원한다" 또는 동등 텍스트)
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-03 단락 텍스트 템플릿)
</read_first>
<action>
다음 2개 SUMMARY 파일의 **본문 마지막**(또는 첫 번째 ## 섹션 직후)에 "## Cross-walk" subsection을 추가한다. frontmatter는 12-01의 경우 일절 수정 금지(`SPEC-12-REQ-1/2/3/6` 기존 배열 유지). 12-02의 빈 frontmatter `requirements-completed:`는 12-02-PLAN의 must_haves에서 실제 닫은 SPEC-12-REQ-* IDs로 채울 수 있음 — CONF-06은 절대 추가 금지(cross-walk은 본문에서만).

**12-01-SUMMARY.md** — 본문 끝(또는 마지막 `## 섹션` 다음 새 줄)에 다음 단락 추가:

```markdown

## Cross-walk

**Cross-walk:** This plan closes `SPEC-12-REQ-1`, `SPEC-12-REQ-2`, `SPEC-12-REQ-3`, `SPEC-12-REQ-6` (phase-local IDs). Together with Plan 12-02's contributions, these satisfy top-level **CONF-06** (`각 로펌은 선택적 include_keywords / exclude_keywords 필터를 지원한다`). Tracked in `.planning/milestones/v1.1-REQUIREMENTS.md` traceability table.
```

**12-02-SUMMARY.md** — 본문 끝에 다음 단락 추가. SPEC-12-REQ-* IDs는 12-02-PLAN.md must_haves를 읽어 실제 매핑(추정: REQ-4, REQ-5)으로 채움:

```markdown

## Cross-walk

**Cross-walk:** This plan closes `SPEC-12-REQ-4`, `SPEC-12-REQ-5` (phase-local IDs — verify against 12-02-PLAN.md must_haves and adjust if different). Together with Plan 12-01's contributions, these satisfy top-level **CONF-06** (`각 로펌은 선택적 include_keywords / exclude_keywords 필터를 지원한다`). Tracked in `.planning/milestones/v1.1-REQUIREMENTS.md` traceability table.
```

(12-02-PLAN.md를 read_first 단계에서 읽어 정확한 SPEC-12-REQ-N IDs를 확정한 뒤 단락에 반영. 추정과 일치하면 그대로, 다르면 실제 IDs로 교체.)

**선택 작업 — 12-02 frontmatter 빈 값 채우기:** 12-02-PLAN.md를 읽어 실제로 닫힌 SPEC-12-REQ-* IDs를 확인. 12-02-SUMMARY.md frontmatter의 기존 `requirements-completed:` (현재 비어있거나 inline `[]`)에 block list 또는 inline 형태로 채움. **CONF-06은 절대 추가 금지** — top-level ID는 cross-walk 단락에서만 인용.

**중요:** 12-01 frontmatter `requirements-completed: [SPEC-12-REQ-1, SPEC-12-REQ-2, SPEC-12-REQ-3, SPEC-12-REQ-6]` 라인은 한 글자도 수정 금지.
</action>
<acceptance_criteria>
- `grep -lE "Cross-?walk.*CONF-06|CONF-06.*satisfied|satisfies.*CONF-06" .planning/phases/12-topic-based-filter/12-01-SUMMARY.md .planning/phases/12-topic-based-filter/12-02-SUMMARY.md` → 두 파일 모두 반환
- `grep -E "SPEC-12-REQ" .planning/phases/12-topic-based-filter/12-02-SUMMARY.md` → 매치 (12-02도 SPEC-12-REQ-N IDs 등장)
- `grep -E "^requirements-completed:" .planning/phases/12-topic-based-filter/12-01-SUMMARY.md` → 기존 값(또는 같은 줄 시작) 유지. `grep -E "SPEC-12-REQ-1.*SPEC-12-REQ-2.*SPEC-12-REQ-3.*SPEC-12-REQ-6" .planning/phases/12-topic-based-filter/12-01-SUMMARY.md` 또는 동등한 block list 매치 → 12-01 frontmatter 값 보존 확인
- 12-01 frontmatter의 기존 키 일절 변경 없음: `git diff .planning/phases/12-topic-based-filter/12-01-SUMMARY.md | awk '/^@@/{p=0} /^@@.*Cross-?walk|^@@.*## Cross/{p=1} p' | grep -E "^-" | grep -v "^---" | wc -l` → diff에서 본문 추가 외 삭제 라인 없음
- `git diff --name-only .planning/phases/12-topic-based-filter/` → 정확히 2개 파일(12-01-SUMMARY.md, 12-02-SUMMARY.md)
- `grep -E "^\| CONF-06 \|" .planning/phases/12-topic-based-filter/12-01-SUMMARY.md` → 0 매치 (frontmatter `requirements-completed:`에 CONF-06 추가하지 않음)
- `grep -E "^\| CONF-06 \|" .planning/phases/12-topic-based-filter/12-02-SUMMARY.md` → 0 매치
</acceptance_criteria>
</task>

<task id="16-02-T4">
<title>META-01b commit 생성</title>
<type>execute</type>
<read_first>
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-05 commit shape — Commit 2: META-01b — SUMMARYs, 7 파일 묶음)
- 본 plan T1·T2·T3 변경 내역
</read_first>
<action>
다음 7개 파일만 staging한 후 commit:

```bash
git add .planning/phases/10-data-quality-observability/10-01-SUMMARY.md \
        .planning/phases/10-data-quality-observability/10-02-SUMMARY.md \
        .planning/phases/10-data-quality-observability/10-03-SUMMARY.md \
        .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md \
        .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md \
        .planning/phases/12-topic-based-filter/12-01-SUMMARY.md \
        .planning/phases/12-topic-based-filter/12-02-SUMMARY.md

git commit -m "docs(meta-01b): backfill SUMMARY frontmatter and Phase 12 CONF-06 cross-walk

- Phase 10 SUMMARYs (10-01/02/03): add requirements-completed [DQOBS-01/02/03]
- Phase 11 SUMMARYs (11-01/02): add requirements-completed [RESUME-01]
  (11-03 owned by Phase 15 — not touched here)
- Phase 12 SUMMARYs (12-01/02): add Cross-walk section linking phase-local SPEC-12-REQ-* IDs to top-level CONF-06
- Phase 12 frontmatter requirements-completed preserved (SPEC-12-REQ-* IDs unchanged)

Phase: 16-v1-1-metadata-hygiene (META-01b)"
```

**Safety check 후 commit:**
- `git status --porcelain | grep -vE \"(10-data-quality-observability/10-0[123]-SUMMARY\\.md|11-cron-resumption-gate/11-0[12]-SUMMARY\\.md|12-topic-based-filter/12-0[12]-SUMMARY\\.md)\" | grep -v '^$'` 가 빈 출력이어야 함 — staging에 다른 파일이 묻어 들어가지 않음 보증.
- 본 plan은 push 수행 안 함.
</action>
<acceptance_criteria>
- `git log -1 --pretty=%s` → "docs(meta-01b): backfill SUMMARY frontmatter..." 로 시작
- `git diff HEAD~1 --name-only | sort` → 정확히 7줄:
  ```
  .planning/phases/10-data-quality-observability/10-01-SUMMARY.md
  .planning/phases/10-data-quality-observability/10-02-SUMMARY.md
  .planning/phases/10-data-quality-observability/10-03-SUMMARY.md
  .planning/phases/11-cron-resumption-gate/11-01-SUMMARY.md
  .planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md
  .planning/phases/12-topic-based-filter/12-01-SUMMARY.md
  .planning/phases/12-topic-based-filter/12-02-SUMMARY.md
  ```
- `git status` → working tree clean
- `git log -1 --pretty=%B | grep -E "META-01b"` → 매치
- `git diff HEAD~1 -- .planning/phases/11-cron-resumption-gate/11-03-SUMMARY.md .planning/phases/11-cron-resumption-gate/11-03-PLAN.md` → 빈 출력 (11-03 일절 touch 안 함)
</acceptance_criteria>
</task>

</tasks>

<verification>

**Goal-backward checks (META-01b):**

1. **Phase 10 frontmatter backfill** — `for f in .planning/phases/10-data-quality-observability/10-{01,02,03}-SUMMARY.md; do awk '/^---$/{c++; next} c==1 && /^requirements-completed:/{found=1} END{exit !found}' "$f" || echo "MISSING $f"; done` → 0 MISSING
2. **DQOBS 합집합** — `grep -hE "^\s*- DQOBS-0[123]" .planning/phases/10-data-quality-observability/*-SUMMARY.md | sort -u | wc -l` → `3`
3. **Phase 11 frontmatter backfill** — 동일 awk 검사 11-01/11-02 둘 다 found
4. **RESUME-01 합집합** — `grep -hE "^\s*- RESUME-01" .planning/phases/11-cron-resumption-gate/11-0[12]-SUMMARY.md | wc -l` → ≥ 1
5. **Phase 12 cross-walk** — `grep -lE "Cross-?walk.*CONF-06|satisfies.*CONF-06" .planning/phases/12-topic-based-filter/12-0[12]-SUMMARY.md | wc -l` → `2`
6. **Phase 12 frontmatter 보존** — `grep -E "SPEC-12-REQ-1" .planning/phases/12-topic-based-filter/12-01-SUMMARY.md` 매치 + `grep -E "^\| CONF-06" .planning/phases/12-topic-based-filter/12-0[12]-SUMMARY.md` 0 매치(CONF-06이 frontmatter 배열에 추가되지 않음)
7. **11-03 unchanged** — `git diff HEAD~1 -- .planning/phases/11-cron-resumption-gate/11-03*` → 빈 출력
8. **Single-scope commit** — `git diff HEAD~1 --name-only` 가 정확히 7개 파일 (모두 SUMMARY.md, 모두 .planning/phases/ 하위)

**가짜 양성 방지:**
- 10-XX-PLAN.md 본문이 D-02의 추정 매핑과 다르면 합집합 정확성을 우선 — DQOBS-01/02/03 3개 모두 최소 1회 등장만 충족하면 plan별 1:1 분담은 reasonable judgment 가능.
- 12-02-PLAN.md를 읽지 않고 추정 SPEC-12-REQ-{4,5}를 쓰지 않는다 — 실제 매핑을 plan 본문에서 확인 후 cross-walk 단락에 정확한 IDs 명시.

**보안/안전 노트 (threat model — docs-only):**
본 plan은 코드/secret 일절 touch 없음. 7개 markdown 파일 텍스트 추가만. 위협 표면 없음.
</verification>
</content>
</invoke>