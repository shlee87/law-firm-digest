---
phase: 16
plan: "03"
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-firm-audit-probe/06-AUDIT.md
  - CLAUDE.md
autonomous: true
requirements: [META-02]
---

# Plan 16-03: META-02 — 06-AUDIT.md Regen + CLAUDE.md `audit:firms` Freshness Policy

<objective>
`pnpm audit:firms`를 재실행해 `.planning/phases/06-firm-audit-probe/06-AUDIT.md`가 현재 `config/firms.yaml` 상태를 정확히 반영하도록 한다(특히 `freshfields` 행 제거 + header timestamp 갱신). 동시에 프로젝트 `CLAUDE.md`의 `## Conventions` 섹션 아래 `### Audit freshness (audit:firms)` subsection을 추가해 audit:firms 재실행 trigger + command + commit 책임 3요소를 단일 정책 단락에서 정의한다.
</objective>

<must_haves>
- `pnpm audit:firms` 명령이 0 exit로 성공 — 재실행 가능 상태 확인
- `.planning/phases/06-firm-audit-probe/06-AUDIT.md`에서 `freshfields` 행 부재 (`grep -nE "^\| freshfields " ...` 0 매치)
- `06-AUDIT.md` header `_Generated by_ \`pnpm audit:firms\` _at_ {TIMESTAMP}` 라인의 timestamp가 `2026-05-26` 이후 (실제 실행 시점)
- `CLAUDE.md`에 `### Audit freshness (audit:firms)` 또는 동등 subsection 추가 — `grep -niE "audit:firms" CLAUDE.md` 매치, 매치된 단락에 (trigger 조건 + 명령 + commit 책임) 3요소가 prose로 모두 존재
- subsection 위치: `## Conventions` 섹션 내, `### Workflow scheduling (cron edit policy)` subsection 다음(Phase 14가 추가한 위치 직속 인접)
- `config/firms.yaml`, `src/`, `scripts/`, `.github/workflows/` 일절 touch 금지 — `pnpm audit:firms`는 `config/firms.yaml` read-only로만 사용
- audit:firms 외 다른 side-effect 명령(예: `pnpm sync-schedule`, gemini API call, network send) 일절 실행 금지
</must_haves>

<tasks>

<task id="16-03-T1">
<title>config/firms.yaml 현재 상태 verify (freshfields 제거 반영 확인)</title>
<type>execute</type>
<read_first>
- config/firms.yaml (특히 line 180~182 부근 — "(freshfields removed by user 2026-05-18)" 코멘트 위치)
- .planning/phases/06-firm-audit-probe/06-AUDIT.md (현재 13행 — `| freshfields | rss | OK | 40 | n/a | n/a |` 존재 확인)
- package.json (line 13 부근 — `audit:firms` script 존재 확인: `tsx src/cli/auditFirms.ts`)
- src/cli/auditFirms.ts (실행 entry point — 본 plan은 코드 변경 없이 실행만)
</read_first>
<action>
**Verify 단계 (코드 변경 없음, 읽기만):**

다음 명령으로 사전 검증:
```bash
# (1) freshfields 코멘트 확인 — firms.yaml에서 firm은 제거되었으나 코멘트로 흔적 보존
grep -n "freshfields" config/firms.yaml || echo "no freshfields reference found"
# 기대: 코멘트 라인(예: "# (freshfields removed by user 2026-05-18)")만 매치, 실제 firm entry는 없음

# (2) firms.yaml에 활성 firm으로 freshfields 없음 확인 (yaml entry로서)
node -e "const y=require('yaml').parse(require('fs').readFileSync('config/firms.yaml','utf8')); const ids=(y.firms||[]).map(f=>f.id); console.log('freshfields in firms.yaml:', ids.includes('freshfields'))"
# 기대: "freshfields in firms.yaml: false"

# (3) 06-AUDIT.md 현재 freshfields 행 존재 확인 (regen 전)
grep -nE "^\| freshfields " .planning/phases/06-firm-audit-probe/06-AUDIT.md
# 기대: 1 line match (regen 후에는 사라짐)

# (4) audit:firms script 존재 확인
grep -E '"audit:firms"' package.json
# 기대: "audit:firms": "tsx src/cli/auditFirms.ts" 또는 동등
```

만약 (1)이 코멘트가 아닌 활성 entry로 freshfields를 보여주거나, (2)가 `true`를 출력하거나, (4)가 script 부재면 STOP — Phase 16 SPEC 가정 위반(SPEC R-06의 Current 상태와 mismatch). 사용자에게 escalate.

위 4개 verify 모두 통과 시 T2로 진행.
</action>
<acceptance_criteria>
- `grep -n "freshfields" config/firms.yaml` → 매치된 모든 라인이 코멘트(`#` 시작) — yaml entry 아님
- node check: `freshfields in firms.yaml: false` 출력
- `grep -nE "^\| freshfields " .planning/phases/06-firm-audit-probe/06-AUDIT.md` → 1 매치 (regen 전, line 13 부근)
- `grep -E '"audit:firms"' package.json` → 매치
</acceptance_criteria>
</task>

<task id="16-03-T2">
<title>`pnpm audit:firms` 실행 → 06-AUDIT.md 재생성</title>
<type>execute</type>
<read_first>
- .planning/phases/06-firm-audit-probe/06-AUDIT.md (실행 전 상태 backup용 — 헤더 + freshfields 행 위치)
- src/cli/auditFirms.ts (출력 경로 확인 — 06-AUDIT.md 직접 overwrite 여부)
- src/audit/firmAudit.ts (audit 로직)
</read_first>
<action>
**실행 단계:**

```bash
pnpm audit:firms
```

명령이 0 exit로 종료해야 함. 만약 non-zero exit 또는 stderr에 에러 메시지가 나오면 STOP — escalate.

**실행 후 verify:**
```bash
# (1) freshfields 행 사라짐 확인
grep -nE "^\| freshfields " .planning/phases/06-firm-audit-probe/06-AUDIT.md
# 기대: 0 매치 (exit 1 from grep)

# (2) header timestamp 갱신 확인 — 2026-05-26 이후
grep -nE "Generated by.*audit:firms.*at" .planning/phases/06-firm-audit-probe/06-AUDIT.md
# 기대: 매치된 줄의 timestamp가 `2026-05-26` 이후 (예: 2026-05-27 또는 이후 일자)

# (3) 다른 활성 firms는 여전히 존재 (cooley, clifford-chance, yulchon, logos, skadden, lee-ko, yoon-yang, barun, latham — 9개)
grep -cE "^\| (cooley|clifford-chance|yulchon|logos|skadden|lee-ko|yoon-yang|barun|latham) " .planning/phases/06-firm-audit-probe/06-AUDIT.md
# 기대: 9 또는 9 이상 (per-firm evidence 섹션에서도 매치 가능, Summary table만 카운트는 정확히 9 — 실제 출력 확인)

# (4) git status 확인 — 06-AUDIT.md만 변경
git status --porcelain .planning/phases/06-firm-audit-probe/06-AUDIT.md
# 기대: "M .planning/phases/06-firm-audit-probe/06-AUDIT.md" 한 줄
```

만약 audit:firms가 06-AUDIT.md 외 다른 파일(예: `.planning/phases/06-firm-audit-probe/06-CONTEXT.md` 또는 새 파일)을 만들거나 변경하면 confirm — SPEC scope는 06-AUDIT.md 단일 파일 변경. 다른 파일 변경이 발생하면 일시 stash 후 escalate.

**Safety check:**
```bash
# audit:firms 실행이 src/, scripts/, config/, .github/ 디렉토리를 건드리지 않음 보증
git status --porcelain | grep -E "^\s*M\s+(src/|scripts/|config/|\.github/)" && echo "FAIL: side-effect detected" || echo "OK: no code/config/workflow changes"
# 기대: "OK: no code/config/workflow changes"
```

**오직 audit:firms만 실행** — `pnpm sync-schedule`, gemini 호출, network send 등 다른 명령 일절 실행 금지.
</action>
<acceptance_criteria>
- `pnpm audit:firms` exit code `0` (실행 성공)
- `grep -nE "^\| freshfields " .planning/phases/06-firm-audit-probe/06-AUDIT.md` → 0 매치 (exit 1)
- `grep -nE "Generated by.*audit:firms.*at" .planning/phases/06-firm-audit-probe/06-AUDIT.md` → 매치 + timestamp의 일자 부분이 `2026-05-26` 또는 그 이후 (즉 본 실행 시점 반영)
- `git status --porcelain | grep -vE "^\s*M\s+\.planning/phases/06-firm-audit-probe/06-AUDIT\.md$" | grep -vE "^\?\?" | grep -v '^$'` → 빈 출력 (06-AUDIT.md 외 다른 modify 없음; untracked는 무시)
- `git status --porcelain | grep -E "^\s*M\s+(src/|scripts/|config/firms\.yaml|\.github/)"` → 빈 출력 (코드/firms.yaml/워크플로우 변경 없음 — audit:firms는 firms.yaml read-only)
- audit Summary table의 활성 firm 9개 모두 존재: `for f in cooley clifford-chance yulchon logos skadden lee-ko yoon-yang barun latham; do grep -qE "^\| $f " .planning/phases/06-firm-audit-probe/06-AUDIT.md || echo "MISSING:$f"; done` → 0 MISSING
</acceptance_criteria>
</task>

<task id="16-03-T3">
<title>CLAUDE.md에 `### Audit freshness (audit:firms)` subsection 추가</title>
<type>execute</type>
<read_first>
- CLAUDE.md (`## Conventions` 섹션 위치 — line 191, 그 아래 `### Workflow scheduling (cron edit policy)` subsection — line 193부터 line 211 `## Architecture` 직전까지)
- .planning/phases/14-scheduling-coverage/14-CONTEXT.md (D-02 — Workflow scheduling subsection 위치/형식 reference)
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-04 — Audit freshness 단락 권장 텍스트 전체)
- .planning/phases/16-v1-1-metadata-hygiene/16-SPEC.md (R-07 — trigger + command + commit 책임 3요소 acceptance)
</read_first>
<action>
`CLAUDE.md`의 `## Conventions` 섹션 내 `### Workflow scheduling (cron edit policy)` subsection 다음에 새 subsection 삽입.

**삽입 위치:** `### Workflow scheduling` subsection의 마지막 단락이 끝난 직후, 다음 `## Architecture` 섹션(line 211) 직전.

**삽입 내용 (정확한 텍스트):**

```markdown

### Audit freshness (audit:firms)

`pnpm audit:firms`를 재실행해야 하는 조건:
1. `config/firms.yaml`을 편집한 직후 (firm 추가/제거/disable 토글, tier 변경, selector 수정 등).
2. 마지막 audit 실행으로부터 6개월 이상 경과 시 (firms가 사이트 변경에 silent drift 했는지 확인 목적).

실행 절차:
- `pnpm audit:firms` → `.planning/phases/06-firm-audit-probe/06-AUDIT.md` 가 재생성된다(header timestamp + per-firm 행 갱신).
- 실행자가 06-AUDIT.md 변경분을 같은 PR/commit에서 함께 commit한다 (drift 상태가 자동 push되지 않도록 manual gate 유지).

CI 통합(자동 fail-on-drift)은 over-engineering으로 의도적 보류 — single-developer project에서 manual policy가 충분.
```

**삽입 전후 line 단위 정확성:**
- 기존 `### Workflow scheduling` subsection의 마지막 줄 다음에 빈 줄 1개 두고 `### Audit freshness (audit:firms)` 헤더 시작
- 새 subsection 마지막 줄 다음에 빈 줄 1개 두고 기존 `## Architecture` 섹션 시작

**중요:**
- `## Conventions` 헤더 자체와 기존 `### Workflow scheduling` 단락은 일절 수정 금지
- `## Architecture`, `## Project Skills`, `## GSD Workflow Enforcement`, `## Developer Profile` 등 그 아래 섹션도 일절 수정 금지
- 본 plan은 CLAUDE.md 외 다른 파일을 추가로 touch 하지 않음 (06-AUDIT.md는 T2에서 이미 변경 — 본 T3는 CLAUDE.md만)
</action>
<acceptance_criteria>
- `grep -niE "### Audit freshness.*audit:firms" CLAUDE.md` → 매치 (subsection 헤더 존재)
- `grep -niE "audit:firms" CLAUDE.md | wc -l` → ≥ 3 (subsection 헤더 + 본문 실행 절차 + CI 통합 단락에 모두 등장)
- subsection에 3요소 prose 검증 (수동 verify-work 단계에서 단락 전체 읽기로 검증, 단 grep으로 keyword 존재 확인):
  - Trigger keyword: `grep -E "firms\.yaml.*편집|편집.*firms\.yaml|편집한 직후" CLAUDE.md` 매치
  - Command keyword: `grep -E "pnpm audit:firms" CLAUDE.md` 매치 (subsection 외 다른 곳에도 가능)
  - Commit 책임 keyword: `grep -E "commit한다|commit 한다|함께 commit|same.*commit|PR/commit" CLAUDE.md` 매치
- subsection 위치 정확성: `awk '/^### Workflow scheduling/,/^## /' CLAUDE.md | grep -E "^### Audit freshness"` → 0 매치 (Workflow scheduling 단락 내부에 들어가지 않음); `awk '/^### Audit freshness/,/^## /' CLAUDE.md | head -1 | grep -E "^### Audit freshness"` → 매치 (별도 subsection); `awk '/^## Conventions/,/^## Architecture/' CLAUDE.md | grep -E "^### Audit freshness"` → 매치 (Conventions 섹션 내부)
- `git diff CLAUDE.md | grep -E "^-[^-]" | wc -l` → `0` (기존 라인 삭제/수정 없음, 새 라인 추가만)
- `git diff --name-only` 가 정확히 2개 파일: `.planning/phases/06-firm-audit-probe/06-AUDIT.md` + `CLAUDE.md` (T2 + T3)
</acceptance_criteria>
</task>

<task id="16-03-T4">
<title>META-02 commit 생성</title>
<type>execute</type>
<read_first>
- .planning/phases/16-v1-1-metadata-hygiene/16-CONTEXT.md (D-05 commit shape — Commit 3: META-02 — audit + policy 단일 commit)
- 본 plan T2·T3 변경 내역
</read_first>
<action>
다음 2개 파일을 staging한 후 commit:

```bash
git add .planning/phases/06-firm-audit-probe/06-AUDIT.md CLAUDE.md

git commit -m "docs(meta-02): regen 06-AUDIT.md and add audit:firms freshness policy

- Re-run pnpm audit:firms: 06-AUDIT.md timestamp refreshed, freshfields row removed (firm was deleted from firms.yaml on 2026-05-18, audit had stale entry until now)
- CLAUDE.md: add ### Audit freshness (audit:firms) subsection under ## Conventions, after ### Workflow scheduling
  - Triggers: after firms.yaml edits OR after 6 months since last audit
  - Command: pnpm audit:firms
  - Commit responsibility: runner commits 06-AUDIT.md changes in same PR
  - CI integration intentionally deferred (single-developer policy sufficient)

Phase: 16-v1-1-metadata-hygiene (META-02)"
```

**Safety check 후 commit:**
- `git status --porcelain | grep -vE "(\.planning/phases/06-firm-audit-probe/06-AUDIT\\.md|^M  CLAUDE\\.md| M CLAUDE\\.md)" | grep -vE "^\?\?" | grep -v '^$'` 가 빈 출력이어야 함 — staging에 다른 파일이 묻어 들어가지 않음 보증.
- 본 plan은 push 수행 안 함.
</action>
<acceptance_criteria>
- `git log -1 --pretty=%s` → "docs(meta-02): regen 06-AUDIT.md..." 로 시작
- `git diff HEAD~1 --name-only | sort` → 정확히 2줄:
  ```
  .planning/phases/06-firm-audit-probe/06-AUDIT.md
  CLAUDE.md
  ```
- `git status` → working tree clean (이 plan의 모든 작업이 commit됨)
- `git log -1 --pretty=%B | grep -E "META-02"` → 매치
- `git diff HEAD~1 -- src/ scripts/ .github/ config/firms.yaml` → 빈 출력 (다른 디렉토리 일절 touch 없음)
</acceptance_criteria>
</task>

</tasks>

<verification>

**Goal-backward checks (META-02):**

1. **06-AUDIT 재생성** — `grep -nE "^\| freshfields " .planning/phases/06-firm-audit-probe/06-AUDIT.md` 0 매치
2. **Timestamp 갱신** — `grep -nE "Generated by.*audit:firms.*at" .planning/phases/06-firm-audit-probe/06-AUDIT.md` 의 timestamp가 `2026-05-26` 이후
3. **audit:firms 재실행 가능 상태** — Plan 실행 시 `pnpm audit:firms` exit 0
4. **CLAUDE.md freshness policy 추가** — `grep -niE "audit:firms" CLAUDE.md` 매치 + 단락 내 trigger + command + commit 책임 3요소 prose 검증 (verify-work에서 사람이 단락 통독)
5. **In-scope file 경계** — `git diff HEAD~1 --name-only` 가 06-AUDIT.md + CLAUDE.md 정확히 2개
6. **Code/config/workflow 변경 없음** — `git diff HEAD~1 -- src/ scripts/ .github/ config/firms.yaml package.json pnpm-lock.yaml` 빈 출력

**가짜 양성 방지:**
- `pnpm audit:firms`가 다른 파일(예: `.planning/phases/06-firm-audit-probe/06-CONTEXT.md` 또는 새 evidence 파일)을 변경하면 escalate — SPEC scope는 06-AUDIT.md 단일 파일.
- audit:firms가 0 exit지만 freshfields 행이 여전히 남아 있으면 (config/firms.yaml에 실제로 entry가 남아 있다는 뜻) STOP — Phase 16 가정 위반.
- CLAUDE.md subsection이 `### Workflow scheduling` 내부에 nested되지 않도록 빈 줄로 명확히 구분 (markdown heading 인식 보장).

**보안/안전 노트 (threat model):**
- `pnpm audit:firms`는 firms.yaml read + 각 firm 사이트 HTTP HEAD/GET probe 가능성 있음 → 본 plan 실행 시 네트워크 호출 발생. 위협 표면: 사이트 차단 / TLS 검증 실패. **mitigation:** `pnpm audit:firms` 자체가 Phase 6에서 이미 신뢰된 도구로 검증됨 (`auditFirms.ts` 변경 없이 재실행만). 만약 실행 중 외부 사이트 측 변경으로 timeout/SSL 에러가 발생하면 audit는 해당 firm 행에 `error` status로 기록 — 본 plan은 audit 자체의 결과를 그대로 받아 commit (audit 신뢰성은 Phase 6 책임).
- CLAUDE.md 변경은 secret/credential 포함 없음 — 단순 정책 prose 추가.

</verification>
</content>
</invoke>