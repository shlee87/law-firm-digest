---
phase: 07-spa-aware-detail-tier
plan: 05
type: execute
wave: 5
depends_on: [07-04]
files_modified:
  - config/firms.yaml
autonomous: false
requirements: [DETAIL-04]
tags: [phase-7, kim-chang, root-cause, investigation, audit]

must_haves:
  truths:
    - "kim-chang's detail fetch status is definitively diagnosed — either 'OK' (detail body non-empty) or explicitly disabled with WAF/404 evidence (D-10 fallback)"
    - "If kim-chang is disabled, the YAML inline comment captures the exact diagnosis (WAF signature, 404 pattern, or URL template error) for audit trail"
    - "kim-chang row in the regenerated 06-AUDIT.md matches its YAML state — `OK` if enabled, not-present-in-enabled-scope if disabled"
    - "DETAIL-04 literal requirement is satisfied OR a written disable-sufficient-reason is recorded per D-10"
  artifacts:
    - path: "config/firms.yaml"
      provides: "kim-chang block either (a) activated with detail_tier='js-render' and working detail fetch, or (b) disabled with comment explaining why"
      contains: "id: kim-chang"
  key_links:
    - from: "config/firms.yaml kim-chang block"
      to: "pnpm audit:firms kim-chang row"
      via: "D-11 verification contract"
      pattern: "kim-chang"
    - from: "SUMMARY.md root-cause diagnosis"
      to: "config/firms.yaml kim-chang inline comment"
      via: "written evidence trail for Phase 11 cron gate"
      pattern: "comment"
---

<objective>
Determine why kim-chang's detail fetch currently fails (06-AUDIT.md reports "only 0/2 detail fetches succeeded") and either fix it or record a definitive disable reason. kim-chang got `detail_tier: 'js-render'` in plan 03 per DETAIL-04 literal, but if Playwright still fails to fetch the detail page, this plan does the "actually open the browser and look" investigation that the earlier planning phases deferred.

Purpose: Phase 7 cannot close with kim-chang in an indeterminate state. Phase 10/11 handles observability and cron-gate, not root-cause fixes. Per D-10, kim-chang resolution is Phase 7 scope.
Output: kim-chang is either a passing `OK` row in audit, or definitively disabled with a written reason. Either state is acceptable — indeterminacy is not.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md
@.planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md
@.planning/phases/06-firm-audit-probe/06-AUDIT.md
@.planning/phases/07-spa-aware-detail-tier/07-03-SUMMARY.md
@.planning/phases/07-spa-aware-detail-tier/07-04-SUMMARY.md

<interfaces>
<!-- Current kim-chang block after plan 03 (detail_tier added): -->
```yaml
  - id: kim-chang
    name: 김앤장 법률사무소
    language: ko
    type: html
    url: https://www.kimchang.com/ko/newsletter.kc
    timezone: Asia/Seoul
    enabled: true
    detail_tier: 'js-render'  # Phase 7 DETAIL-04 activation (detail fetch previously empty)
    selectors:
      list_item: "ul.notice_list > li"
      title: ".notice_txt .title, .notice_txt._dotdotdot_news"
      link_onclick_regex: "goDetail\\('(\\d+)','(\\d+)'\\)"
      link_template: "https://www.kimchang.com/ko/insights/detail.kc?sch_section={1}&idx={2}"
      date: ".notice_date"
```

<!-- Current audit diagnosis from 06-AUDIT.md: -->
**kim-chang (html, detail-quality-unknown)**
- Items: 5 (list fetch works)
- Evidence: only 0/2 detail fetches succeeded
- Remediation: monitor
- Target Phase: Phase 10/11

<!-- Hypothesis tree from 07-CONTEXT.md §D-10: -->
1. URL template `detail.kc?sch_section={1}&idx={2}` is wrong — actual URL pattern differs
2. WAF challenge — site blocks headless browser or non-Korean IP
3. 404 — the detail path requires a session / authentication
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Investigate kim-chang detail fetch failure (root-cause analysis)</name>
  <files>config/firms.yaml (kim-chang block — eventual target of the chosen remediation in Task 2)</files>
  <action>
    Perform the investigation_procedure below (probe detail URLs in browser, run Playwright probe, cross-check onclick regex). Classify evidence into one of three options (A fix URL template, B fix selector, C disable with reason). Pause for the human to confirm the chosen option before Task 2 applies the remediation.
  </action>
  <verify>
    <automated>pnpm check:firm kim-chang 2>&1 | tail -40</automated>
  </verify>
  <done>
    Human has responded with one of: "option-A: <evidence>", "option-B: <evidence>", or "option-C: <evidence>". The evidence summary is captured in Task 2's read_first context.
  </done>
  <decision>
    Run manual investigation of kim-chang detail-page fetch. Collect evidence across three hypotheses (URL template / WAF / 404-session). Choose remediation: (A) fix URL template in YAML, (B) fix selector and/or `link_onclick_regex` in YAML, (C) disable firm with comment explaining root cause.
  </decision>
  <context>
    06-AUDIT.md says "only 0/2 detail fetches succeeded" for kim-chang — meaning both sampled detail URLs failed to return usable body text. After plan 03, detail fetches now route through Playwright; if they still fail, the failure is not about JS rendering but about URL or access.

    Per D-10, this investigation must be DONE, not deferred. The monitor-route-to-Phase-10/11 that 06-AUDIT.md suggests is superseded by Phase 7's explicit activation scope.
  </context>
  <read_first>
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions D-10 (literal fallback path)
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (current kim-chang diagnosis)
    - .planning/backlog/v1.0-data-quality-audit.md (original kim-chang hallucination report)
    - src/audit/firmAudit.ts (lines 160-210 — how the audit performs detail fetches; understand what "0/2 succeeded" actually means)
    - config/firms.yaml kim-chang block (current state)
    - scripts/probe-js-render.ts (reusable single-firm Playwright debugging tool)
  </read_first>
  <options>
    <option id="A-fix-url-template">
      <name>(A) Fix URL template — manual probe revealed correct URL pattern</name>
      <pros>kim-chang becomes OK; DETAIL-04 literal fully satisfied; one fewer monitored firm.</pros>
      <cons>Requires live browsing + DevTools Network tab to capture actual request URL.</cons>
    </option>
    <option id="B-fix-selector">
      <name>(B) Fix selector (list_item / link_onclick_regex) — manual probe revealed selector drift</name>
      <pros>kim-chang becomes OK; root cause was never URL template, was always selector.</pros>
      <cons>Requires verifying the `goDetail('N','M')` regex still matches the live HTML — if Kim Chang has moved to different onclick shape, the regex needs updating.</cons>
    </option>
    <option id="C-disable-with-reason">
      <name>(C) Disable kim-chang with `enabled: false` + WAF/404 evidence comment</name>
      <pros>Honest reporting: firm is blocked and we say so. Phase 11 cron gate can proceed without a hallucinating firm.</pros>
      <cons>KR roster loses one firm; DETAIL-04 literal (kim-chang activation) is NOT fully satisfied — SUMMARY.md must explain why.</cons>
    </option>
  </options>
  <investigation_procedure>
    Perform ALL of these steps before choosing an option — the choice must be evidence-based:

    1. **Get 2 actual detail URLs from live list fetch:**
       ```
       pnpm check:firm kim-chang 2>&1 | head -80
       ```
       Capture the first 2 item URLs printed under "parsed items" / "would-summarize" sections.

    2. **Manually browse one detail URL** in a regular browser. Note:
       - Does the URL load? (200 / 404 / 403 / 5xx)
       - If it loads, is it the same article that was listed, or a generic landing page?
       - Open DevTools → Network tab → reload. What URL actually serves the article content? Does it match our `link_template` literal?

    3. **Probe the same URL with Playwright:**
       ```
       pnpm tsx scripts/probe-js-render.ts kim-chang 2>&1 | tail -80
       ```
       If `probe-js-render.ts` does not support detail-URL probing, write an ad-hoc inline script (do NOT commit):
       ```typescript
       // scripts/_tmp-kim-chang-detail.ts (NOT committed — delete after use)
       import { chromium } from 'playwright';
       const browser = await chromium.launch({ headless: true });
       const ctx = await browser.newContext({ userAgent: 'LegalNewsletterBot/1.0 (+github.com/...)' });
       const page = await ctx.newPage();
       await page.goto('<actual-detail-url-from-step-1>', { waitUntil: 'domcontentloaded', timeout: 15000 });
       console.log('status:', page.url());
       console.log('title:', await page.title());
       console.log('body-preview:', (await page.content()).slice(0, 2000));
       await browser.close();
       ```
       Then: `pnpm tsx scripts/_tmp-kim-chang-detail.ts` — observe the actual page response. **DELETE the tmp file before committing.**

    4. **Cross-check the regex extraction.** Inspect the list-page HTML for kim-chang:
       ```
       curl -sL "https://www.kimchang.com/ko/newsletter.kc" -A "LegalNewsletterBot/1.0" | grep -iE "goDetail|notice_list|onclick" | head -20
       ```
       Verify the `onclick="goDetail('N','M')"` shape is still present. If onclick has changed to, e.g., `viewDetail('N')`, the regex must be updated.

    5. **Classify the finding** based on the evidence collected:
       - Evidence shows correct URL pattern differs from YAML `link_template` → choose **A**.
       - Evidence shows onclick regex no longer matches / list selectors drift → choose **B**.
       - Evidence shows all detail URLs return 403/CF challenge/401 regardless of user-agent/Playwright → choose **C**.

    6. **Implement the chosen remediation** in the YAML (plan 03's file), with an inline comment on the changed line(s) summarizing the evidence.

    7. **Re-run** `pnpm audit:firms` — kim-chang row should either show `OK` (options A/B) or not appear in enabled scope (option C).
  </investigation_procedure>
  <remediation_yaml_shapes>
    **Option A (URL template fix) — example diff:**
    ```yaml
        link_template: "<corrected-template>"  # Phase 7 fix — DevTools showed actual URL is /ko/insights/<observed-pattern>, not detail.kc
    ```

    **Option B (selector fix) — example diff:**
    ```yaml
        list_item: "<corrected-selector>"           # Phase 7 fix — notice_list renamed to <observed-class> on 2026-MM-DD
        # or
        link_onclick_regex: "<corrected-regex>"     # Phase 7 fix — onclick shape changed from goDetail to <observed-function>
    ```

    **Option C (disable) — example diff:**
    ```yaml
      - id: kim-chang
        name: 김앤장 법률사무소
        language: ko
        type: html
        url: https://www.kimchang.com/ko/newsletter.kc
        timezone: Asia/Seoul
        enabled: false  # disabled YYYY-MM-DD — Phase 7 investigation: <observed-blocker>, see 07-05-SUMMARY.md. Restore via <proposed-path>.
        detail_tier: 'js-render'
        # ... selectors block preserved ...
    ```
  </remediation_yaml_shapes>
  <resume-signal>
    Type one of:
    - `option-A: <1-line evidence summary>` (e.g., `option-A: DevTools captured /ko/insights/newsletter/<id>, template updated`)
    - `option-B: <1-line evidence summary>` (e.g., `option-B: list_item ul.notice_list renamed to ul.newsletter_list`)
    - `option-C: <1-line evidence summary>` (e.g., `option-C: Playwright returns CF challenge HTML; UA rotation tested, confirmed blocked`)
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Apply chosen remediation + regenerate audit + record evidence</name>
  <read_first>
    - config/firms.yaml kim-chang block (current state)
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions D-10 (required comment shape for option C)
    - Task 1's resume-signal evidence summary
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (to compare before/after)
  </read_first>
  <action>
    Based on Task 1's resume-signal, perform ONE of:

    **If Task 1 returned `option-A` (URL template fix):**
    - Edit kim-chang block in `config/firms.yaml`, update the `link_template` value with the observed correct pattern.
    - Add inline comment referencing 07-05-SUMMARY.md.

    **If Task 1 returned `option-B` (selector fix):**
    - Edit kim-chang block, update the relevant selector(s) (`list_item` / `title` / `link_onclick_regex` / `date`).
    - Add inline comment referencing 07-05-SUMMARY.md.

    **If Task 1 returned `option-C` (disable with reason):**
    - Set `enabled: false` on kim-chang block.
    - Update the inline comment on the `enabled: false` line to include: the date (YYYY-MM-DD), the Phase 7 investigation marker, the blocker signature observed (WAF / 404 / session-required / other), and a reference to `.planning/phases/07-spa-aware-detail-tier/07-05-SUMMARY.md` for the full evidence trail.
    - Leave `detail_tier: 'js-render'` and `selectors:` in place — preserve the block shape so re-enablement is a one-line `enabled: true` flip in a future phase.

    In ALL cases:
    - **Delete any temporary files** created during Task 1 investigation (`scripts/_tmp-*.ts`, ad-hoc probe scripts).
    - Run `pnpm audit:firms` — regenerates `.planning/phases/06-firm-audit-probe/06-AUDIT.md`.
    - Verify the kim-chang row behavior:
      - Options A/B: row shows `OK`.
      - Option C: row absent (kim-chang is now disabled and audit default-scope excludes disabled firms per Phase 6 D-04). Verify by running `pnpm audit:firms --include-disabled` — kim-chang should appear with a `(disabled, baseline)` tag or equivalent marker.

    Record in SUMMARY.md:
    - All evidence collected during Task 1 (URL responses, HTML snippets, Playwright traces)
    - The YAML diff applied
    - Before/after audit row shape
    - Any follow-up action for future phases (e.g., "re-probe in 3 months to check if WAF lifted")
  </action>
  <verify>
    <automated>pnpm audit:firms 2>&1 | grep -E "^\| kim-chang"</automated>
  </verify>
  <done>
    kim-chang is in a definitive state — either `OK` row in audit (options A/B) or disabled with comment (option C). SUMMARY.md captures the evidence trail.
  </done>
  <acceptance_criteria>
    - For options A/B: `pnpm audit:firms 2>&1 | grep -E "^\| kim-chang .* \| OK \|"` returns a match.
    - For option C: `grep -E "^    enabled: false.*kim-chang|id: kim-chang" config/firms.yaml` shows the block with `enabled: false` AND `grep "07-05-SUMMARY.md" config/firms.yaml` returns a match (the inline comment references the summary).
    - `grep -c "id: kim-chang" config/firms.yaml` returns 1 (no accidental duplication).
    - `ls scripts/_tmp-*.ts 2>/dev/null | wc -l | tr -d ' '` returns 0 (no tmp files committed).
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm vitest run` exits 0 (no regression).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kim & Chang website → Playwright browser | Phase 7 investigation navigates kim-chang's detail pages; possibility of WAF, rate-limit, or session-required content. |
| Ad-hoc investigation scripts → repository | Temporary probe scripts must NOT be committed (credential leak, reputation). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12 | Information Disclosure | Committing a temporary probe script that contains hardcoded URLs, cookies, or headers | mitigate | Task 2 acceptance criterion checks `ls scripts/_tmp-*.ts` returns 0 files. Task 1 instructions explicitly say "DELETE the tmp file before committing." |
| T-07-13 | Denial of Service | Repeatedly probing kim-chang during investigation triggering their rate-limiter or IP block | accept | Manual investigation is 1-time; `pnpm check:firm kim-chang` and `pnpm audit:firms` each make ~5-7 requests total. One-off per plan. Politeness delay (500ms in enrichBody) applies. |
| T-07-14 | Tampering | A WAF-substituted page passing our length-threshold check but containing adversarial content | accept | Gemini prompt (Phase 8 pending) will be defense-in-depth. For now, manual Task 1 inspection catches this — if the page content looks wrong, the operator chooses option C (disable). |
</threat_model>

<verification>
- kim-chang row in regenerated 06-AUDIT.md matches the chosen option:
  - Options A/B → kim-chang shows `OK`.
  - Option C → kim-chang absent from default-scope audit, present with disabled tag under `--include-disabled`.
- SUMMARY.md captures the full evidence trail (Task 1 output + YAML diff + final audit row).
- `pnpm vitest run` exits 0.
- No uncommitted temporary files remain.
</verification>

<success_criteria>
- DETAIL-04 literal (kim-chang activation) is satisfied (options A/B) OR explicitly deferred with evidence (option C).
- SUMMARY.md is detailed enough that a future operator can re-attempt activation in 3 months without re-running the investigation from scratch.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spa-aware-detail-tier/07-05-SUMMARY.md` per template. Include the full investigation trace: 2 detail URLs tried, response status codes, HTML snippets showing the blocker (WAF challenge page / 404 / empty body), the YAML diff applied, and the before/after audit row.
</output>
