---
phase: 07-spa-aware-detail-tier
plan: 04
type: execute
wave: 4
depends_on: [07-03]
files_modified:
  - config/firms.yaml
autonomous: false
requirements: [DETAIL-04]
tags: [phase-7, yaml, selector, remediation, audit]

must_haves:
  truths:
    - "logos (was selector-empty) produces items and distinct detail bodies — audit row OK"
    - "skadden (was selector-empty) produces items and distinct detail bodies — audit row OK"
    - "lee-ko (was detail-empty, body 36/32 chars) produces non-empty detail bodies over threshold 100 chars — audit row OK"
    - "barun (was detail-empty, body 0/0 chars) produces non-empty detail bodies over threshold 100 chars — audit row OK"
    - "No regression introduced to other firms — their audit status is unchanged or improved"
  artifacts:
    - path: "config/firms.yaml"
      provides: "Updated selector blocks for logos, skadden, lee-ko, barun + optional detail_tier promotion if live probe shows static detail fetch fails"
      contains: "id: logos"
  key_links:
    - from: "config/firms.yaml selector changes"
      to: "pnpm check:firm <id> live probe output"
      via: "iterative selector tuning loop"
      pattern: "list_item, body"
    - from: "config/firms.yaml"
      to: ".planning/phases/06-firm-audit-probe/06-AUDIT.md"
      via: "pnpm audit:firms — single source of truth (D-11)"
      pattern: "Status column"
---

<objective>
Fix broken selectors for 4 firms flagged by 06-AUDIT.md with `fix-selector` remediation: logos + skadden (list-selector empty, 0 items) and lee-ko + barun (detail-empty, body too short). Use the `pnpm check:firm <id>` probe-fix-probe iteration loop to tune selectors live. If post-selector-fix a firm's detail page still returns empty, promote that firm to `detail_tier: 'js-render'` per Claude's Discretion in 07-CONTEXT.md.

Purpose: Four firms were silently contributing zero or hallucinated content to the daily digest. Without this fix, Phase 11 cron gate cannot pass. The Phase 7 scope explicitly includes these firms per D-01.
Output: config/firms.yaml with 4 selector blocks repaired. All 4 firms show `OK` in `pnpm audit:firms`.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md
@.planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md
@.planning/phases/06-firm-audit-probe/06-AUDIT.md
@.planning/phases/07-spa-aware-detail-tier/07-03-SUMMARY.md

<interfaces>
<!-- Current broken firm blocks in config/firms.yaml -->

logos (line ~102) — list-selector returns 0 items:
```yaml
  - id: logos
    url: https://www.lawlogos.com/sub/news/newsletter.asp
    type: html
    selectors:
      list_item: ".board-box .list tr"   # <-- current, returns 0
      title: "a"
      link: "a"
      date: ".date"
```

skadden (line ~124) — list-selector returns 0 items:
```yaml
  - id: skadden
    url: https://www.skadden.com/insights
    type: html
    selectors:
      list_item: ".views-row"            # <-- current, returns 0
      title: ".insightful-card__title, .highlight-card-title"
      link: "a.insightful-card, a.highlight-card"
      date: ".insightful-card__date"
```

lee-ko (line ~167) — detail body 36/32 chars (threshold 100), detail_tier is now 'js-render' after plan 03:
```yaml
  - id: lee-ko
    type: js-render
    detail_tier: 'js-render'
    selectors:
      list_item: ".leeko-new-newsletter__item"
      # NO body selector — util.ts default chain being used, returning only 36/32 chars
```

barun (line ~199) — detail body 0/0 chars, detail_tier is now 'js-render' after plan 03:
```yaml
  - id: barun
    type: js-render
    detail_tier: 'js-render'
    url: https://barunlaw.com/barunnews/N
    selectors:
      list_item: ".articlebox"
      # NO body selector — util.ts default chain returning 0
```

<!-- Reusable patterns -->
- Existing body-selector override precedent: shin-kim at line 87 `body: ".post-content"` — this is the canonical pattern.
- Existing `pnpm check:firm <id>` CLI: prints raw→parsed→would-summarize→would-render stages without sending email or writing state.
- Existing `pnpm audit:firms`: runs full 12-firm audit, writes 06-AUDIT.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix logos + skadden list_item selectors via live probe</name>
  <read_first>
    - config/firms.yaml (current logos and skadden blocks — see interfaces above)
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (current evidence: "list page OK; 0 items extracted" for both)
    - .planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md §`config/firms.yaml` Pattern C (selector workflow)
    - src/cli/checkFirm.ts (to know what output `pnpm check:firm <id>` will print)
  </read_first>
  <action>
    **Probe-fix-probe loop for logos and skadden** (do both sequentially — each repeats the same loop):

    **Step 1: Fetch live HTML.** Run:
    ```
    pnpm check:firm logos 2>&1 | head -200
    ```
    Look at the "raw fetch" section — this prints the first ~200 chars of the list page HTML. If the page is not reachable (timeout/403), STOP and record the issue in the SUMMARY.md — this becomes a `monitor` case analogous to shin-kim and is NOT a Phase 7 selector problem.

    **Step 2: Inspect actual list structure.** Either:
    (a) `curl -sL "https://www.lawlogos.com/sub/news/newsletter.asp" -A "LegalNewsletterBot/1.0" | grep -iE "class=|<tr|<li|<div" | head -40` — find the wrapping element of newsletter rows, OR
    (b) Open the page in a browser (or `pnpm tsx scripts/probe-js-render.ts logos` if the firm needs JS) and use DevTools to locate the list container.

    **Step 3: Update `list_item` (and `title`/`link`/`date` if necessary).** Edit `config/firms.yaml` logos block. Example shape (replace `<observed-selector>` with the actual one):
    ```yaml
        selectors:
          list_item: "<observed-selector>"
          title: "<observed-title-selector>"
          link: "<observed-link-selector>"
          date: "<observed-date-selector>"
    ```

    **Step 4: Re-probe.** Run `pnpm check:firm logos` again. Expect parsed-items count > 0. If still 0, repeat steps 2-3 with a different selector hypothesis (up to 3 iterations). If 3 iterations still fail, set `detail_tier: 'js-render'` on logos and try again — the list may itself be JS-rendered, in which case `type: html` is wrong (but type change is out of scope; if that is the diagnosis, record it and leave the block as-is for a Phase 10/11 escalation).

    **Step 5: Check detail fetch.** Once list_item returns items, check `pnpm check:firm logos` "would-render" section — each item should show distinct body text. If detail bodies look SPA/identical, promote to `detail_tier: 'js-render'` (add the line per plan 03's pattern, with comment `# Phase 7 — list fixed + detail promoted to Playwright`).

    **Repeat all 5 steps for skadden** with these live URLs:
    - skadden list URL: `https://www.skadden.com/insights`
    - Known historical note (from existing inline comment at line 116-123): the `.views-row` wrapper is correct shape; if drifted, observed wrapper replaces it. Skadden renders via Drupal views, so selectors may be brittle across their redesigns.

    **Hard constraint:** Do NOT modify any firm block other than logos and skadden in this task. `list_item`, `title`, `link`, `date` may all be updated for these 2 firms as needed.

    **Record every selector change attempted** in the plan SUMMARY.md with the observed HTML snippet that justified it.
  </action>
  <verify>
    <automated>pnpm check:firm logos 2>&1 | grep -E "(items extracted|parsed|fetched)" && pnpm check:firm skadden 2>&1 | grep -E "(items extracted|parsed|fetched)"</automated>
  </verify>
  <done>
    logos and skadden both produce items > 0 in `pnpm check:firm <id>` output. If one firm could not be fixed within 3 selector iterations, that fact is documented in the SUMMARY.md with the failed hypotheses.
  </done>
  <acceptance_criteria>
    - `pnpm check:firm logos` prints at least 1 item in the parsed-items stage OR the SUMMARY.md documents why it could not be fixed with proposed next-phase escalation.
    - `pnpm check:firm skadden` prints at least 1 item in the parsed-items stage OR the SUMMARY.md documents why it could not be fixed.
    - `grep -c "id: logos" config/firms.yaml` returns 1 (block is still a single entry, no accidental duplication).
    - `grep -c "id: skadden" config/firms.yaml` returns 1.
    - `pnpm tsc --noEmit` exits 0 (if loader types change).
    - `pnpm vitest run` exits 0 (no regression in loader tests).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fix lee-ko + barun detail body selectors (or promote body selector)</name>
  <read_first>
    - config/firms.yaml (current lee-ko and barun blocks with detail_tier already 'js-render' from plan 03)
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (lee-ko: body 36/32 chars; barun: body 0/0 chars)
    - .planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md §`config/firms.yaml` Pattern C (selector workflow)
    - src/scrapers/util.ts (extractBody default chain — understand which selectors it tries in priority order, so an override makes sense)
  </read_first>
  <action>
    **Probe-fix-probe loop for lee-ko and barun** (both are already `detail_tier: 'js-render'` after plan 03, so detail pages fetch via Playwright — the issue is `extractBody`'s default selector chain not finding the actual body element on the hydrated HTML).

    **Step 1: Fetch a live detail page.** Run:
    ```
    pnpm check:firm lee-ko 2>&1 | head -200
    ```
    Inspect the "would-render" section — body lengths per item. Confirm still 36/32 or under 100.

    **Step 2: Probe a single detail URL manually.** Pick the first item's URL from step 1. Open it in a browser with DevTools, or run:
    ```
    pnpm tsx scripts/probe-js-render.ts <detail-url> 2>&1 | head -100
    ```
    (Note: probe-js-render.ts is currently a list-page probe; if it does not support detail URLs, write a minimal ad-hoc playwright script inline — do NOT commit it. `pnpm check:firm` already runs Playwright for js-render firms; use its output.)

    Identify the CSS selector that wraps the actual article body (common shapes: `.article-content`, `.post-content`, `.view-content`, `#content`, `.leeko-newsletter-view`, `.barun-article-body`, etc.). For Korean legal firms, the body element is typically >500 chars of prose inside a single `<div>`.

    **Step 3: Add `body:` selector override to the firm block.** Mirror the shin-kim pattern at line 87. Example for lee-ko:
    ```yaml
      - id: lee-ko
        # ... existing fields ...
        detail_tier: 'js-render'
        selectors:
          list_item: ".leeko-new-newsletter__item"
          title: ".leeko-new-newsletter__item-title"
          link_onclick_regex: "goDetail\\('([0-9]+)'\\)"
          link_template: "/leenko/news/newsLetterView.do?lang=KR&newsletterNo={1}"
          date: ".leeko-new-newsletter__item-date"
          body: "<observed-body-selector>"   # NEW — Phase 7 selector fix
    ```

    **Step 4: Re-probe.** `pnpm check:firm lee-ko` — expect body length ≥ 200 chars per item. Repeat for barun.

    **Step 5 (barun-specific): URL verification.** The current barun `url` is `https://barunlaw.com/barunnews/N` — the literal `N` looks like a placeholder. Verify it fetches a real list page (not 404). If the URL is malformed, FIX it before tuning selectors. Per 04-07 PROBE-RESULTS.md note: "barun canonical newsletter URL is https://barunlaw.com/barunnews/N (discovered live); Phase 2 audit candidate www.baruninews.com has dead DNS. Server-rendered HTML; js-render tier works as a superset." If `N` is indeed the literal path, keep it; if it is supposed to be a dynamic ID, correct it.

    **Hard constraints:**
    - Do NOT change `type`, `detail_tier`, `url` (unless barun's URL is malformed per Step 5), `wait_for`, `list_item`, `title`, `link*`, `date` fields for lee-ko/barun — only ADD a `body:` selector override.
    - If adding a `body:` override still yields <100 chars, document the observed hydrated HTML structure in SUMMARY.md and leave the firm as-is for Phase 10/11 monitor escalation (do not block the phase on one recalcitrant firm).
  </action>
  <verify>
    <automated>pnpm check:firm lee-ko 2>&1 | grep -iE "(body|bodies|length)" && pnpm check:firm barun 2>&1 | grep -iE "(body|bodies|length)"</automated>
  </verify>
  <done>
    lee-ko and barun produce detail bodies ≥ 100 chars per item (or documented exception per step 5 of action).
  </done>
  <acceptance_criteria>
    - `grep -c "body:" config/firms.yaml` is ≥ (pre-plan count + 2) — i.e., at least 2 new body selector overrides landed. (Pre-plan: shin-kim is the only `body:` override; post-plan: shin-kim + lee-ko + barun = 3.)
    - `pnpm check:firm lee-ko` shows at least one item with body length ≥ 100 chars OR SUMMARY.md documents the exception with HTML evidence.
    - `pnpm check:firm barun` shows at least one item with body length ≥ 100 chars OR SUMMARY.md documents the exception with HTML evidence.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm vitest run` exits 0.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify selector fixes via audit regression</name>
  <files>.planning/phases/06-firm-audit-probe/06-AUDIT.md (regenerated, read-only for human review)</files>
  <action>
    Pause for the human to review the regenerated audit report and approve/reject the selector changes made in Tasks 1 and 2. Claude automates the audit regeneration and test-suite spot-check BEFORE pausing; the human only reads the output.
  </action>
  <verify>
    <automated>pnpm audit:firms && pnpm vitest run</automated>
  </verify>
  <done>
    Human has responded with one of: "approved", "approved with exceptions: <list>", or "regression: <firm-id>". If regression reported, plan is paused for revert and re-run; do NOT proceed to plan 05.
  </done>
  <what-built>
    Selectors updated for logos, skadden, lee-ko, barun. Run the full audit and confirm the target rows flipped to OK (or the documented exceptions match the SUMMARY.md).
  </what-built>
  <how-to-verify>
    1. Run `pnpm audit:firms` (takes ~35s). Wait for completion.
    2. Read the regenerated `.planning/phases/06-firm-audit-probe/06-AUDIT.md`.
    3. Inspect the summary table — the target rows should show:
       - `bkl` — `OK` (from plan 03)
       - `logos` — `OK` (or documented exception)
       - `skadden` — `OK` (or documented exception)
       - `lee-ko` — `OK` (or documented exception)
       - `barun` — `OK` (or documented exception)
       - `yoon-yang`, `latham` — still `OK` (no regression)
       - `clifford-chance`, `freshfields` — still `OK` (no regression)
       - `shin-kim`, `yulchon`, `kim-chang` — unchanged (not targeted by this plan)
    4. **Approve criteria:**
       - If all 4 target rows show `OK` → type "approved".
       - If some target rows did not flip to OK but SUMMARY.md documents the reason with HTML evidence and a proposed escalation → type "approved with exceptions: <list>".
       - If a previously-OK row regressed (e.g., yoon-yang now shows detail-empty) → type "regression: <firm-id>" and STOP — we need to revert the change to that firm and re-run.

    5. Spot-check: run `pnpm vitest run` once more — expect 0 regressions in the 180+ existing test suite.
  </how-to-verify>
  <resume-signal>Type "approved", "approved with exceptions: X, Y", or "regression: X"</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External firm HTML → cheerio/Playwright selector match | A firm's HTML is untrusted; malformed selectors could hang parsing or match unexpected elements. |
| External site → Playwright navigation (lee-ko/barun) | Detail pages already load via Playwright per plan 03; this plan does not change the network surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | Tampering | A firm's redesigned HTML matching our new selector incorrectly → wrong text extracted | accept | All 4 firms are legitimate legal-firm public pages. The audit probe's 4-signal classifier (exact hash + jaccard + title-token + length) will catch any wrong-body regression by flagging it `detail-identical` or `detail-empty`; plan 06's final audit is the backstop. |
| T-07-10 | Denial of Service | New selector causing Playwright `waitForSelector` hang (lee-ko, barun — Phase 4 list page) | mitigate | `wait_for` field is unchanged by this plan; DETAIL_PAGE_TIMEOUT_MS=15000 still caps detail navigation. No new hang surface introduced. |
| T-07-11 | Information Disclosure | A firm's HTML containing unintended content matched by overly broad body selector | accept | Personal-use tool; recipient is the authoring user. Overly broad content is a quality issue, not a security one — it would manifest as a low-quality Gemini summary, not a data leak. |
</threat_model>

<verification>
- `pnpm audit:firms` regenerates 06-AUDIT.md with target rows flipped to OK (or documented exceptions).
- `pnpm vitest run` exits 0 (no regression).
- `pnpm tsc --noEmit` exits 0.
- Human checkpoint confirms the audit table shape.
</verification>

<success_criteria>
- logos, skadden, lee-ko, barun — each shows `OK` in audit report OR has a documented escalation path.
- No regression introduced to previously-OK firms.
- SUMMARY.md captures every selector change with the HTML evidence that justified it (for future maintenance).
</success_criteria>

<output>
After completion, create `.planning/phases/07-spa-aware-detail-tier/07-04-SUMMARY.md` per template. Include a per-firm table: `Firm | Old selector | New selector | Evidence HTML snippet | Audit status before → after`.
</output>
