---
phase: 07-spa-aware-detail-tier
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - config/firms.yaml
autonomous: true
requirements: [DETAIL-04]
tags: [phase-7, yaml, config, migration]

must_haves:
  truths:
    - "bkl has `detail_tier: 'js-render'` declared in firms.yaml (fixes v1.0 UAT hallucination — SC-2)"
    - "kim-chang has `detail_tier: 'js-render'` declared in firms.yaml (DETAIL-04 activation — SC-3)"
    - "lee-ko, yoon-yang, barun, latham (all existing type: js-render firms) have `detail_tier: 'js-render'` explicitly declared to prevent regression from the type-gate removal in plan 02"
    - "All other firms (clifford-chance, freshfields, shin-kim, yulchon, logos, skadden, cooley) remain untouched — they inherit zod's 'static' default (DETAIL-03 backwards compat)"
    - "The YAML header block documents the new `detail_tier` field in Korean for non-developers (CONF-07)"
    - "Running `pnpm audit:firms` after this plan shows bkl row transitioning from `detail-identical` → `OK`"
  artifacts:
    - path: "config/firms.yaml"
      provides: "6 firm blocks with detail_tier: 'js-render' + updated header stanza"
      contains: "detail_tier: 'js-render'"
  key_links:
    - from: "config/firms.yaml"
      to: "src/config/schema.ts FirmSchema"
      via: "zod validation at startup"
      pattern: "detail_tier"
    - from: "config/firms.yaml bkl block"
      to: "src/pipeline/enrichBody.ts Playwright branch"
      via: "detail_tier-gated dispatch"
      pattern: "detail_tier: 'js-render'"
---

<objective>
Add `detail_tier: 'js-render'` to 6 firm blocks in `config/firms.yaml` and append an explanatory stanza to the header comment block. This is the declarative activation step — plans 01/02 delivered the schema + code; this plan tells the config which firms opt in.

Purpose: Enables DETAIL-04 literal (bkl + kim-chang activation), preserves Phase 4 behavior for existing js-render firms (lee-ko, yoon-yang, barun, latham) after the type-gate removal, and keeps the YAML self-documenting for non-developers (CONF-07).
Output: One YAML file updated with 6 line-insertions + 1 header stanza. Verification via `pnpm audit:firms`.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md
@.planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md
@.planning/phases/06-firm-audit-probe/06-AUDIT.md
@.planning/phases/07-spa-aware-detail-tier/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add detail_tier: 'js-render' to 6 firm blocks in config/firms.yaml</name>
  <read_first>
    - config/firms.yaml (FULL — 226 lines; confirm current line numbers before editing)
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions (D-04 — the 6-firm list)
    - .planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md §`config/firms.yaml` (Pattern A insertion point + Pattern B header stanza)
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (evidence that bkl is detail-identical, kim-chang is detail-quality-unknown)
  </read_first>
  <action>
    In `config/firms.yaml`, add ONE LINE `detail_tier: 'js-render'` to each of 6 firm blocks. Insertion position: **between the `enabled:` line and the `selectors:` line** in each block (matches the Phase 4 ordering convention where tier-routing fields cluster above `selectors:`).

    **The 6 firms** (D-04) with exact inline-comment text per firm:

    1. **bkl** (line ~152) — after `enabled: true`, before `selectors:`:
       ```yaml
           detail_tier: 'js-render'  # Phase 7 — list is static HTML, detail is SPA (v1.0 UAT hallucination fix)
       ```

    2. **kim-chang** (line ~138) — after `enabled: true`, before `selectors:`:
       ```yaml
           detail_tier: 'js-render'  # Phase 7 DETAIL-04 activation (detail fetch previously empty)
       ```

    3. **lee-ko** (line ~167) — after `enabled: true` (line 174), before `selectors:` (line 175):
       ```yaml
           detail_tier: 'js-render'  # Phase 7 migration — explicit declaration (preserves Phase 4 behavior after type-gate removal)
       ```

    4. **yoon-yang** (line ~182) — after `enabled: true` (line 189), before `selectors:` (line 190):
       ```yaml
           detail_tier: 'js-render'  # Phase 7 migration — explicit declaration
       ```

    5. **barun** (line ~199) — after `enabled: true` (line 206), before `selectors:` (line 207):
       ```yaml
           detail_tier: 'js-render'  # Phase 7 migration — explicit declaration
       ```

    6. **latham** (line ~214) — after `enabled: true` (line 221), before `selectors:` (line 222):
       ```yaml
           detail_tier: 'js-render'  # Phase 7 migration — explicit declaration
       ```

    **CRITICAL constraints:**
    - Do NOT touch any other firm block (clifford-chance, freshfields, shin-kim, yulchon, logos, skadden, cooley). They MUST remain unchanged — zod defaults their detail_tier to 'static' automatically (DETAIL-03 backwards compat literal).
    - Do NOT modify `selectors:` contents in any firm — selector remediation for logos/skadden/lee-ko/barun is plan 04's job.
    - Do NOT modify `wait_for`, `type`, `url`, or any other existing field.
    - Use **single-quoted** strings `'js-render'` to match the inline-comment pattern in the codebase (not double-quoted). YAML treats both as strings; this is a style consistency choice.
    - Indentation MUST match the sibling lines (4 spaces for `detail_tier:`, matching `enabled:` and `selectors:`).

    **After all 6 edits**: Run `pnpm tsx -e "import('./src/config/loader.js').then(async m => { const {firms} = await m.loadFirms({includeDisabled: true}); console.log(JSON.stringify(firms.filter(f => f.detail_tier === 'js-render').map(f => f.id)));})"` — expect output `["kim-chang","bkl","lee-ko","yoon-yang","barun","latham"]` (order may vary).
  </action>
  <verify>
    <automated>grep -c "detail_tier: 'js-render'" config/firms.yaml</automated>
  </verify>
  <done>
    6 `detail_tier: 'js-render'` lines present (one per firm). Other firms untouched. YAML parses without zod errors.
  </done>
  <acceptance_criteria>
    - `grep -c "detail_tier: 'js-render'" config/firms.yaml` returns exactly 6.
    - `grep -n "detail_tier" config/firms.yaml` shows 6 lines of content plus lines from the header stanza added in Task 2.
    - `pnpm tsx -e "import('./src/config/loader.js').then(async m => { const {firms} = await m.loadFirms({includeDisabled: true}); const ids = firms.filter(f => f.detail_tier === 'js-render').map(f => f.id).sort().join(','); console.log(ids); })"` prints `barun,bkl,kim-chang,latham,lee-ko,yoon-yang`.
    - `pnpm tsc --noEmit` exits 0 (no type regression from config shape).
    - `pnpm vitest run` exits 0 (full suite — config/loader tests still pass).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Append detail_tier explanation stanza to firms.yaml header comment block</name>
  <read_first>
    - config/firms.yaml lines 1-46 (existing non-developer-facing header comment block)
    - .planning/phases/07-spa-aware-detail-tier/07-PATTERNS.md §`config/firms.yaml` (Pattern B header stanza shape)
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions (D-04 migration rationale)
  </read_first>
  <action>
    In `config/firms.yaml`, append a stanza to the header comment block (lines 1-46) explaining the `detail_tier` field. Insertion point: **AFTER the `enabled     :` description block (ends around line 21 with `블록 삭제 없이도 건너뛸 수 있습니다.`)**, **BEFORE** the `# -----...` separator (around line 22).

    Insert these exact lines:

    ```yaml
    #   detail_tier : detail 페이지 fetch 방식 (Phase 7+).
    #                 - 생략 시 기본값 'static' (Phase 1-6 behavior)
    #                 - 'js-render' 시 enrichBody 가 Playwright 로 detail 페이지를 가져옵니다.
    #                   list 페이지는 static HTML 인데 detail 페이지만 SPA 인 경우(bkl, kim-chang).
    #                 - type: 'js-render' 인 firm 도 명시적으로 선언 권장 (Phase 7 migration).
    ```

    Match the existing comment style: `#   fieldname   : Korean description\n#                 - bullet point 1\n#                 - bullet point 2` (see the existing `enabled` stanza at lines 20-21 for alignment).

    **DO NOT** modify:
    - The Phase 1 seed firm block (lines 23-31).
    - The Phase 2 + Phase 4 firm list block (lines 32-46).
    - The `===` separator lines.
    - Any existing field descriptions (id, name, language, type, url, timezone, enabled).
  </action>
  <verify>
    <automated>grep -c "detail_tier : detail 페이지 fetch 방식" config/firms.yaml</automated>
  </verify>
  <done>
    Header stanza appended exactly once to the non-developer-facing comment block. Non-developer readers see a description of the new field in Korean.
  </done>
  <acceptance_criteria>
    - `grep -c "detail_tier : detail 페이지 fetch 방식" config/firms.yaml` returns 1.
    - `grep -c "enrichBody 가 Playwright 로 detail 페이지를 가져옵니다" config/firms.yaml` returns 1.
    - The stanza is located BEFORE any firm block (i.e., before line 48 `firms:`).
    - YAML parses without errors (comments are whitespace in YAML): `pnpm tsx -e "import('./src/config/loader.js').then(async m => { await m.loadFirms({includeDisabled: true}); console.log('OK'); })"` prints `OK`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Regression audit — run pnpm audit:firms and confirm bkl flips to OK</name>
  <read_first>
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (current state — bkl is detail-identical, kim-chang is detail-quality-unknown)
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions (D-11 verification contract)
    - src/audit/firmAudit.ts (to understand the audit probe logic — especially the detail-identity classifier)
  </read_first>
  <action>
    Run `pnpm audit:firms` (does NOT require --include-disabled since all 6 migrated firms are enabled: true). This regenerates `.planning/phases/06-firm-audit-probe/06-AUDIT.md`.

    **Expected results after this plan's changes:**
    - **bkl**: `detail-identical` → `OK` (Playwright detail fetch now returns distinct, real article bodies per URL — the fix for the v1.0 UAT hallucination).
    - **kim-chang**: `detail-quality-unknown` → `OK` **or stays non-OK** (detail URL may still fail — if so, plan 05 handles root-cause investigation). This plan does NOT gate on kim-chang transitioning — plan 05 is where kim-chang gets fixed.
    - **lee-ko**: stays `detail-empty` (body-length under threshold; selector fix is plan 04's responsibility).
    - **barun**: stays `detail-empty` (same — plan 04 fixes it).
    - **yoon-yang, latham**: stay `OK` (already passing; migration to explicit detail_tier is a no-op behaviorally).
    - **logos, skadden**: stay `selector-empty` (list selector fix is plan 04).

    **If bkl does NOT transition to OK:** STOP and report. Something is wrong with plan 02's enrichBody branch or plan 01's schema — do NOT proceed to plan 04. Investigate by running `pnpm check:firm bkl` and comparing body lengths across 2 items.

    **If bkl DOES transition to OK:** Record the 06-AUDIT.md diff summary in the plan 03 SUMMARY.md (which rows changed). Do NOT commit the regenerated 06-AUDIT.md — it will be re-regenerated at the end of Phase 7 (plan 06) as the final verification artifact.

    Note: `pnpm audit:firms` takes ~35s per the current run metadata in 06-AUDIT.md. Network-dependent. If it fails with network errors, retry once; if still failing, proceed to plan 04 anyway and let plan 06's final audit be the acceptance gate.
  </action>
  <verify>
    <automated>pnpm audit:firms 2>&1 | grep -E "^\| bkl"</automated>
  </verify>
  <done>
    Audit regenerated. bkl row shows `OK` status in the summary table, or investigation is triggered and reported.
  </done>
  <acceptance_criteria>
    - `pnpm audit:firms 2>&1 | tail -n +1 | grep -E "^\| bkl .* \| OK \|"` returns a match OR a SUMMARY.md entry explains why bkl did not transition (and what was attempted).
    - The regenerated 06-AUDIT.md still exists at `.planning/phases/06-firm-audit-probe/06-AUDIT.md` (no file deletion).
    - `pnpm vitest run` exits 0 (no test regression from YAML change).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| YAML author → runtime behavior | Adding `detail_tier: 'js-render'` to a firm opts it into Playwright detail fetching. A malicious YAML editor could toggle expensive firms into Playwright mode, amplifying GHA minute consumption. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of Service | Malicious YAML setting detail_tier='js-render' on ALL firms → GHA minute exhaustion | accept | Single-user personal tool; the user IS the YAML author. `.github/workflows/daily.yml` has `concurrency: {group: digest-pipeline}` preventing overlap. Practical worst case: 12 firms × 10 items × 15s timeout = 30min/run; still well under the 2000min/mo GHA budget. |
| T-07-08 | Tampering | YAML injection via a firm's `detail_tier` value (e.g., YAML anchor/reference abuse) | mitigate | `z.enum(['js-render', 'static'])` is an enum literal check — zod rejects any YAML value that is not one of the two exact strings. `.strict()` at FirmSchema rejects unknown top-level keys. Threat T-07-01 (plan 01) already covers this at the schema layer. |
</threat_model>

<verification>
- `grep -c "detail_tier: 'js-render'" config/firms.yaml` === 6.
- `grep -c "detail_tier : detail 페이지" config/firms.yaml` === 1 (header stanza).
- `pnpm tsc --noEmit` exits 0.
- `pnpm vitest run` exits 0.
- `pnpm audit:firms` runs to completion; bkl row shows `OK` (SC-2 validation).
</verification>

<success_criteria>
- 6 firms opt-in to detail_tier='js-render' in YAML.
- Header block self-documents the new field in Korean.
- bkl v1.0 UAT hallucination symptom resolved (detail-identical → OK in audit).
- No regression in non-migrated firms' audit status.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spa-aware-detail-tier/07-03-SUMMARY.md` per template. Include the 06-AUDIT.md diff summary (which rows changed status).
</output>
