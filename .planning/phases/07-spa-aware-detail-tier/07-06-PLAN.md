---
phase: 07-spa-aware-detail-tier
plan: 06
type: execute
wave: 6
depends_on: [07-05]
files_modified:
  - .planning/phases/06-firm-audit-probe/06-AUDIT.md
autonomous: false
requirements: [DETAIL-01, DETAIL-02, DETAIL-03, DETAIL-04, DETAIL-05]
tags: [phase-7, verification, audit, uat, hallucination-regression]

must_haves:
  truths:
    - "Running `pnpm audit:firms` produces a regenerated 06-AUDIT.md where all Phase 7 target rows (bkl, logos, skadden, lee-ko, barun, kim-chang) are either `OK` or explicitly deferred per plan 05 option C"
    - "Running the full suite (`pnpm vitest run`) exits 0 — no code regression across 180+ tests"
    - "A manual `workflow_dispatch` run (or DRY_RUN=1 execution) confirms the v1.0 UAT hallucination symptom for bkl is gone — at least 2 of bkl's detail bodies are distinct, non-generic text"
    - "SC-1 (detail_tier field works + missing field behaves identically to before — DETAIL-03) is demonstrably true"
    - "SC-2 (bkl shows 2+ items with distinct, non-identical bodies) is demonstrably true"
    - "SC-3 (kim-chang shows at least one item with non-empty body) is demonstrably true OR plan 05 option C is in force with documented reason"
    - "SC-4 (invalid detail_tier value fails startup with path-qualified zod error) is demonstrably true via test suite + one manual probe"
  artifacts:
    - path: ".planning/phases/06-firm-audit-probe/06-AUDIT.md"
      provides: "Regenerated audit report — Phase 7's single source of truth per D-11"
      contains: "Firm | Tier | Status"
  key_links:
    - from: ".planning/phases/06-firm-audit-probe/06-AUDIT.md"
      to: "ROADMAP.md Phase 7 Success Criteria"
      via: "D-11 verification contract"
      pattern: "Status"
---

<objective>
Run the full verification sweep for Phase 7: regenerate the audit report, run the full test suite, perform a DRY_RUN pipeline check, and explicitly tick off each of SC-1 through SC-4 from the ROADMAP. The regenerated 06-AUDIT.md becomes the archived evidence that Phase 7's Success Criteria are met.

Purpose: Phase 7 closes with a concrete audit artifact, not a vibe-check. Phase 11 (Cron Resumption Gate) reads this audit to decide whether cron can restart; if any Phase 7 row is non-OK without a documented exception, Phase 11 blocks.
Output: Regenerated 06-AUDIT.md + SUMMARY.md mapping each SC to its evidence.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md
@.planning/phases/07-spa-aware-detail-tier/07-01-SUMMARY.md
@.planning/phases/07-spa-aware-detail-tier/07-02-SUMMARY.md
@.planning/phases/07-spa-aware-detail-tier/07-03-SUMMARY.md
@.planning/phases/07-spa-aware-detail-tier/07-04-SUMMARY.md
@.planning/phases/07-spa-aware-detail-tier/07-05-SUMMARY.md
@.planning/phases/06-firm-audit-probe/06-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate audit + full test suite regression sweep</name>
  <read_first>
    - .planning/phases/06-firm-audit-probe/06-AUDIT.md (current state, post-plan-05)
    - .planning/phases/07-spa-aware-detail-tier/07-CONTEXT.md §decisions D-11 (verification contract)
    - .planning/phases/07-spa-aware-detail-tier/07-05-SUMMARY.md (kim-chang final state)
  </read_first>
  <action>
    Run the following commands in sequence and record each outcome in SUMMARY.md:

    1. **Full test suite regression.**
       ```
       pnpm vitest run
       ```
       Expected: exits 0, all ~180+ tests pass including the 4 new Phase 7 schema tests from plan 01. If any test fails, STOP and escalate — do NOT proceed to audit regeneration until tests are green.

    2. **TypeScript typecheck.**
       ```
       pnpm tsc --noEmit
       ```
       Expected: exits 0. Type regressions anywhere (enrichBody.ts, run.ts, schema.ts, types.ts consumers) are a blocker.

    3. **Lint check (if eslint is wired).**
       ```
       pnpm eslint . 2>/dev/null || echo "eslint not configured — skip"
       ```
       Expected: exits 0 (or script reports "not configured"). New dead code (e.g., STATIC_BODY_MIN_CHARS leftovers) would surface here.

    4. **Full audit regeneration — THE verification artifact.**
       ```
       pnpm audit:firms
       ```
       Expected: takes ~35-40s. Exits 0 if all enabled firms are OK; exits 1 if any enabled firm is non-OK. Either exit code is acceptable as long as the non-OK firms match plan 04/05 documented exceptions.

    5. **Inspect the regenerated 06-AUDIT.md.** Run:
       ```
       cat .planning/phases/06-firm-audit-probe/06-AUDIT.md | head -40
       ```
       Confirm the summary table shape. Per-row expectations (compare against the original 06-AUDIT.md captured pre-Phase 7):

       | Firm | Before Phase 7 | Expected After Phase 7 |
       |------|---------------|------------------------|
       | clifford-chance | OK | OK (unchanged) |
       | freshfields | OK | OK (unchanged) |
       | shin-kim | list-fail | list-fail (monitor, Phase 10/11 — not Phase 7 scope) |
       | yulchon | detail-quality-unknown | detail-quality-unknown (monitor, Phase 10/11 — not Phase 7 scope) |
       | logos | selector-empty | **OK** (plan 04 selector fix) or documented exception |
       | skadden | selector-empty | **OK** (plan 04 selector fix) or documented exception |
       | kim-chang | detail-quality-unknown | **OK** (plan 05 option A/B) OR absent if plan 05 option C disabled it |
       | bkl | detail-identical | **OK** (plan 02+03 detail_tier='js-render' via Playwright) |
       | lee-ko | detail-empty | **OK** (plan 04 body selector fix) or documented exception |
       | yoon-yang | OK | OK (unchanged — plan 03 migration was a no-op) |
       | barun | detail-empty | **OK** (plan 04 body selector fix) or documented exception |
       | latham | OK | OK (unchanged — plan 03 migration was a no-op) |

    6. **Compute the before/after delta** and write it to SUMMARY.md as a diff table. Any row where "After Phase 7" does not match expectation becomes a Phase 7 scope blocker — document and escalate.

    7. **Commit the regenerated 06-AUDIT.md.** This IS the Phase 7 deliverable that Phase 11 reads. git-add `.planning/phases/06-firm-audit-probe/06-AUDIT.md` — note this file belongs semantically to Phase 6's directory but Phase 7 regenerates it as part of verification per the D-11 "single source of truth" contract. No new file path needed.
  </action>
  <verify>
    <automated>pnpm vitest run && pnpm tsc --noEmit && pnpm audit:firms</automated>
  </verify>
  <done>
    Test suite green, typecheck green, audit regenerated with the Phase 7 target rows in expected state (or documented exceptions).
  </done>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0.
    - `pnpm tsc --noEmit` exits 0.
    - `pnpm audit:firms` completes (exit 0 or 1 both acceptable per Phase 6 D-03; exit 2 is a blocker).
    - `.planning/phases/06-firm-audit-probe/06-AUDIT.md` exists and contains the summary table header `| Firm | Tier | Status | Items | Remediation | Target Phase |`.
    - `grep -E "^\| bkl .* \| OK \|" .planning/phases/06-firm-audit-probe/06-AUDIT.md` returns a match (SC-2 evidence).
    - Target-row audit status matches the expectation table above OR SUMMARY.md documents each deviation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SC-4 zod error probe — invalid detail_tier value at startup</name>
  <read_first>
    - src/config/schema.ts (final state after plan 01)
    - test/config/schema.test.ts Phase 7 detail_tier extension block (added in plan 01 — confirms this unit-test-level)
    - .planning/ROADMAP.md Phase 7 Success Criteria (SC-4 literal)
  </read_first>
  <action>
    SC-4 is "Setting `detail_tier: 'invalid-value'` in firms.yaml causes startup to fail with a zod error that includes the precise YAML path (e.g., `firms[2].detail_tier`)." Plan 01 added a unit test proving this at the schema level, but SC-4 literally says "in firms.yaml causes startup to fail" — this task confirms the end-to-end YAML → loader → zod error path.

    1. **Create an isolated test invocation** (do NOT modify the real `config/firms.yaml`). Run the following inline test:
       ```
       pnpm tsx -e "
         import('./src/config/loader.js').then(async (m) => {
           try {
             // Simulate malformed config in-memory — if loader supports a filepath override, use a temp file.
             // Otherwise, hand a known-bad firm through FirmSchema directly.
             const { FirmSchema } = await import('./src/config/schema.js');
             const bad = {
               id: 'test', name: 'T', language: 'ko', type: 'html',
               url: 'https://example.com', timezone: 'Asia/Seoul', enabled: true,
               detail_tier: 'invalid-value',
               selectors: { list_item: 'li', title: '.t', link: 'a' }
             };
             const r = FirmSchema.safeParse(bad);
             if (r.success) { console.error('FAIL: expected rejection'); process.exit(2); }
             const issueString = JSON.stringify(r.error.issues);
             if (!issueString.includes('detail_tier')) {
               console.error('FAIL: error does not mention detail_tier', issueString);
               process.exit(2);
             }
             console.log('OK: error mentions detail_tier');
             console.log('issues:', issueString.slice(0, 300));
           });
         "
       ```
       Expected stdout contains `OK: error mentions detail_tier`.

    2. **Optional — full loader-level probe.** Create `/tmp/phase7-sc4-test-firms.yaml` with one malformed firm:
       ```yaml
       firms:
         - id: test
           name: T
           language: ko
           type: html
           url: https://example.com
           timezone: Asia/Seoul
           enabled: true
           detail_tier: 'invalid-value'
           selectors:
             list_item: li
             title: .t
             link: a
       ```
       If the loader supports a CONFIG_PATH env var or equivalent (check `src/config/loader.ts`), run:
       ```
       CONFIG_PATH=/tmp/phase7-sc4-test-firms.yaml pnpm tsx -e "import('./src/config/loader.js').then(m => m.loadFirms({includeDisabled: true})).catch(e => { console.log('expected rejection:', e.message.slice(0,300)); })"
       ```
       Expected stderr contains `detail_tier`. Delete the temp file after use.

       If the loader does NOT support a path override (reads hard-coded `config/firms.yaml`), skip this step — step 1's schema-level test is sufficient evidence for SC-4 since zod runs identically whether the source is YAML or literal.

    3. Record both the inline command output and the (optional) full-loader output in SUMMARY.md as SC-4 evidence.
  </action>
  <verify>
    <automated>pnpm vitest run test/config/schema.test.ts -t "Phase 7 detail_tier extension"</automated>
  </verify>
  <done>
    SC-4 is demonstrated either via the inline schema-level probe or a full loader-level probe. Evidence recorded in SUMMARY.md.
  </done>
  <acceptance_criteria>
    - Schema-level probe (step 1) prints `OK: error mentions detail_tier`.
    - Unit test suite `pnpm vitest run test/config/schema.test.ts -t "Phase 7 detail_tier extension"` exits 0 (locks the schema behavior).
    - SUMMARY.md contains the captured output proving the zod error mentions the `detail_tier` field path.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT regression check — bkl hallucination pattern gone</name>
  <files>(no file modification — read-only UAT inspection of DRY_RUN output and recent archive/ files)</files>
  <action>
    Claude runs DRY_RUN=1 pnpm tsx src/main.ts (or a workflow_dispatch run) BEFORE pausing, captures the bkl section of the reporter output, and presents it to the human. Human inspects for hallucination pattern (identical summary prefixes across bkl items) and approves or rejects.
  </action>
  <verify>
    <automated>DRY_RUN=1 pnpm tsx src/main.ts 2>&1 | tail -200</automated>
  </verify>
  <done>
    Human has responded with one of: "approved", "approved, no-new-today", or "regression: <details>". If regression reported, plan is paused for investigation; do NOT proceed to Task 4.
  </done>
  <what-built>
    All of Phase 7: schema + code + 6 firm YAML migrations + 4 selector fixes + kim-chang resolution + regenerated audit.

    Per 07-CONTEXT.md §specifics, the v1.0 UAT hallucination reference is the 2026-04-19 bkl digest where "all 9 items started with 법무법인 태평양은 1980년에 설립된...". Phase 7 completion demands this pattern be gone.
  </what-built>
  <how-to-verify>
    1. **Trigger a manual workflow_dispatch run** against the current branch (via GitHub UI: Actions → daily.yml → Run workflow on the Phase 7 branch) OR run locally in DRY_RUN mode:
       ```
       DRY_RUN=1 pnpm tsx src/main.ts 2>&1 | tail -200
       ```
       DRY_RUN=1 runs the full pipeline (fetch → enrich → dedup → summarize) but skips email send AND state write — safe for verification.

    2. **Inspect bkl items in the output.** Look at the reporter section for the `bkl` firm — specifically the "summarized" or "would-render" stage. Expect:
       - At least 2 items with non-null `summary_ko`.
       - The first 50 characters of at least 2 summaries are DISTINCT from each other.
       - None of the summaries start with "법무법인 태평양은 1980년에 설립된" (or the semantically-equivalent hallucination phrase).
       - Confidence field is `high` or `medium` — not all `low` (low-across-the-board is the Phase 8 hallucination-guard signal, which should now be rare).

    3. **Inspect other Phase 7 firms** (logos, skadden, lee-ko, barun) if they have new items today:
       - Each item's description is non-empty and reads like article content, not generic firm blurb.
       - Summaries are content-specific.

    4. **Inspect the archive HTML** (Phase 3 OPS-09 committed `archive/YYYY/MM-DD.html`). If a recent archive exists from a prior real run, diff-compare the bkl section: old had 9 identical-sounding summaries, new should have distinct per-item content.

    5. **Approve criteria:**
       - If bkl shows ≥2 items with distinct summaries → type "approved".
       - If bkl still shows identical-sounding summaries across items → type "regression: bkl hallucination persists" and investigate — likely a missed enrichBody branch or a selector issue.
       - If bkl has 0 new items today (bootstrap seeded all URLs, nothing new) → type "approved, no-new-today" — the hallucination symptom is quiescent, and the NEXT real cron run will be the true UAT. Record this in SUMMARY.md as a caveat that Phase 11's manual UAT remains the final gate.
       - If kim-chang plan 05 landed as option C (disabled), confirm kim-chang is NOT in the digest at all — neither a hallucination nor a failed-firm footer entry for kim-chang blocks approval.

    6. Spot-check the email footer (if any items produced a render): failed-firm list should match the audit's non-OK rows (shin-kim, yulchon, and any Phase 4 exceptions).
  </how-to-verify>
  <resume-signal>Type "approved", "approved, no-new-today", or "regression: <details>"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Emit Phase 7 VERIFICATION.md mapping each SC to its evidence</name>
  <read_first>
    - .planning/ROADMAP.md Phase 7 Success Criteria (SC-1..4 literal text)
    - Regenerated .planning/phases/06-firm-audit-probe/06-AUDIT.md
    - .planning/phases/07-spa-aware-detail-tier/07-01-SUMMARY.md through 07-05-SUMMARY.md
    - Task 3 resume-signal (UAT result)
  </read_first>
  <action>
    Create `.planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md` with the following exact shape (mirrors Phase 5's VERIFICATION.md approach):

    ```markdown
    # Phase 7 Verification Report

    **Generated:** <YYYY-MM-DD>
    **Status:** <PASSED | PASSED WITH EXCEPTIONS | BLOCKED>
    **Audit artifact:** .planning/phases/06-firm-audit-probe/06-AUDIT.md (regenerated by this phase)

    ## Success Criteria → Evidence

    ### SC-1: detail_tier field works; unset firms behave identically (DETAIL-01, DETAIL-03)
    - **Evidence:** <link to 07-01-SUMMARY.md — schema test block>, <link to 07-02-SUMMARY.md — enrichBody static branch unchanged>, audit rows for clifford-chance / freshfields / shin-kim / yulchon / skadden / logos / cooley (all unset detail_tier) show NO regression from pre-Phase-7 state.
    - **Audit row (representative):** `| clifford-chance | rss | OK | 50 | n/a | n/a |` — unchanged from pre-Phase 7.
    - **Status:** PASSED

    ### SC-2: bkl shows 2+ items with distinct, non-identical extracted body (DETAIL-02, DETAIL-04)
    - **Evidence:** Regenerated audit row `| bkl | html | OK | 9 | n/a | n/a |` (was `detail-identical` pre-Phase-7). UAT DRY_RUN <date> showed distinct first-50-char body prefixes across items.
    - **Status:** PASSED

    ### SC-3: kim-chang shows ≥1 item with non-empty body (DETAIL-04)
    - **Evidence:** <one of>
      - (option A/B) Audit row `| kim-chang | html | OK | 5 | n/a | n/a |`. Body lengths >100 chars per sampled item.
      - (option C) kim-chang disabled with documented reason per 07-05-SUMMARY.md; DETAIL-04 deferred with WAF evidence; Phase 11 gate sees kim-chang excluded, not hallucinating.
    - **Status:** <PASSED | DEFERRED WITH DOCUMENTED REASON>

    ### SC-4: detail_tier: 'invalid-value' fails startup with zod path-qualified error (DETAIL-05)
    - **Evidence:** `pnpm vitest run test/config/schema.test.ts -t "Phase 7 detail_tier extension"` passes 4/4 tests including the "rejects detail_tier: 'invalid-value' with path-qualified zod error" assertion (landed in 07-01). Inline tsx probe also confirmed JSON.stringify(issues) includes `detail_tier`.
    - **Status:** PASSED

    ## Out-of-scope residuals

    - shin-kim list-fail — Phase 10/11 monitor per 06-AUDIT.md.
    - yulchon detail-quality-unknown — Phase 10/11 monitor per 06-AUDIT.md.
    - <any plan-04 documented exceptions>
    - <plan-05 option C disable-with-reason, if applicable>

    ## Phase 7 close-out

    Closing conditions satisfied:
    - SC-1..4 met (or deferred with explicit reason).
    - `pnpm vitest run` green.
    - `pnpm audit:firms` regenerated; Phase 7 target rows flipped to OK or documented exceptions.
    - UAT regression checkpoint approved per 07-06 Task 3.

    Phase 7 is complete. Downstream Phase 8 (hallucination guard) can proceed.
    ```

    Fill in `<bracketed>` sections with the actual evidence from prior tasks. Commit this file alongside the regenerated 06-AUDIT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md && grep -E "^## Success Criteria" .planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md</automated>
  </verify>
  <done>
    Phase 7 VERIFICATION.md written, all 4 SC mapped to evidence, Phase 7 close-out recorded.
  </done>
  <acceptance_criteria>
    - `.planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md` exists.
    - `grep -cE "^### SC-[1-4]:" .planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md` returns 4.
    - `grep -c "Status: PASSED" .planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md` returns at least 3 (SC-3 may be DEFERRED if option C; SC-1/2/4 must be PASSED).
    - `grep -c "06-AUDIT.md" .planning/phases/07-spa-aware-detail-tier/07-VERIFICATION.md` returns at least 1 (references the regenerated artifact).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DRY_RUN=1 local execution → production code paths | Task 3 runs the full fetch → enrich → summarize pipeline locally. The only external surfaces are the 12 firm websites + Gemini API (via GEMINI_API_KEY env var). |
| Regenerated 06-AUDIT.md → Phase 11 decision | This file becomes the authoritative Phase 7 artifact. If it is accidentally truncated or malformed, Phase 11 could proceed on stale evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-15 | Repudiation | Regenerated 06-AUDIT.md without timestamp → Phase 11 cannot prove the audit is current | mitigate | The audit report header contains `_Generated by_ pnpm audit:firms _at_ <ISO-timestamp>` — always current at regeneration. Task 1 step 7 commits it with a clear git message. |
| T-07-16 | Information Disclosure | DRY_RUN output in Task 3 leaking GEMINI_API_KEY or recipient email to logs | mitigate | Phase 1 scrubSecrets (util/logging.ts) already masks any `process.env.*` value appearing in logs (length>8 gate). UAT output prints to local stdout only — not committed. |
| T-07-17 | Tampering | Manually-edited 06-AUDIT.md (bypassing `pnpm audit:firms`) | mitigate | Task 1 step 4 REQUIRES running `pnpm audit:firms` to regenerate; Task 1 step 7 commits the file. Human reviewer at Task 3 checkpoint can spot-check the timestamp in the report header against the current run time. |
</threat_model>

<verification>
- All of Task 1-4 acceptance criteria met.
- Phase 7 VERIFICATION.md committed with SC→evidence mapping.
- Regenerated 06-AUDIT.md committed.
- `pnpm vitest run`, `pnpm tsc --noEmit`, `pnpm audit:firms` all complete successfully.
</verification>

<success_criteria>
- ROADMAP.md Phase 7 can be marked `[x]`.
- Audit artifact is refreshed for Phase 11 cron gate.
- VERIFICATION.md provides human-readable evidence trail.
</success_criteria>

<output>
After completion, create `.planning/phases/07-spa-aware-detail-tier/07-06-SUMMARY.md` per template. Note: 07-VERIFICATION.md is also created as part of Task 4.
</output>
