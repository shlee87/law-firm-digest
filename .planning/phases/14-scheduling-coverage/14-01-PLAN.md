---
phase: 14-scheduling-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/daily.yml
  - .github/workflows/weekly.yml
autonomous: false
requirements: [SCHED-01]
tags: [scheduling, github-actions, cron, ops]

must_haves:
  truths:
    - "daily.yml cron triggers on every day of the week (Mon–Sun) at 21:00 KST"
    - "weekly.yml cron triggers on Monday 06:00 KST (= Sunday 21:00 UTC), separated in time from daily Mon run"
    - "concurrency: digest-pipeline lock blocks remain byte-identical in both yml files"
    - "Header comments above each cron line accurately describe the new cadence"
  artifacts:
    - path: ".github/workflows/daily.yml"
      provides: "Daily fetch schedule covering all 7 days"
      contains: "cron: '0 12 * * 0-6'"
      must_not_modify:
        - region: "concurrency block (group: digest-pipeline + cancel-in-progress: false)"
          pattern: "concurrency:[\\s\\S]*?group: digest-pipeline[\\s\\S]*?cancel-in-progress: false"
          rationale: "SPEC R-03 byte-identity invariant — concurrency lock must remain unchanged pre/post phase. Enforced at task acceptance via `git diff | grep -E '^[-+]\\s+(group: digest-pipeline|cancel-in-progress)'` returning zero lines."
    - path: ".github/workflows/weekly.yml"
      provides: "Weekly Mon 06:00 KST schedule (= Sun 21:00 UTC)"
      contains: "cron: '0 21 * * 0'"
      must_not_modify:
        - region: "concurrency block (group: digest-pipeline + cancel-in-progress: false)"
          pattern: "concurrency:[\\s\\S]*?group: digest-pipeline[\\s\\S]*?cancel-in-progress: false"
          rationale: "SPEC R-03 byte-identity invariant — concurrency lock must remain unchanged pre/post phase. Enforced at task acceptance via `git diff | grep -E '^[-+]\\s+(group: digest-pipeline|cancel-in-progress)'` returning zero lines."
  key_links:
    - from: ".github/workflows/daily.yml schedule.cron"
      to: "GitHub Actions cron parser"
      via: "workflow_dispatch + natural schedule trigger"
      pattern: "cron:\\s*'0 12 \\* \\* 0-6'"
    - from: ".github/workflows/weekly.yml schedule.cron"
      to: "GitHub Actions cron parser"
      via: "workflow_dispatch + natural schedule trigger"
      pattern: "cron:\\s*'0 21 \\* \\* 0'"
---

<objective>
Add Monday to daily.yml so the pipeline fetches every weekday (closing the Sun→Tue ~48h gap) and move weekly.yml to Mon 06:00 KST so the two Monday runs are time-separated (15h apart). Cron lines + their header comments only — concurrency/permissions/steps stay byte-identical.

Purpose: Eliminate the 일–화 ~48h fetch gap identified in v1.1 close (SCHED-01) while preserving the Phase 13 daily/weekly RPD split.
Output: 2 yml files modified, ready for the SCHED-02 mechanical cleanup in Plan 14-02 (commit boundary aligned with D-01).
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-scheduling-coverage/14-SPEC.md
@.planning/phases/14-scheduling-coverage/14-CONTEXT.md
@.planning/phases/14-scheduling-coverage/14-PATTERNS.md

<interfaces>
Current cron + concurrency state. Source of truth for byte-identity diffs.
Extracted from .github/workflows/{daily,weekly}.yml at commit 2c74640.

daily.yml lines 22-28 (current):
```yaml
on:
  # 발송 일정 — config/settings.yaml의 schedule.cron과 동일하게 유지하세요.
  # 변경 방법: 아래 cron 값을 수정한 뒤 config/settings.yaml도 동일하게 맞춰주세요.
  # 형식: '분 시(UTC) 일 월 요일' (KST = UTC+9)
  schedule:
    - cron: '0 12 * * 0,2-6'  # Phase 13: 화~일 매일 21:00 KST (월요일은 weekly.yml 이 담당)
  workflow_dispatch: {} # manual trigger for smoke tests / gap closure
```

daily.yml lines 30-32 (BYTE-IDENTITY INVARIANT — must not change):
```yaml
concurrency: # OPS-02 — serialize overlapping runs (Pitfall #5)
  group: digest-pipeline
  cancel-in-progress: false
```

weekly.yml lines 26-31 (current):
```yaml
on:
  # Phase 13 cadence — config/settings.yaml decoupling tracked separately
  # (SPEC §Out-of-scope §1 / §2).
  schedule:
    - cron: '0 12 * * 1'  # 월요일 21:00 KST = 12:00 UTC
  workflow_dispatch: {} # manual trigger for smoke tests / gap closure
```

weekly.yml lines 33-35 (BYTE-IDENTITY INVARIANT — must not change):
```yaml
concurrency: # OPS-02 — serialize overlapping runs (shared with daily.yml)
  group: digest-pipeline
  cancel-in-progress: false
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update daily.yml cron to all 7 days + refresh header comment</name>
  <files>.github/workflows/daily.yml</files>
  <read_first>
    - .github/workflows/daily.yml (full file — verify line numbers haven't drifted)
    - .planning/phases/14-scheduling-coverage/14-PATTERNS.md (lines 23-60 — daily.yml target shape)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Requirements §1 + §3)
  </read_first>
  <action>
    Replace lines 22-28 of .github/workflows/daily.yml with the new header comment + 7-day cron. Target state (per PATTERNS.md §daily.yml + CONTEXT D-01):

    ```yaml
    on:
      # 발송 일정 — cron은 이 파일과 weekly.yml에서 직접 관리합니다.
      # 변경 절차: 아래 cron 줄을 수정 → commit → push → `gh workflow run daily.yml`로 검증.
      # Phase 13 lesson: day-of-week 필드에서 0(Sun)과 7(Sun alias) 동시 사용 금지 (GH Actions reject).
      # 형식: '분 시(UTC) 일 월 요일' (KST = UTC+9)
      schedule:
        - cron: '0 12 * * 0-6'  # Phase 14: 매일(월~일) 21:00 KST
      workflow_dispatch: {} # manual trigger for smoke tests / gap closure
    ```

    Key rules (per D-01 + PATTERNS.md):
    - Cron value MUST be `'0 12 * * 0-6'` (collision-free, no day-7 / day-0 overlap — Phase 13 lesson preserved per STATE.md Accumulated Context line 62).
    - Drop the old "config/settings.yaml과 동일하게 유지" line — settings.yaml is no longer the source of truth.
    - Header comment block stays 4 lines (formatting parity with current shape).
    - DO NOT touch lines 30-32 (concurrency block — SPEC R-03 byte-identity invariant).
    - DO NOT touch line 34+ (permissions, jobs, steps, git-auto-commit-action — D-23 atomic commit invariant from STATE.md / CONTEXT).
    - DO NOT alter `workflow_dispatch: {}` line — leave as-is.
  </action>
  <verify>
    <automated>[ "$(grep -c "cron: '0 12 \* \* 0-6'" .github/workflows/daily.yml)" = "1" ] &amp;&amp; [ "$(grep -cE "cron: '0 12 \* \* 0,2-6'" .github/workflows/daily.yml)" = "0" ] &amp;&amp; grep -A2 '^concurrency:' .github/workflows/daily.yml | grep -q 'group: digest-pipeline'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cron: '0 12 \* \* 0-6'" .github/workflows/daily.yml` returns `1` (exactly one occurrence).
    - `grep -cE "cron: '0 12 \* \* 0,2-6'" .github/workflows/daily.yml` returns `0` (old cron fully replaced).
    - `grep -qE "sync-schedule|config/settings\.yaml의 schedule" .github/workflows/daily.yml` returns non-zero exit (zero matches — the stale "keep in sync" instruction is gone).
    - `git diff .github/workflows/daily.yml | grep -E "^[-+]\s+(group: digest-pipeline|cancel-in-progress)"` returns zero lines (concurrency block byte-identical).
    - `git diff .github/workflows/daily.yml | grep -cE "^[-+]\s+- cron:"` returns at most 2 (one minus, one plus).
    - File still parses as valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/daily.yml'))"` exits 0 (or skip if python3 yaml unavailable; gh CLI parse in Task 3 covers this).
  </acceptance_criteria>
  <done>daily.yml cron line is `'0 12 * * 0-6'`, header comment describes new 7-day cadence + Phase 13 lesson + direct-edit procedure, concurrency block unchanged, no other diff outside lines 22-28.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update weekly.yml cron to Mon 06:00 KST + refresh header comment</name>
  <files>.github/workflows/weekly.yml</files>
  <read_first>
    - .github/workflows/weekly.yml (full file — verify line numbers haven't drifted)
    - .planning/phases/14-scheduling-coverage/14-PATTERNS.md (lines 62-95 — weekly.yml target shape + cron arithmetic check)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Requirements §2 + §3)
  </read_first>
  <action>
    Replace lines 26-31 of .github/workflows/weekly.yml with the new header comment + Mon-06:00-KST cron. Target state (per PATTERNS.md §weekly.yml + CONTEXT D-01):

    ```yaml
    on:
      # 발송 일정 — Mon 06:00 KST. daily.yml의 Mon 21:00 KST run과 시간 분리.
      # 변경 절차: 아래 cron 줄을 수정 → commit → push → `gh workflow run weekly.yml`로 검증.
      schedule:
        - cron: '0 21 * * 0'  # Phase 14: 월요일 06:00 KST = 일요일 21:00 UTC
      workflow_dispatch: {} # manual trigger for smoke tests / gap closure
    ```

    Cron arithmetic verification (per PATTERNS.md §weekly.yml lines 86-89):
    - Mon 06:00 KST = Mon 06:00 - 9h UTC = Sun 21:00 UTC.
    - Day-of-week `0` = Sunday in cron. Standalone `0` is collision-free (Phase 13 lesson only fires when `0` AND `7` coexist in same field).
    - Resulting cron string: `'0 21 * * 0'` (minute=0, hour=21 UTC, day=*, month=*, dow=0 Sunday).

    Key rules:
    - Cron value MUST be `'0 21 * * 0'` (per SPEC R-02 acceptance criterion line 77 — exact string `'0 21 * * 0'` required).
    - Header comment block is 2 lines (matches current 2-line shape, just reworded).
    - DO NOT touch lines 33-35 (concurrency block — SPEC R-03 byte-identity invariant).
    - DO NOT touch line 37+ (permissions, jobs, steps, git-auto-commit-action, Issue-on-failure step at 123-156 — D-23 atomic commit invariant).
    - DO NOT alter `workflow_dispatch: {}` line — leave as-is.
  </action>
  <verify>
    <automated>[ "$(grep -c "cron: '0 21 \* \* 0'" .github/workflows/weekly.yml)" = "1" ] &amp;&amp; [ "$(grep -cE "cron: '0 12 \* \* 1'" .github/workflows/weekly.yml)" = "0" ] &amp;&amp; grep -A2 '^concurrency:' .github/workflows/weekly.yml | grep -q 'group: digest-pipeline'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cron: '0 21 \* \* 0'" .github/workflows/weekly.yml` returns `1` (exact target string present, exactly once).
    - `grep -cE "cron: '0 12 \* \* 1'" .github/workflows/weekly.yml` returns `0` (old Mon 21:00 KST cron fully replaced).
    - `git diff .github/workflows/weekly.yml | grep -E "^[-+]\s+(group: digest-pipeline|cancel-in-progress)"` returns zero lines (concurrency block byte-identical).
    - `git diff .github/workflows/weekly.yml | grep -cE "^[-+]\s+- cron:"` returns at most 2 (one minus, one plus).
    - File still parses as valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/weekly.yml'))"` exits 0 (or skip if python3 yaml unavailable; gh CLI parse in Task 3 covers this).
  </acceptance_criteria>
  <done>weekly.yml cron line is `'0 21 * * 0'`, header comment describes Mon 06:00 KST cadence with time-separation rationale, concurrency block unchanged, no other diff outside lines 26-31.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify both workflows accept new cron via workflow_dispatch (D-04)</name>
  <files>.github/workflows/daily.yml, .github/workflows/weekly.yml</files>
  <read_first>
    - .github/workflows/daily.yml (confirm Task 1 changes committed)
    - .github/workflows/weekly.yml (confirm Task 2 changes committed)
    - .planning/phases/14-scheduling-coverage/14-CONTEXT.md (D-04 — verification approach)
  </read_first>
  <what-built>
    daily.yml cron now `'0 12 * * 0-6'` (all 7 days at 21:00 KST). weekly.yml cron now `'0 21 * * 0'` (Mon 06:00 KST). Both header comments rewritten. Concurrency blocks untouched.
  </what-built>
  <action>
    Per CONTEXT D-04 (both workflows dispatched to validate GH Actions parser accepts both cron strings — Phase 11 D-03 acceptance-note pattern):

    1. Commit Plan 14-01 changes locally:
       ```
       git add .github/workflows/daily.yml .github/workflows/weekly.yml
       git commit -m "fix(14): daily/weekly cron split — add Mon to daily, shift weekly to Mon 06:00 KST"
       git push
       ```
       (Commit message wording is Claude's Discretion per CONTEXT line 79 + PATTERNS.md commit style — match Phase 13 `fix(13-06)` precedent.)

    2. After push lands on main, dispatch both workflows:
       ```
       gh workflow run daily.yml
       gh workflow run weekly.yml
       ```

    3. Wait ~30s, then list recent runs and capture the IDs:
       ```
       gh run list --workflow=daily.yml --limit=3
       gh run list --workflow=weekly.yml --limit=3
       ```
       Expected: most recent run for each shows `workflow_dispatch` event with status `in_progress` or `completed` + `success`.

    4. Confirm GH Actions parsed the new cron strings:
       ```
       gh workflow view daily.yml --yaml | grep "cron:"
       gh workflow view weekly.yml --yaml | grep "cron:"
       ```
       Expected output: `- cron: '0 12 * * 0-6'` and `- cron: '0 21 * * 0'`.

    5. Pass both run IDs back to user — they will be embedded in the STATE.md acceptance note created in Plan 14-02 Task 4.

    If either workflow_dispatch fails on the cron line itself, GH Actions will reject before the runner starts — that's the aggressive failure signal (Phase 13 D-04 detection principle) we're catching here.
  </action>
  <how-to-verify>
    1. `git log --oneline -1` shows the SCHED-01 functional commit.
    2. `gh run list --workflow=daily.yml --limit=1` shows a `workflow_dispatch` event entry from this session.
    3. `gh run list --workflow=weekly.yml --limit=1` shows a `workflow_dispatch` event entry from this session.
    4. `gh workflow view daily.yml --yaml | grep cron` returns `- cron: '0 12 * * 0-6'`.
    5. `gh workflow view weekly.yml --yaml | grep cron` returns `- cron: '0 21 * * 0'`.
    6. Both runs complete (or are progressing toward) `success` — no syntax-rejection at GH Actions parser.
  </how-to-verify>
  <verify>
    <automated>git log --oneline -1 | grep -qiE 'cron|14|sched' &amp;&amp; gh workflow view daily.yml --yaml 2>/dev/null | grep -q "cron: '0 12 \* \* 0-6'" &amp;&amp; gh workflow view weekly.yml --yaml 2>/dev/null | grep -q "cron: '0 21 \* \* 0'"</automated>
  </verify>
  <acceptance_criteria>
    - SCHED-01 commit is present on main (`git log --oneline -1` shows the Phase 14 commit subject).
    - `gh workflow view daily.yml --yaml | grep "cron:"` returns exactly `- cron: '0 12 * * 0-6'`.
    - `gh workflow view weekly.yml --yaml | grep "cron:"` returns exactly `- cron: '0 21 * * 0'`.
    - `gh run list --workflow=daily.yml --limit=1 --json conclusion,event,databaseId` shows event=`workflow_dispatch` and conclusion in {`success`, `null` (still running)}.
    - `gh run list --workflow=weekly.yml --limit=1 --json conclusion,event,databaseId` shows event=`workflow_dispatch` and conclusion in {`success`, `null` (still running)}.
    - Both run IDs (`databaseId` from the previous two commands) captured and recorded for Plan 14-02 Task 4.
  </acceptance_criteria>
  <done>SCHED-01 commit landed on main, both workflows dispatched and accepted by GH Actions parser (success or in-progress), both run IDs captured for the STATE.md note in Plan 14-02.</done>
  <resume-signal>Type "approved" with the two run IDs (e.g., "approved daily=12345 weekly=67890") or describe issues. Proceed to Plan 14-02 only after both dispatches succeed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local edit → GH Actions parser | yml syntax change becomes runtime cron behavior the moment commit lands on main |
| GH Actions runner → digest-pipeline lock | Two scheduled triggers (Mon daily 21:00 KST + Mon weekly 06:00 KST) could overlap with hosting delays — concurrency lock is the serialization guarantee |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Denial of Service | daily.yml cron expression | mitigate | Use collision-free `'0 12 * * 0-6'` (Phase 13 lesson per STATE.md line 62). GH Actions rejects `0,7` overlap silently — would silently kill daily fetches. Task 3 dispatch validates parser accepts the new string before merge. |
| T-14-02 | Denial of Service | weekly.yml cron expression | mitigate | Use `'0 21 * * 0'` (standalone day-0 is safe, no day-7 alias collision). Task 3 `gh workflow run weekly.yml` confirms acceptance. |
| T-14-03 | Tampering | concurrency: digest-pipeline block (both yml) | mitigate | SPEC R-03 byte-identity invariant. Task 1 + Task 2 acceptance criteria use `git diff | grep -E "^[-+]\s+(group:|cancel-in-progress)"` to PROVE block was not touched. Checker re-verifies. |
| T-14-04 | Tampering | git-auto-commit-action step + commit_message + file_pattern (both yml) | mitigate | D-23 atomic state commit invariant from CONTEXT. Tasks 1+2 explicitly exclude lines below the cron block. Acceptance: only lines 22-28 (daily) / 26-31 (weekly) appear in diff. |
| T-14-05 | Denial of Service | Mon overlap of daily 21:00 KST + weekly 06:00 KST | accept | 15h time separation makes simultaneous trigger near-impossible. concurrency lock serializes if hosting delay collides them. Issue-on-failure step in weekly.yml (lines 123-156) catches any actual failure. |
| T-14-06 | Information Disclosure | New header comments | accept | Comments describe procedure ("edit cron line, push, gh workflow run") — no secrets, no internal URLs, no PII. Public repo would expose nothing sensitive. |
| T-14-07 | Repudiation | Cron change without verification trail | mitigate | Task 3 checkpoint captures run IDs into STATE.md note in Plan 14-02 Task 4 (per CONTEXT D-04 + Phase 11 D-03 pattern) — provides post-hoc audit trail of when split shipped + whether GH Actions parser accepted. |
</threat_model>

<verification>
- All 3 task acceptance_criteria checks pass.
- Task 3 checkpoint records both run IDs.
- `git diff` on commit boundary shows ONLY lines 22-28 (daily.yml) + lines 26-31 (weekly.yml) — nothing else in either file.
- No file outside `.github/workflows/` modified in this plan's commit.
</verification>

<success_criteria>
- daily.yml schedule.cron is `'0 12 * * 0-6'` (all 7 days, 21:00 KST).
- weekly.yml schedule.cron is `'0 21 * * 0'` (Mon 06:00 KST = Sun 21:00 UTC).
- concurrency blocks of both yml files: byte-identical pre/post diff.
- Both workflows accepted by GH Actions parser via workflow_dispatch (Task 3 evidence).
- Single git commit at plan boundary: `fix(14): daily/weekly cron split — add Mon to daily, shift weekly to Mon 06:00 KST` (or similar — Claude's Discretion per CONTEXT line 79 + PATTERNS.md commit style).
</success_criteria>

<output>
After completion, create `.planning/phases/14-scheduling-coverage/14-01-SUMMARY.md` documenting:
- Final cron values committed (both files).
- workflow_dispatch run IDs from Task 3 checkpoint.
- Confirmation that concurrency blocks were not touched (git diff evidence).
- Commit SHA for the SCHED-01 functional commit.
</output>
</content>
</invoke>