---
phase: 14-scheduling-coverage
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - scripts/sync-schedule.ts
  - package.json
  - src/config/loader.ts
  - config/settings.yaml
  - CLAUDE.md
  - .planning/STATE.md
autonomous: false
requirements: [SCHED-02]
tags: [scheduling, cleanup, docs, ops, conventions]

must_haves:
  truths:
    - "scripts/sync-schedule.ts no longer exists in the repo"
    - "pnpm sync-schedule fails loudly with 'Command not found' (aggressive failure detection)"
    - "src/config/loader.ts has no toCron() export (orphaned after sync-schedule deletion)"
    - "config/settings.yaml schedule block remains zod-parseable but documents itself as '현재 미사용'"
    - "CLAUDE.md ## Conventions section contains a Workflow scheduling subsection documenting (a) direct-edit procedure for both yml files, (b) Phase 13 day-7/day-0 lesson, and (c) the time-separation rationale"
    - "STATE.md has a Phase 11 D-03 style acceptance note recording the workflow_dispatch run IDs from Plan 14-01 Task 3"
    - "Phase 14 ships exactly 2 commits total: SCHED-01 functional (from Plan 14-01) + SCHED-02 mechanical-cleanup-with-STATE-note (from Plan 14-02 Task 4) — per CONTEXT D-01"
  artifacts:
    - path: "scripts/sync-schedule.ts"
      provides: "ABSENCE — file must NOT exist after this plan"
    - path: "package.json"
      provides: "scripts dict without sync-schedule key"
      contains: "audit:firms"
    - path: "src/config/loader.ts"
      provides: "loader without toCron() export"
    - path: "config/settings.yaml"
      provides: "schedule block as zod-parseable placeholder with 미사용 header"
      contains: "schedule:"
    - path: "CLAUDE.md"
      provides: "Workflow scheduling subsection inside GSD conventions fence"
      contains: "Workflow scheduling"
    - path: ".planning/STATE.md"
      provides: "Phase 14 acceptance note with run IDs (staged INTO the SCHED-02 cleanup commit, not a separate docs commit — D-01 commit-shape lock)"
  key_links:
    - from: "CLAUDE.md ## Conventions"
      to: "Workflow scheduling subsection"
      via: "inside GSD:conventions-start/end HTML comment fence"
      pattern: "GSD:conventions-start.*Workflow scheduling.*GSD:conventions-end"
    - from: "config/settings.yaml schedule:"
      to: "src/config/schema.ts SettingsSchema.schedule"
      via: "zod parse at startup (block kept for backwards compat)"
      pattern: "schedule:.*time_utc:"
---

<objective>
Mechanical cleanup of the sync-schedule footgun: delete the script, remove its package.json entry, drop the now-orphaned toCron() helper, rewrite the settings.yaml schedule header comment to "현재 미사용", document the cron edit policy inside CLAUDE.md's GSD conventions fence, AND record the Phase 14 acceptance note in STATE.md with the run IDs captured during Plan 14-01 Task 3 — ALL bundled into a single commit per CONTEXT D-01 (2 commits total for Phase 14: one in 14-01, one in 14-02).

Purpose: Permanently eliminate the silent-regression risk that `pnpm sync-schedule` would overwrite the Phase 13 daily/weekly cron split (SCHED-02). After this plan, the only way to change cron is to edit the yml files directly, and that procedure is documented in the same Conventions block where Phase 16 will add audit:firms freshness policy.

Output: 5 files modified + 1 file deleted + 1 STATE.md note appended — all staged into a single commit at plan boundary per D-01 commit-shape lock.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14-scheduling-coverage/14-SPEC.md
@.planning/phases/14-scheduling-coverage/14-CONTEXT.md
@.planning/phases/14-scheduling-coverage/14-PATTERNS.md
@.planning/phases/14-scheduling-coverage/14-01-SUMMARY.md

<interfaces>
Current state of every file this plan touches. Source: 14-PATTERNS.md.

scripts/sync-schedule.ts (full file — DELETION TARGET):
- 37 lines total
- Only consumer of toCron() from src/config/loader.ts (verified via `grep -rn "toCron"` — only hit is sync-schedule.ts:10)

package.json scripts block (lines 6-16, current):
```json
  "scripts": {
    "dev": "tsx src/main.ts --mode=daily",
    "dev:weekly": "tsx src/main.ts --mode=weekly",
    "dry-run": "DRY_RUN=1 tsx src/main.ts --mode=daily",
    "dry-run:weekly": "DRY_RUN=1 tsx src/main.ts --mode=weekly",
    "check:firm": "tsx src/cli/checkFirm.ts",
    "audit:firms": "tsx src/cli/auditFirms.ts",
    "test": "vitest run",
    "sync-schedule": "tsx scripts/sync-schedule.ts",
    "typecheck": "tsc --noEmit"
  },
```

src/config/loader.ts toCron() block (lines 85-101, current — DELETION TARGET):
```typescript
// toCron: converts human-readable schedule fields to a GitHub Actions cron string.
// Called by scripts/sync-schedule.ts to update .github/workflows/daily.yml.
export function toCron(schedule: { time_utc: string; days: string }): string {
  const [hoursStr, minutesStr] = schedule.time_utc.split(':');
  const hours = parseInt(hoursStr, 10);
  const minutes = parseInt(minutesStr, 10);
  if (schedule.days === 'biweekly') {
    return `${minutes} ${hours} 1,15 * *`;
  }
  const daysPart =
    schedule.days === 'weekdays' ? '1-5' :
    schedule.days === 'weekends' ? '0,6' :
    schedule.days === 'weekly' ? '1' :
    '*';
  return `${minutes} ${hours} * * ${daysPart}`;
}
```

config/settings.yaml schedule block (lines 21-38, current — header comment + trailing inline comments on time_utc/days lines will be replaced; `schedule:` block itself kept):
```yaml
# --------------------------------------------------------------
# 발송 일정
# --------------------------------------------------------------
# 변경 후 터미널에서 `pnpm sync-schedule` 실행 → daily.yml 자동 업데이트
#
# time_utc 예시 (UTC 기준, KST = UTC+9):
#   '00:00'  = 09:00 KST   '01:00'  = 10:00 KST
#   '02:00'  = 11:00 KST   '22:00'  = 07:00 KST (다음날)
#
# days 선택지:
#   daily     = 매일 (기본값)
#   weekdays  = 평일만 (월~금)
#   weekends  = 주말만 (토~일)
#   weekly    = 주 1회 (매주 월요일)
#   biweekly  = 격주 1회 (매달 1일·15일 기준)
schedule:
  time_utc: '12:00'   # UTC 기준 발송 시각 (12:00 UTC = 08:00 Chicago CDT / 07:00 CST)
  days: weekly         # daily / weekdays / weekends / weekly / biweekly
```

CLAUDE.md ## Conventions fence (lines 190-194, current):
```markdown
<!-- GSD:conventions-start source:CONVENTIONS.md -->
## Conventions

Conventions not yet established. Will populate as patterns emerge during development.
<!-- GSD:conventions-end -->
```

CRITICAL: The new subsection MUST be added INSIDE the fence (between the start and end HTML comments) per CONTEXT D-02 + PATTERNS.md §CLAUDE.md "Critical rules". Anything outside the fence is wiped by future CLAUDE.md regeneration.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete sync-schedule script + remove package.json entry + remove toCron() helper</name>
  <files>scripts/sync-schedule.ts, package.json, src/config/loader.ts</files>
  <read_first>
    - scripts/sync-schedule.ts (confirm content before deletion)
    - package.json (confirm scripts block layout before editing)
    - src/config/loader.ts (confirm toCron() lives at lines 85-101 and is exported)
    - .planning/phases/14-scheduling-coverage/14-PATTERNS.md (lines 98-200 — exact target shapes for all 3 files)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Requirements §4 + acceptance criteria #4, #5, #6, #9)
  </read_first>
  <action>
    Three sub-actions in a single task (all three coupled — deleting only one creates a dangling import or broken pnpm script):

    A) Delete `scripts/sync-schedule.ts` entirely.
    Use `git rm scripts/sync-schedule.ts` (preferred) or `rm scripts/sync-schedule.ts && git add -u scripts/`. No partial edit — the entire 37-line file goes.

    B) Remove the `"sync-schedule"` key from package.json scripts block.
    Target line to delete: `    "sync-schedule": "tsx scripts/sync-schedule.ts",` (currently line 14).
    After removal, the scripts block matches PATTERNS.md §package.json target:
    ```json
      "scripts": {
        "dev": "tsx src/main.ts --mode=daily",
        "dev:weekly": "tsx src/main.ts --mode=weekly",
        "dry-run": "DRY_RUN=1 tsx src/main.ts --mode=daily",
        "dry-run:weekly": "DRY_RUN=1 tsx src/main.ts --mode=weekly",
        "check:firm": "tsx src/cli/checkFirm.ts",
        "audit:firms": "tsx src/cli/auditFirms.ts",
        "test": "vitest run",
        "typecheck": "tsc --noEmit"
      },
    ```
    Comma hygiene: the line above (`"test": "vitest run",`) keeps its trailing comma because `"typecheck"` still follows. No other keys touched. No changes to `dependencies`, `devDependencies`, `packageManager`, `name`, `type`, `private`, or anything else outside the scripts block.

    C) Remove `toCron()` function + its 2-line comment header from `src/config/loader.ts`.
    Delete lines 85-101 inclusive (the `// toCron: ...` + `// Called by ...` comment lines at 85-86, plus the entire function body from `export function toCron(...)` through the closing `}` at line 101). Per PATTERNS.md §loader.ts, collapse to single blank line between `loadSettings()` above and `loadTopics()` below for visual hygiene.

    Verification rationale (per CONTEXT decisions):
    - Per D-01 these three changes (plus Task 2 + Task 3 + STATE.md note) commit together as commit 2 = "SCHED-02 mechanical cleanup". Task 4 stages everything into a single commit.
    - Per D-04 + STATE.md Phase 13 D-04: aggressive failure detection — after this task, `pnpm sync-schedule` returns "Command not found" loudly. Good.
    - toCron() is dead code immediately after sync-schedule.ts deletion (only importer). YAGNI cleanup per SPEC In-scope §`src/config/loader.ts`.
    - `pnpm typecheck` MUST pass after this task (SPEC AC#9 — no dangling import in src/ tree).
  </action>
  <verify>
    <automated>! test -f scripts/sync-schedule.ts &amp;&amp; [ "$(jq '.scripts."sync-schedule"' package.json)" = "null" ] &amp;&amp; [ "$(grep -c 'toCron' src/config/loader.ts)" = "0" ] &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/sync-schedule.ts` returns non-zero (file gone). Equivalently: `ls scripts/sync-schedule.ts 2>&amp;1 | grep -q 'No such file'`.
    - `jq '.scripts."sync-schedule"' package.json` outputs exactly `null` (key removed).
    - `jq '.scripts."audit:firms"' package.json` outputs `"tsx src/cli/auditFirms.ts"` (other keys preserved — control check).
    - `jq '.scripts."typecheck"' package.json` outputs `"tsc --noEmit"` (other keys preserved — control check).
    - `grep -c 'toCron' src/config/loader.ts` returns `0` (function + comment both gone).
    - `grep -rn 'toCron' src/ scripts/ 2>/dev/null | wc -l` returns `0` (no dangling references repo-wide).
    - `pnpm typecheck` exits 0 (SPEC AC#9 — no orphan import broke compilation).
    - `pnpm sync-schedule` exits non-zero with stderr containing one of: `Command 'sync-schedule' not found`, `Missing script`, or pnpm's equivalent error string (SPEC AC#6 — aggressive failure detection per Phase 13 D-04).
  </acceptance_criteria>
  <done>scripts/sync-schedule.ts deleted, package.json no longer lists sync-schedule, src/config/loader.ts no longer exports toCron, `pnpm typecheck` green, `pnpm sync-schedule` loudly fails.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite config/settings.yaml schedule header to "현재 미사용" (D-03)</name>
  <files>config/settings.yaml</files>
  <read_first>
    - config/settings.yaml (lines 1-50 — confirm current schedule block + adjacent unrelated blocks)
    - .planning/phases/14-scheduling-coverage/14-PATTERNS.md (lines 203-250 — exact target shape for settings.yaml)
    - .planning/phases/14-scheduling-coverage/14-CONTEXT.md (D-03 — recommended text for new header comment)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Constraint §Backwards compatibility — zod schema parse must not break)
  </read_first>
  <action>
    Replace the schedule header comment block + the trailing inline comments on `time_utc:` and `days:` lines (currently lines 21-38 — the comment-only lines above the `schedule:` key AND the inline `# UTC 기준 …` / `# daily / weekdays / …` comments on the value lines at 37-38) with the new "현재 미사용" header and bare value lines. Target state (per PATTERNS.md §settings.yaml + CONTEXT D-03):

    ```yaml
    # --------------------------------------------------------------
    # 발송 일정 (현재 미사용)
    # --------------------------------------------------------------
    # ⚠ 이 schedule 블록은 현재 사용되지 않습니다.
    #   실제 cron 일정은 .github/workflows/daily.yml + weekly.yml에서 직접 관리합니다.
    #   변경 절차는 CLAUDE.md의 "Workflow scheduling" 섹션을 참조하세요.
    #   (zod 호환을 위해 블록 자체는 남겨두지만 값은 무시됩니다.)
    schedule:
      time_utc: '12:00'
      days: weekly
    ```

    Critical preservation rules (per PATTERNS.md "Critical preservation rules" + SPEC Constraint §Backwards compatibility):
    - `schedule:` key MUST remain.
    - `time_utc:` field MUST remain with a parseable value (zod SettingsSchema.schedule.time_utc validates this — SPEC out-of-scope §1 explicitly keeps schema untouched).
    - `days:` field MUST remain with a valid enum value (e.g., `weekly`).
    - Drop the trailing inline comments on `time_utc` and `days` lines (the `# UTC 기준 …` / `# daily / weekdays / …` notes) — they implied the values matter, which they no longer do.
    - NO mention of `sync-schedule` anywhere in the resulting file (SPEC AC#7).
    - DO NOT touch any other section: `recipient:` (lines 18-19), `gemini:` (~56-60), `digest:` (~65-68), `prompt:` (~85-99), header banner (lines 1-5). SPEC out-of-scope explicit.

    Why keep the block at all (per D-03): `src/config/schema.ts SettingsSchema.schedule` zod definition is SPEC out-of-scope. Deleting the YAML block would break zod parse at boot. The block becomes a documented placeholder until a future v1.3+ cleanup phase removes both the schema and the block.
  </action>
  <verify>
    <automated>[ "$(grep -cE 'sync-schedule' config/settings.yaml)" = "0" ] &amp;&amp; grep -qE '현재 미사용' config/settings.yaml &amp;&amp; grep -qE '^schedule:' config/settings.yaml &amp;&amp; grep -qE '^\s+time_utc:' config/settings.yaml &amp;&amp; grep -qE '^\s+days:' config/settings.yaml &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'sync-schedule' config/settings.yaml` returns `0` (SPEC AC#7 — no sync-schedule mentions remain anywhere in settings.yaml).
    - `grep -qE '현재 미사용' config/settings.yaml` exits 0 (new "currently unused" header in place).
    - `grep -qE '^schedule:' config/settings.yaml` exits 0 (top-level `schedule:` key preserved — zod parse compat).
    - `grep -qE '^\s+time_utc:' config/settings.yaml` exits 0 (time_utc field preserved).
    - `grep -qE '^\s+days:' config/settings.yaml` exits 0 (days field preserved).
    - Trailing inline comments on time_utc/days lines are removed: `grep -qE "time_utc:.*#" config/settings.yaml` exits non-zero (no `#` comment after time_utc value), and `grep -qE "days:.*#" config/settings.yaml` exits non-zero (no `#` comment after days value).
    - `pnpm typecheck` exits 0 (settings.yaml still parses against SettingsSchema — control check).
    - `grep -qE '^recipient:' config/settings.yaml` exits 0 (recipient block untouched — control check that other sections weren't touched).
    - `grep -qE '^gemini:' config/settings.yaml` exits 0 (gemini block untouched — control check).
    - `git diff config/settings.yaml | grep -cE '^[+-]' | head -1` — diff bounded to schedule header region (no spurious diffs in other sections; manual review acceptable since exact line count depends on comment wording).
  </acceptance_criteria>
  <done>settings.yaml schedule header rewritten to "현재 미사용" with pointer to daily.yml/weekly.yml + CLAUDE.md, `schedule:` block preserved for zod compat, trailing inline comments on time_utc/days lines stripped, no sync-schedule mentions remain, all other sections untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add "Workflow scheduling (cron edit policy)" subsection inside CLAUDE.md GSD conventions fence (D-02)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (lines 188-200 — confirm GSD:conventions fence location and current placeholder)
    - .planning/phases/14-scheduling-coverage/14-PATTERNS.md (lines 252-294 — exact target shape for CLAUDE.md subsection + critical fence rules)
    - .planning/phases/14-scheduling-coverage/14-CONTEXT.md (D-02 — recommended subsection content elements)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Requirement §5 + acceptance criterion #8)
  </read_first>
  <action>
    Inside the GSD conventions fence (lines 190-194 currently), replace the placeholder line `Conventions not yet established. Will populate as patterns emerge during development.` with a new `### Workflow scheduling (cron edit policy)` subsection. Target state (per PATTERNS.md §CLAUDE.md + CONTEXT D-02):

    ```markdown
    <!-- GSD:conventions-start source:CONVENTIONS.md -->
    ## Conventions

    ### Workflow scheduling (cron edit policy)

    Cron 일정의 단일 진실 원천은 `.github/workflows/daily.yml` + `.github/workflows/weekly.yml`의 `schedule.cron` 두 줄이다. `config/settings.yaml`의 `schedule:` 블록은 zod 호환을 위한 placeholder이며 runtime에 사용되지 않는다.

    **변경 절차:**
    1. `daily.yml` / `weekly.yml`의 `schedule.cron` 줄을 직접 편집한다.
    2. Commit + push.
    3. `gh workflow run daily.yml` + `gh workflow run weekly.yml`로 즉시 dispatch — 양쪽 yml syntax가 GH Actions parser를 통과하는지 검증.

    **Phase 13 lesson (cron syntax 충돌):**
    GH Actions는 같은 day-of-week 필드에서 `0`(Sun)과 `7`(Sun alias)이 동시에 등장하면 cron을 reject한다. Monday를 추가할 때 `2-7,0` 같은 형태를 쓰지 말고 `0-6`, `*`, 또는 `0,1,2-6`처럼 collision-free 표현을 사용한다.

    **시간 분리 원칙:**
    daily와 weekly cron의 발사 시각은 분리해 유지한다. `concurrency: digest-pipeline` lock이 동시 실행을 직렬화하긴 하지만, 동일 시각 트리거는 한쪽이 다른 쪽이 끝나길 기다리는 슬롯 슬립을 만든다.

    <!-- GSD:conventions-end -->
    ```

    CRITICAL fence discipline rules (per PATTERNS.md "Critical rules" + CONTEXT D-02):
    - The entire new subsection MUST be placed INSIDE the `<!-- GSD:conventions-start -->` / `<!-- GSD:conventions-end -->` fence. Anything outside the fence is wiped by the next CLAUDE.md regeneration.
    - The existing placeholder line `Conventions not yet established...` is replaced (it becomes redundant once a real subsection exists).
    - The `## Conventions` heading itself stays.
    - The `<!-- GSD:conventions-start source:CONVENTIONS.md -->` and `<!-- GSD:conventions-end -->` HTML comments stay byte-identical — these are the fence markers and any edit to them risks breaking GSD's regeneration detection.
    - DO NOT quote literal cron values like `'0 12 * * 0-6'` in the policy text (CONTEXT §specifics line 146 explicitly prohibits this — every future cron change would require CLAUDE.md sync, recreating the footgun we just removed). Describe procedure, not values.
    - DO NOT touch any other section of CLAUDE.md: `## Project`, `## Technology Stack`, `## Architecture`, `## Project Skills`, `## GSD Workflow Enforcement`, `## Developer Profile`. These are SPEC out-of-scope.

    Title flexibility (Claude's Discretion per CONTEXT line 75): `### Workflow scheduling (cron edit policy)` is the recommended title. `### Cron edit policy` or `### Scheduling` are acceptable alternatives if they read more naturally — pick one and commit.
  </action>
  <verify>
    <automated>awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE 'Workflow scheduling|Cron edit policy|Scheduling' &amp;&amp; awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE 'daily\.yml.*weekly\.yml|daily\.yml/weekly\.yml' &amp;&amp; awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE '0.*Sun.*7.*alias|day-7|alias 충돌|Sun alias' &amp;&amp; [ "$(grep -c 'GSD:conventions-start' CLAUDE.md)" = "1" ] &amp;&amp; [ "$(grep -c 'GSD:conventions-end' CLAUDE.md)" = "1" ] &amp;&amp; ! awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE "'[0-9].*\*.*\*'"</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE 'Workflow scheduling|Cron edit policy|Scheduling'` exits 0 (subsection title present INSIDE fence).
    - `awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE 'daily\.yml.*weekly\.yml|daily\.yml/weekly\.yml'` exits 0 (procedure references both yml files INSIDE fence).
    - `awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE 'day-7|Sun alias|alias 충돌'` exits 0 (Phase 13 lesson documented INSIDE fence — SPEC AC#8).
    - `grep -c 'GSD:conventions-start' CLAUDE.md` returns `1` and `grep -c 'GSD:conventions-end' CLAUDE.md` returns `1` (fence markers preserved byte-identical, exactly one of each — PATTERNS.md "Critical rules" §fence discipline).
    - `awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE "'[0-9].*\*.*\*'"` exits non-zero (NO literal cron values quoted inside the policy text — CONTEXT §specifics line 146 prohibits this future footgun).
    - `grep -qE 'Conventions not yet established' CLAUDE.md` exits non-zero (old placeholder line removed — superseded by real subsection).
    - Sections outside fence untouched: `grep -c '## GSD Workflow Enforcement' CLAUDE.md` returns `1`, `grep -c '## Developer Profile' CLAUDE.md` returns `1` (control checks).
  </acceptance_criteria>
  <done>CLAUDE.md ## Conventions section contains a Workflow scheduling subsection INSIDE the GSD fence, documenting (a) direct-edit procedure for both yml files, (b) Phase 13 day-7/day-0 lesson, (c) time separation principle. No literal cron values. Fence markers byte-identical.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Append STATE.md acceptance note + commit ALL Plan 14-02 changes (5 files + STATE.md) as the single SCHED-02 cleanup commit (D-01 + D-04)</name>
  <files>.planning/STATE.md, scripts/sync-schedule.ts, package.json, src/config/loader.ts, config/settings.yaml, CLAUDE.md</files>
  <read_first>
    - .planning/STATE.md (full file — confirm current format of Accumulated Context section before appending)
    - .planning/phases/14-scheduling-coverage/14-CONTEXT.md (D-01 commit-shape lock — 2 commits total for Phase 14, NOT 3; D-04 — exact acceptance note format)
    - .planning/phases/14-scheduling-coverage/14-SPEC.md (Acceptance Criteria lines 76-85 — full 10-item checklist)
    - .planning/phases/14-scheduling-coverage/14-01-SUMMARY.md (workflow_dispatch run IDs captured in Plan 14-01 Task 3)
  </read_first>
  <what-built>
    Tasks 1-3 of this plan have deleted scripts/sync-schedule.ts, removed the package.json entry + toCron() helper, rewritten settings.yaml schedule header to "현재 미사용", and added the Workflow scheduling subsection inside CLAUDE.md's GSD conventions fence. `pnpm typecheck` is green. `pnpm sync-schedule` now fails loudly. All these changes are currently unstaged/staged in the working tree but NOT yet committed — they bundle with the STATE.md note (step 1 below) into a single commit per CONTEXT D-01.
  </what-built>
  <action>
    CRITICAL ordering note (per CONTEXT D-01 — Phase 14 ships exactly 2 commits total: SCHED-01 from Plan 14-01, SCHED-02 + STATE-note from Plan 14-02. NOT 3 commits.):
    The STATE.md acceptance note MUST be written BEFORE the commit step so that `.planning/STATE.md` is dirty at commit time and gets bundled into the same `git add` / `git commit` as the 5 cleanup files. There is NO separate "docs(14): record acceptance note" commit.

    1. Append STATE.md acceptance note FIRST (before any git staging). Format (per CONTEXT D-04 exact specification + Phase 11 D-03 style):
       ```
       YYYY-MM-DD Phase 14 cron split: daily/weekly workflow_dispatch 검증 완료 — daily run ID {N1}, weekly run ID {N2}. 다음 자연 schedule trigger 확인은 1주 내 production history에서.
       ```
       Where:
       - `YYYY-MM-DD` = today's date.
       - `{N1}` = daily workflow_dispatch run ID captured during Plan 14-01 Task 3 checkpoint (from `gh run list --workflow=daily.yml`).
       - `{N2}` = weekly workflow_dispatch run ID captured during Plan 14-01 Task 3 checkpoint.

       Placement: under STATE.md `## Accumulated Context` → `### Decisions` section, at the top of the recent-decisions bullet list (most recent first), OR append a new line under `## Performance Metrics` → `**v1.2 (this milestone):**`. Either is acceptable — pick whichever matches the existing STATE.md formatting after re-reading the file. Update `last_activity:` frontmatter to today.

    2. Stage ALL 6 files (5 cleanup files + STATE.md) into a single commit and commit Plan 14-02 SCHED-02 cleanup:
       ```
       git add scripts/sync-schedule.ts package.json src/config/loader.ts config/settings.yaml CLAUDE.md .planning/STATE.md
       git commit -m "chore(14): remove sync-schedule footgun — delete script, package.json entry, toCron(), settings.yaml ref; add CLAUDE.md edit policy + STATE.md acceptance note"
       git push
       ```
       (Commit message wording is Claude's Discretion per CONTEXT line 79 + PATTERNS.md commit style — match Phase 13 `chore(13-06)` precedent. Subject line MAY mention "+ STATE.md acceptance note" to reflect the bundle, but full content is at Claude's discretion.)

    3. Run full verification gate to confirm SPEC acceptance criteria #4–#7, #9 and #6 (loud-fail):
       ```
       test -f scripts/sync-schedule.ts; echo "exit=$?"        # expect non-zero
       jq '.scripts."sync-schedule"' package.json              # expect null
       grep -c 'toCron' src/config/loader.ts                    # expect 0
       grep -cE 'sync-schedule' config/settings.yaml            # expect 0
       pnpm typecheck                                            # expect exit 0
       pnpm sync-schedule; echo "exit=$?"                       # expect non-zero with "not found" message
       ```

    4. Confirm Phase 14 produced exactly 2 commits (D-01 commit-shape lock):
       ```
       git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)'
       ```
       Expected output: `2` (one SCHED-01 functional from Plan 14-01, one SCHED-02 cleanup-with-STATE-note from Plan 14-02). If output is `3` or more, the commit-shape lock has been violated — investigate and roll back the extra commit before proceeding.

    5. Cross-check all 10 SPEC.md Acceptance Criteria (lines 76-85) and record pass/fail in 14-02-SUMMARY.md.
  </action>
  <how-to-verify>
    1. `git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)'` returns exactly `2` (Phase 14 produced exactly 2 commits per D-01).
    2. The most recent commit (the SCHED-02 cleanup) shows `.planning/STATE.md` in its file list: `git show --stat HEAD | grep -q '.planning/STATE.md'` exits 0.
    3. There is NO separate `docs(14)` commit: `git log --oneline -5 | grep -cE 'docs\(14\)'` returns `0`.
    4. All 6 verification gate commands from action step 3 produce expected outputs.
    5. STATE.md contains a line matching pattern `Phase 14 cron split.*daily run ID.*weekly run ID`.
    6. All 10 items in SPEC.md Acceptance Criteria checklist pass.
  </how-to-verify>
  <verify>
    <automated>[ "$(git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)')" = "2" ] &amp;&amp; [ "$(git log --oneline -5 | grep -cE 'docs\(14\)')" = "0" ] &amp;&amp; git show --stat HEAD | grep -q '.planning/STATE.md' &amp;&amp; ! test -f scripts/sync-schedule.ts &amp;&amp; [ "$(jq '.scripts."sync-schedule"' package.json)" = "null" ] &amp;&amp; [ "$(grep -c 'toCron' src/config/loader.ts)" = "0" ] &amp;&amp; [ "$(grep -cE 'sync-schedule' config/settings.yaml)" = "0" ] &amp;&amp; pnpm typecheck &amp;&amp; grep -qE 'Phase 14 cron split.*daily run ID.*weekly run ID' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)'` returns exactly `2` (D-01 commit-shape lock: Phase 14 = SCHED-01 + SCHED-02 with STATE.md bundled).
    - `git log --oneline -5 | grep -cE 'docs\(14\)'` returns `0` (NO separate STATE.md docs commit — would violate D-01).
    - `git show --stat HEAD | grep -q '.planning/STATE.md'` exits 0 (STATE.md is part of the SCHED-02 commit, not a separate commit).
    - All 10 SPEC.md Acceptance Criteria (lines 76-85) checked off as PASS in 14-02-SUMMARY.md with the grep/jq/test output that proved each one.
    - `grep -qE 'Phase 14 cron split.*daily run ID.*weekly run ID' .planning/STATE.md` exits 0 (acceptance note present and matches the D-04 format).
    - STATE.md `last_activity:` frontmatter updated to today's date.
    - Both run IDs from Plan 14-01 Task 3 are literally present in the STATE.md note (not placeholders).
  </acceptance_criteria>
  <done>Phase 14 produced exactly 2 commits on main (SCHED-01 functional + SCHED-02 cleanup-with-STATE-note). STATE.md has the dated acceptance note with both run IDs and was bundled into the SCHED-02 commit. All 10 SPEC acceptance criteria pass. Ready for /gsd:verify-work.</done>
  <resume-signal>Type "approved" once exactly 2 Phase 14 commits land on main (verify: `git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)'` returns 2, and `grep -cE 'docs\(14\)'` returns 0) and all 10 SPEC acceptance criteria pass. Then proceed to /gsd:verify-work.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local file edit → src/ runtime | Removing `toCron()` from loader.ts could break src/main.ts boot if any non-sync-schedule consumer imports it — verified via grep that only sync-schedule.ts:10 imports |
| config/settings.yaml → zod schema | Removing required fields would break boot — schedule block kept intact, only header comment rewritten |
| CLAUDE.md GSD fence → future regeneration | Content outside `<!-- GSD:conventions-start/end -->` markers is wiped on regeneration — subsection MUST be inside fence |
| Plan 14-02 working tree → single SCHED-02 commit | D-01 commit-shape lock: 5 cleanup files + STATE.md note MUST be staged together; a separate docs commit would violate the locked 2-commit boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-08 | Tampering | src/config/loader.ts toCron() removal | mitigate | Pre-removal grep confirmed only `scripts/sync-schedule.ts:10` imports toCron (CONTEXT D-01 verification). Task 1 verify gate runs `pnpm typecheck` to catch any dangling reference. `grep -rn 'toCron' src/ scripts/` repo-wide check is in acceptance criteria. |
| T-14-09 | Denial of Service | config/settings.yaml zod parse at boot | mitigate | `schedule:` block + `time_utc` + `days` fields preserved per SPEC Constraint §Backwards compatibility. Task 2 verify gate runs `pnpm typecheck` (settings parse path) + grep-asserts both fields still present. If removed, src/main.ts boot fails — caught immediately. |
| T-14-10 | Information Disclosure | CLAUDE.md scheduling subsection | accept | Content describes edit procedure and cron syntax lesson — no secrets, no internal URLs, no PII. Public-repo-safe. |
| T-14-11 | Tampering | CLAUDE.md GSD fence markers | mitigate | Task 3 acceptance criteria: `grep -c 'GSD:conventions-start' CLAUDE.md` = 1 and `grep -c 'GSD:conventions-end' CLAUDE.md` = 1 (markers preserved byte-identical, exactly one of each). Loss of fence = future regeneration silently wipes the new subsection — caught at task acceptance, not 6 months later. |
| T-14-12 | Repudiation | Phase 14 verification trail (run IDs lost to memory) | mitigate | Task 4 step 1 appends STATE.md note with daily + weekly workflow_dispatch run IDs per Phase 11 D-03 pattern, bundled into the SCHED-02 commit. Future audit can `gh run view {N}` to confirm GH Actions accepted the cron strings. |
| T-14-13 | Elevation of Privilege | pnpm sync-schedule "command not found" loudness | accept | Aggressive failure detection is the desired property (Phase 13 D-04). User typing `pnpm sync-schedule` post-cleanup gets loud "not found" instead of silent regression overwriting daily.yml. Mitigation = the loudness itself. |
| T-14-14 | Information Disclosure | Literal cron values in CLAUDE.md policy text | mitigate | Acceptance criterion `awk '/GSD:conventions-start/,/GSD:conventions-end/' CLAUDE.md | grep -qE "'[0-9].*\*.*\*'"` exits non-zero — no literal cron quoted inside policy text. Prevents the documentation footgun described in CONTEXT §specifics line 146 (every cron change requiring CLAUDE.md sync). |
| T-14-15 | Tampering | Phase 14 commit-shape (D-01 lock) | mitigate | Task 4 acceptance criteria: `git log --oneline -5 | grep -cE 'fix\(14\)\|chore\(14\)'` returns exactly `2`, AND `grep -cE 'docs\(14\)'` returns `0`. Catches accidental 3rd commit (e.g., separate STATE.md docs commit) that would violate the locked 2-commit boundary. |
</threat_model>

<verification>
- All 4 task acceptance_criteria checks pass.
- All 10 SPEC.md acceptance criteria (lines 76-85) confirmed pass (checklist in Task 4 instructions).
- `git log --oneline -5` shows exactly 2 Phase 14 commits on main (SCHED-01 functional + SCHED-02 cleanup-with-STATE-note) per D-01 commit-shape lock.
- The most recent commit (SCHED-02) includes `.planning/STATE.md` in its file list (`git show --stat HEAD`).
- Task 4 checkpoint records both run IDs in STATE.md note.
- No file outside `files_modified` list touched in this plan's commits.
</verification>

<success_criteria>
- scripts/sync-schedule.ts no longer exists.
- package.json scripts dict does not contain "sync-schedule" key.
- src/config/loader.ts does not contain `toCron`.
- config/settings.yaml schedule block kept (time_utc + days fields) with new "현재 미사용" header pointing to daily.yml/weekly.yml + CLAUDE.md, trailing inline comments stripped. No sync-schedule mentions.
- CLAUDE.md ## Conventions section contains a Workflow scheduling subsection INSIDE the GSD fence, documenting direct-edit procedure + Phase 13 lesson + time separation principle, with no literal cron values quoted.
- STATE.md has a dated Phase 14 acceptance note with both workflow_dispatch run IDs, bundled into the SCHED-02 commit (NOT a separate commit).
- Phase 14 ships exactly 2 commits on main per D-01 commit-shape lock.
- `pnpm typecheck` green.
- `pnpm sync-schedule` fails loudly with "not found".
</success_criteria>

<output>
After completion, create `.planning/phases/14-scheduling-coverage/14-02-SUMMARY.md` documenting:
- Commit SHA for SCHED-02 mechanical cleanup commit (the single commit that bundles 5 cleanup files + STATE.md note).
- Confirmation of all 10 SPEC.md acceptance criteria passing (checklist with pass/fail per item + the grep/jq/test output that proved it).
- Confirmation that Phase 14 produced exactly 2 commits total (`git log --oneline -5 | grep -cE 'fix\(14\)|chore\(14\)'` = 2, `grep -cE 'docs\(14\)'` = 0) per D-01 commit-shape lock.
- Final state of CLAUDE.md ## Conventions subsection (paste the new block — note "Phase 16 META-02 will add a sibling subsection here for audit:firms freshness policy").
- STATE.md note text + line number where it was inserted.
- Reference back to 14-01-SUMMARY.md for the SCHED-01 functional commit + workflow_dispatch run IDs.
</output>
</content>
</invoke>