---
phase: 11-cron-resumption-gate
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - .github/workflows/daily.yml
  - .planning/STATE.md
autonomous: false
requirements:
  - RESUME-01
  - RESUME-02

must_haves:
  truths:
    - "A manual workflow_dispatch run completes successfully and produces a real digest email"
    - "Every item in the digest email has been visually inspected against the original article"
    - "No item's Korean summary is a generic firm description or verbatim title without cause"
    - "The schedule: block in daily.yml is uncommented"
    - "STATE.md contains a dated acceptance note with inspector handle"
  artifacts:
    - path: ".github/workflows/daily.yml"
      provides: "schedule: block uncommented (cron restored)"
      contains: "cron: '0 9 * * *'"
    - path: ".planning/STATE.md"
      provides: "dated acceptance note per D-03"
  key_links:
    - from: ".github/workflows/daily.yml"
      to: "GitHub Actions cron scheduler"
      via: "schedule: - cron: '0 9 * * *'"
      pattern: "cron:"
    - from: ".planning/STATE.md"
      to: "Phase 11 completion record"
      via: "dated acceptance note"
      pattern: "cron 복원 승인"
---

<objective>
Trigger the manual end-to-end run, visually inspect every digest item, uncomment the
cron schedule, and record the dated acceptance note in STATE.md.

Purpose: RESUME-01 requires a manual workflow_dispatch run + full per-item visual
inspection before RESUME-02 (cron uncomment) can proceed. This plan gates the final
production go-live on human sign-off per D-02 (full per-item inspection, not sampling).

Output: Daily cron active, STATE.md acceptance note recorded, Phase 11 closed.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cron-resumption-gate/11-CONTEXT.md
@.planning/phases/11-cron-resumption-gate/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Trigger manual workflow_dispatch and inspect the resulting digest</name>
  <what-built>
    All Phase 6–10 work and Plan 11-01/11-02 are complete:
    - restoreFetchHost fix (bkl + kim-chang unblocked)
    - NODE_EXTRA_CA_CERTS fix (shin-kim unblocked)
    - Hallucination guard Layer 1–3 active (Phase 8)
    - Data-quality observability in step summary + email footer (Phase 10)
    - Cooley restored via sitemap tier (Phase 9)
    All 13 enabled firms are verified working via pnpm check:firm.
  </what-built>
  <how-to-verify>
    **Step 1: Push all committed changes from Plans 11-01 and 11-02 to the remote.**
    The executor will commit and push the PLAN files; ensure the code commits from
    11-01 and 11-02 are also pushed (they should be, but confirm before triggering).

    **Step 2: Trigger the workflow manually.**
    Go to: https://github.com/Phantompal/legalnewsletter/actions
    Click "Daily Digest" → "Run workflow" → confirm on the `dev` branch (or `main`
    if Plans 11-01/11-02 have been merged — follow your normal PR process).

    Wait for the run to complete (typically 2–4 minutes).

    **Step 3: Confirm the run succeeded (green checkmark).**
    If the run fails: read the run log, identify the failing step. Do NOT proceed to
    inspection or schedule uncomment. Fix the failure, re-trigger, and repeat.

    **Step 4: Open the resulting digest email in your Gmail inbox.**
    Subject: the usual daily digest format from your pipeline.

    **Step 5: Full per-item inspection (RESUME-01 D-02 — no sampling).**
    For EVERY item in the digest email:
    a. Click the source link to open the original article.
    b. Compare the Korean summary against the article body.
    c. The summary must NOT be:
       - A generic firm description ("법무법인 태평양은 1980년에 설립된…")
       - An exact verbatim title repeat WITHOUT cause (title-verbatim IS acceptable
         when the item has no body — the badge "본문 확보 실패" will appear)
       - Content clearly unrelated to the article

    **Step 6: Check the GHA step summary.**
    Click the run → "Run daily digest pipeline" step → view step summary.
    The DQOBS table should show per-firm body metrics. No HALLUCINATION_CLUSTER_DETECTED
    markers should appear for content-carrying firms.

    **Pass criteria (RESUME-01):**
    - Workflow run: green
    - Digest email received in Gmail
    - Every item's Korean summary reflects the specific article content
    - No generic firm description summaries (except items with legitimate no-body
      that correctly show the "본문 확보 실패" badge)
    - Step summary shows reasonable AvgBody values for all active firms

    **If any item fails inspection:** Note the firm ID and item title. Do NOT proceed.
    Add a blocker to STATE.md per D-04, fix the root cause, and re-run Phase 11 from
    this task.
  </how-to-verify>
  <resume-signal>Type "inspection pass" with a one-line summary of what you saw (e.g., "8 items checked, all content-specific, no hallucinations. bkl/kim-chang/shin-kim all working.") OR describe the specific failure.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Uncomment schedule in daily.yml and record STATE.md acceptance note</name>
  <files>.github/workflows/daily.yml, .planning/STATE.md</files>
  <action>
    **Only execute this task after Task 1 inspection passes.**

    **A: Uncomment the schedule block in .github/workflows/daily.yml**

    Find the paused schedule block (currently ~line 37):
    ```yaml
    # PAUSED 2026-04-19 — v1.0 data-quality audit in progress.
    # Multiple firms produce hallucinated Gemini summaries due to:
    #   - bkl detail URLs all return the same landing-page HTML (SPA)
    #   - kim-chang detail fetches fail, leaving body empty
    #   - shin-kim list fetches fail
    #   - logos / skadden list selectors return 0 items
    #   - Gemini prompt lacks a generic-body hallucination guard
    # Re-enable after .planning/backlog/v1.0-data-quality-audit.md is closed.
    # schedule:
    #   - cron: '0 9 * * *' # 09:00 UTC = 18:00 KST (OPS-01)
    ```

    Replace with:
    ```yaml
    # Cron RESUMED 2026-04-21 (Phase 11 RESUME-02). All v1.1 data-quality hardening
    # complete: restoreFetchHost fix (bkl/kim-chang), NODE_EXTRA_CA_CERTS (shin-kim),
    # Hallucination Guard (Phase 8), Cooley sitemap (Phase 9), DQOBS (Phase 10).
    # Manual dispatch + full per-item inspection passed before this uncomment.
    # If quality degrades, comment out the schedule block, add STATE.md blocker, fix,
    # re-run Phase 11 inspection before uncommenting (D-04).
    schedule:
      - cron: '0 9 * * *' # 09:00 UTC = 18:00 KST (OPS-01)
    ```

    **B: Record dated acceptance note in .planning/STATE.md**

    Append to the "Session Continuity" section (after the last session entry).
    Use the format specified in D-03:

    ```markdown
    ## Phase 11 Acceptance

    **RESUME-01:** Manual workflow_dispatch run completed successfully. Full per-item
    visual inspection passed — all items' Korean summaries reflect article-specific
    content. No hallucinations detected.

    **RESUME-02:** `schedule:` uncommented in `.github/workflows/daily.yml` as of
    {TODAY_DATE}.

    **Acceptance note (D-03):** {TODAY_DATE} 수동 검수 완료, 전 항목 원문 대조
    이상 없음. cron 복원 승인 (검수자: seonghoonyi)

    **Re-disable protocol (D-04):** If quality degrades post-cron-resume:
    1. Comment out `schedule:` in `.github/workflows/daily.yml`
    2. Add STATE.md blocker with affected firm + symptom
    3. Fix root cause
    4. Re-run Phase 11 Task 1 inspection before uncommenting
    ```

    Replace `{TODAY_DATE}` with the actual date of inspection (YYYY-MM-DD format).

    After editing both files, run `pnpm vitest run` to confirm no regressions.
  </action>
  <verify>
    <automated>cd /Users/seonghoonyi/Documents/projects/legalnewsletter && grep -c "cron: '0 9 \* \* \*'" .github/workflows/daily.yml && grep -c "schedule:" .github/workflows/daily.yml</automated>
  </verify>
  <done>
    daily.yml: schedule: block uncommented with restored cron expression '0 9 * * *';
    STATE.md: acceptance note present with today's date, "수동 검수 완료", "cron 복원 승인", inspector handle "seonghoonyi";
    pnpm vitest run exits 0;
    git commit pushed to dev branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GHA scheduler → production pipeline | Cron re-activation; inspected digest confirms pipeline health before first automatic run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Denial of Service | cron re-activation with broken firm | mitigate | Full per-item inspection in Task 1 before Task 2 runs; D-04 re-disable protocol if post-launch regression observed |
| T-11-03-02 | Information Disclosure | digest email visibility | accept | Single-recipient personal system; email goes to sarakim1705@gmail.com only; no multi-recipient concern |
</threat_model>

<verification>
1. `.github/workflows/daily.yml` contains uncommented `schedule:` with `cron: '0 9 * * *'`
2. `.planning/STATE.md` contains acceptance note with dated entry and "cron 복원 승인"
3. `pnpm vitest run` passes (443+ tests)
4. REQUIREMENTS.md RESUME-01 and RESUME-02 can be marked complete
</verification>

<success_criteria>
- RESUME-01: Manual workflow_dispatch run green + full per-item inspection passed (human-verified)
- RESUME-02: schedule: block uncommented in daily.yml with '0 9 * * *' expression
- STATE.md: dated acceptance note with D-03 format ("YYYY-MM-DD 수동 검수 완료, 전 항목 원문 대조 이상 없음. cron 복원 승인 (검수자: seonghoonyi)")
- All RESUME requirements marked complete in REQUIREMENTS.md
- Phase 11 closed in ROADMAP.md progress table
</success_criteria>

<output>
After completion, create `.planning/phases/11-cron-resumption-gate/11-03-SUMMARY.md`
</output>
