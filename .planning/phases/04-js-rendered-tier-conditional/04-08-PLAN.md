---
phase: 04-js-rendered-tier-conditional
plan: 08
type: execute
wave: 4
depends_on: ["01", "04", "07"]
files_modified:
  - config/firms.yaml
autonomous: false
requirements: [FETCH-01]
tags: [firms-yaml, activation, lee-ko, yoon-yang, barun, latham]
must_haves:
  truths:
    - "config/firms.yaml contains FOUR js-render firms with enabled: true: lee-ko, yoon-yang, barun (newly added), latham"
    - "Each js-render firm carries a wait_for field with a value verified in plan 07 PROBE-RESULTS.md"
    - "loadFirms() returns exactly 13 firms (9 Phase 2 live + 4 Phase 4 activations) when .enabled filter is applied"
    - "All four js-render firms have type js-render (NOT type html as the Phase 2 placeholders did)"
    - "The barun block is BRAND NEW — it did not exist in config/firms.yaml before Phase 4"
    - "After the check:firm smoke test for each firm completes successfully, the whole pipeline runs green via DRY_RUN=1 tsx src/main.ts"
  artifacts:
    - path: "config/firms.yaml"
      provides: "13 firms total, 13 enabled:true, all js-render firms have wait_for"
      contains: "type: js-render"
  key_links:
    - from: "config/firms.yaml"
      to: ".planning/phases/04-js-rendered-tier-conditional/04-07-PROBE-RESULTS.md"
      via: "wait_for + selectors values copied from the probe document verbatim"
      pattern: "wait_for"
    - from: "config/firms.yaml"
      to: "src/config/schema.ts"
      via: "js-render firms validate through the new superRefine (plan 01)"
      pattern: "js-render"
---

<objective>
Activate the four js-render firms in `config/firms.yaml` by: (a) flipping `enabled: true` on the three existing placeholders (lee-ko, yoon-yang, latham) and changing their `type: html` to `type: js-render`, (b) adding a new `barun` block from scratch with `type: js-render`, (c) populating the verified `wait_for` + selectors for all four from plan 07's PROBE-RESULTS.md. Then run end-to-end smoke tests via `pnpm check:firm <id>` for each firm plus a full `DRY_RUN=1 tsx src/main.ts`.

Purpose: This is the PHASE 4 CULMINATION. Plans 01-06 built the code, plan 05 wired GHA, plan 07 verified selectors. Plan 08 flips the live switch. The four firms that were previously returning zero items start contributing to the digest starting today.

Output: Single-file YAML update + smoke test transcripts documenting the first successful end-to-end run with all 13 firms active.
</objective>

<execution_context>
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/templates/summary.md
@/Users/seonghoonyi/Documents/projects/legalnewsletter/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-js-rendered-tier-conditional/04-CONTEXT.md
@.planning/phases/04-js-rendered-tier-conditional/04-RESEARCH.md
@.planning/phases/04-js-rendered-tier-conditional/04-07-PROBE-RESULTS.md
@.planning/phases/04-js-rendered-tier-conditional/04-01-PLAN.md
@config/firms.yaml
@src/config/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip placeholders (lee-ko, yoon-yang, latham) to enabled js-render</name>
  <files>config/firms.yaml</files>
  <read_first>
    - config/firms.yaml (current state — placeholders at approx L166-200 with enabled false and type html)
    - .planning/phases/04-js-rendered-tier-conditional/04-07-PROBE-RESULTS.md (verified configurations section — paste source-of-truth)
    - .planning/phases/04-js-rendered-tier-conditional/04-CONTEXT.md (D-01 four firms activated; D-12 wait_for required; D-02 Kirkland excluded)
  </read_first>
  <action>
Edit `config/firms.yaml`, targeting the three existing placeholder blocks:

1. **For the `lee-ko` block:**
   - Change `type: html` to `type: js-render`
   - Change `enabled: false` comment line to `enabled: true`
   - Add `wait_for: "<VALUE FROM PROBE-RESULTS.md>"` as a top-level field, placed directly AFTER the `url:` line and BEFORE `timezone:`
   - Update the `selectors:` block values to match probe-verified selectors (list_item, title, link)

2. **For the `yoon-yang` block:**
   - Same three changes (type, enabled, wait_for)
   - Update selectors to probe-verified values

3. **For the `latham` block:**
   - Same three changes (type, enabled, wait_for)
   - Update selectors to probe-verified values

4. **Update the Phase 2 comment block** (the one currently titled `Phase 2 firm list (D-P2-14 Option A+B — locked 2026-04-17)`). Replace its body with a Phase 2 + Phase 4 summary:

```
# =============================================================
# Phase 2 + Phase 4 firm list (locked 2026-04-18):
#   Phase 2 live firms (9): 3 RSS (cooley, clifford-chance, freshfields),
#                           4 HTML plain-href (shin-kim, yulchon, logos, skadden),
#                           2 HTML onclick-extract (kim-chang, bkl).
#   Phase 4 activations (4): lee-ko, yoon-yang, latham (flipped from disabled
#                            placeholder to type js-render) + barun (new block).
#                            All four require a wait_for CSS selector — zod
#                            superRefine enforces at config-load time.
#
# Added in Phase 4:
#   type: js-render      : Playwright chromium --only-shell fetches + hydrates
#                          the list page before cheerio extraction.
#   wait_for             : CSS selector page.waitForSelector() must resolve
#                          before reading content(). 15s timeout hardcoded.
# =============================================================
```

**All other pre-Phase-4 content (Phase 1 comment block, RSS firms, HTML firms) remains verbatim unchanged.** Only the three existing placeholder blocks and the Phase 2 comment block change in this task. Task 2 adds the new barun block.
  </action>
  <verify>
    <automated>pnpm tsx -e "const m = await import('./src/config/loader.js'); const f = await m.loadFirms(); const jsr = f.filter(x => x.type === 'js-render'); console.log('js-render count:', jsr.length); for (const x of jsr) console.log('  ', x.id, 'wait_for=' + x.wait_for);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "type: js-render" config/firms.yaml` returns exactly 3 (lee-ko, yoon-yang, latham; barun added in Task 2 brings total to 4).
    - `grep -c "^    wait_for:" config/firms.yaml` returns exactly 3 (same three firms for now).
    - `grep -c "enabled: false" config/firms.yaml` returns 0 (no more disabled firms).
    - `grep -c "enabled: true" config/firms.yaml` returns exactly 12 (9 Phase 2 live + 3 newly-activated).
    - `pnpm tsx -e` one-liner above prints 3 js-render firms each with wait_for value.
    - YAML parses clean (no indentation errors).
  </acceptance_criteria>
  <done>
Three previously-disabled placeholders are now live js-render firms with verified wait_for selectors from plan 07. Schema validation passes (js-render + wait_for combination satisfies superRefine).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add a brand-new barun block</name>
  <files>config/firms.yaml</files>
  <read_first>
    - config/firms.yaml (post Task 1 — find the insertion point)
    - .planning/phases/04-js-rendered-tier-conditional/04-07-PROBE-RESULTS.md (barun URL, wait_for, selectors)
  </read_first>
  <action>
Edit `config/firms.yaml`:

**Insert a new block for `barun`** into the `firms:` list. Choose the location for logical grouping: since the three js-render flips (lee-ko, yoon-yang, latham) cluster at the bottom, add the barun block immediately after `yoon-yang` so Korean js-render firms stay together and latham remains the US js-render firm at the end.

Template (replace the bracketed placeholders with values from PROBE-RESULTS.md):

```
  # ----- KR js-render firm (NEW IN PHASE 4, no Phase 2 placeholder) -----
  - id: barun
    name: 바른
    language: ko
    type: js-render
    url: "[VERIFIED URL FROM PROBE-RESULTS.md]"
    wait_for: "[VERIFIED SELECTOR FROM PROBE-RESULTS.md]"
    timezone: Asia/Seoul
    enabled: true
    selectors:
      list_item: "[VERIFIED FROM PROBE-RESULTS.md]"
      title: "[VERIFIED FROM PROBE-RESULTS.md]"
      link: "[VERIFIED FROM PROBE-RESULTS.md]"
```

**Paste the probe-verified values verbatim** — do NOT paraphrase. If plan 07 revealed barun needs `link_onclick_regex` + `link_template` instead of plain `link` (like Phase 2 kim-chang / bkl firms do), use that pair instead and drop the plain `link`. The schema refine on selectors accepts either shape.

After insertion, the firms list order is:
1. cooley (rss)
2. clifford-chance (rss)
3. freshfields (rss)
4. shin-kim (html plain-href)
5. yulchon (html plain-href)
6. logos (html plain-href)
7. skadden (html plain-href, monthly)
8. kim-chang (html onclick)
9. bkl (html onclick)
10. lee-ko (js-render)
11. yoon-yang (js-render)
12. barun (js-render, NEW)
13. latham (js-render)
  </action>
  <verify>
    <automated>pnpm tsx -e "const m = await import('./src/config/loader.js'); const f = await m.loadFirms(); const ids = f.map(x => x.id).sort().join(','); const expected = 'barun,bkl,clifford-chance,cooley,freshfields,kim-chang,latham,lee-ko,logos,shin-kim,skadden,yoon-yang,yulchon'; if (ids !== expected) { console.error('MISMATCH'); console.error('  expected:', expected); console.error('  got:     ', ids); process.exit(1); } console.log('OK — 13 firms:', ids);"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  - id: barun$" config/firms.yaml` returns exactly 1.
    - `grep -c "type: js-render" config/firms.yaml` returns exactly 4 (lee-ko + yoon-yang + barun + latham).
    - `grep -c "^    wait_for:" config/firms.yaml` returns exactly 4.
    - `grep -c "enabled: true" config/firms.yaml` returns exactly 13.
    - `grep -c "enabled: false" config/firms.yaml` returns 0.
    - The `pnpm tsx -e` one-liner above prints `OK — 13 firms:` followed by the expected sorted id list and exits 0.
    - YAML parses clean (no indentation errors).
  </acceptance_criteria>
  <done>
barun block exists with probe-verified values, all 13 firms enabled, total js-render count = 4. Schema validation still passes (all four js-render firms satisfy the wait_for superRefine).
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-to-do>
Run the end-to-end smoke tests. Playwright binary must be installed locally before running these:

```bash
pnpm exec playwright install chromium --only-shell
```

Then execute each of the four `check:firm` runs and the full `DRY_RUN=1` pipeline:

```bash
# Per-firm smoke tests
pnpm check:firm lee-ko
pnpm check:firm yoon-yang
pnpm check:firm barun
pnpm check:firm latham

# Full pipeline dry-run
DRY_RUN=1 pnpm tsx src/main.ts
```

**Expected result for each check:firm:**
- stage output shows `target`, `fetch`, `enrich`, `filter`, `dedup`, `would-summarize`, `compose` (no sendMail, no state write).
- `fetch` line shows `<firm>: N items (<duration>ms)` with N >= 1.
- Exit code 0.

**Expected result for the DRY_RUN run:**
- All 13 firms fetch successfully OR carry a `FirmResult.error` that appears in the email footer (no silent failures).
- `Pipeline` exits 0 OR exits 1 ONLY if a js-render firm failed (D-08 fail-loud).
- No state/seen.json mutation (DRY_RUN containment).
- stdout contains "would send mail" AND "would write state" markers from the existing DRY_RUN branches in mailer/gmail.ts and state/writer.ts.

**Paste the full stdout from each of the 5 invocations** into `.planning/phases/04-js-rendered-tier-conditional/04-08-SMOKE-TEST.md` (create the file) with one subsection per invocation.

**Blocker conditions:**
- If ANY of the four `check:firm` runs exits non-zero → halt, surface to user. Plan 07 probe-verified selectors were supposed to guarantee success; if one fails now, re-probe or narrow `list_item` selector.
- If the DRY_RUN full pipeline throws a composition-root error (not a per-firm error) → halt.
- If exit 1 after a green email-would-send + state-would-write pair because a js-render firm errored — that is ACCEPTABLE per D-08 fail-loud design. Document which firm failed in the SMOKE-TEST.md and note whether it's a selector drift issue or a transient network issue.
  </what-to-do>
  <resume-signal>Type "approved" once SMOKE-TEST.md is written and all 4 check:firm runs succeeded (or describe any firm that failed and whether to proceed anyway).</resume-signal>
</task>

</tasks>

<verification>
- [ ] `pnpm tsx -e` one-liner for firm id list returns OK with all 13 firms.
- [ ] `grep -c "type: js-render" config/firms.yaml` returns exactly 4.
- [ ] `grep -c "^    wait_for:" config/firms.yaml` returns exactly 4.
- [ ] `grep -c "enabled: true" config/firms.yaml` returns exactly 13.
- [ ] `.planning/phases/04-js-rendered-tier-conditional/04-08-SMOKE-TEST.md` exists with transcripts from 5 invocations.
- [ ] All 4 check:firm invocations exit 0.
- [ ] DRY_RUN full pipeline exits 0 OR exit 1 with clear js-render-specific failure explanation.
</verification>

<success_criteria>
- All 4 js-render firms are live in config/firms.yaml with probe-verified selectors.
- check:firm succeeds per firm (N >= 1 items extracted).
- Full DRY_RUN pipeline runs clean end-to-end.
- Phase 4 goal achieved: FETCH-01 tiered-strategy contract is COMPLETE — RSS → HTML → JS-render all three paths live.
</success_criteria>

<output>
After completion, create `.planning/phases/04-js-rendered-tier-conditional/04-08-SUMMARY.md` capturing:
- Final firm count (13 live).
- Per-firm item count observed in the first successful check:firm run.
- DRY_RUN full-pipeline stdout key markers (would-send, would-write, exit code).
- Confirmation that state/seen.json was NOT mutated during smoke tests.
- Any firm that required a selector adjustment from the PROBE-RESULTS.md values.
</output>
