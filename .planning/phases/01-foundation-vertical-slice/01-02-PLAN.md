---
phase: 01-foundation-vertical-slice
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/firms.yaml
  - .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-05
  - CONF-07
tags: [config, probe, firms]
must_haves:
  truths:
    - "Cooley RSS endpoint URL has been empirically confirmed and recorded in config/firms.yaml"
    - "config/firms.yaml contains exactly one firm entry (Cooley) with all required schema fields"
    - "config/firms.yaml top comment block explains every field in Korean for non-dev editors (CONF-07)"
    - "01-02-SUMMARY.md is created during Task 1 (before Task 2 runs) and records the selected URL + probe evidence (W4)"
  artifacts:
    - path: config/firms.yaml
      provides: "single Cooley firm entry — id, name, language, type, url (confirmed), timezone, enabled + non-dev comment template"
      contains: "id: cooley"
    - path: .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md
      provides: "record of which Cooley RSS endpoint was selected and the probe evidence (HTTP 200, feed item count, robots.txt excerpt) — written by Task 1 so Task 2's grep verify has something to match"
      contains: "Selected URL"
  key_links:
    - from: config/firms.yaml
      to: src/scrapers/rss.ts
      via: "firm.url → feedparser stream input"
      pattern: "url: https://.*cooley"
    - from: Task 1 probe output
      to: .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md
      via: "Task 1 writes probe findings into SUMMARY at end of action"
      pattern: "Selected URL"
---

<objective>
D-02 잠금결정 해제: Cooley의 실제 RSS 엔드포인트를 라이브 probe로 확정한 뒤, Phase 1 전용 `config/firms.yaml`을 작성한다. URL이 확정되지 않으면 downstream scraper 작업(plan 05)은 시작조차 할 수 없다. 이 plan의 Task 1(probe)은 phase 전체에서 가장 먼저 실행되어야 한다. **W4 revision (2026-04-17):** Task 1 끝에서 probe 결과를 `01-02-SUMMARY.md` 에 기록하는 단계를 명시. 이전에는 Task 2 의 grep verify 가 아직 존재하지 않는 SUMMARY 를 조회하는 미묘한 hand-off 순서 문제가 있었음.

Purpose: CONF-01/02/05/07 요구사항을 충족하면서 Cooley 스크래핑의 ground truth URL을 확정한다. 동시에 Phase 2에서 비개발자가 나머지 11개 로펌을 같은 파일에 얹을 수 있도록 주석 템플릿을 설치한다.
Output: 실제 동작하는 Cooley RSS URL을 담은 `config/firms.yaml` + probe 결과 증거 SUMMARY (Task 1 기준).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-vertical-slice/01-CONTEXT.md
@.planning/phases/01-foundation-vertical-slice/01-RESEARCH.md
@.planning/phases/01-foundation-vertical-slice/01-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Canonical shape to copy. Source: PATTERNS.md L955–978 + RESEARCH.md §Standard Stack. -->

config/firms.yaml (comment header + single entry):
```yaml
# =============================================================
# 추적 대상 로펌 목록 (LegalNewsletter)
# -------------------------------------------------------------
# 로펌을 추가하려면 아래 firms: 리스트에 블록을 하나 더 추가하세요.
# 각 필드 설명:
#   id          : 영문 소문자 slug. 변경 금지 (state 저장 key)
#   name        : 이메일에 표시될 사람-읽기용 이름
#   language    : 원문 언어. 'ko' 또는 'en'
#   type        : 수집 방식. Phase 1 은 'rss' 만 지원 (Phase 2 에서 'html' 추가)
#   url         : RSS 피드 절대 URL
#   timezone    : 사이트 표준 IANA 시간대 (예: America/Los_Angeles, Asia/Seoul)
#   enabled     : false 로 두면 일시적으로 비활성화 (파일 삭제 불필요)
# =============================================================

firms:
  - id: cooley
    name: Cooley
    language: en
    type: rss
    url: <CONFIRMED_URL_FROM_TASK_1>
    timezone: America/Los_Angeles
    enabled: true
```

Candidate URLs (D-02 enumerates these; probe MUST try them in order):
- https://cooley.com/feed
- https://cooley.com/rss
- https://cooley.com/alerts/feed
- https://cooley.com/insights/feed
- https://cooley.com/feed.xml
- https://www.cooley.com/news/rss  (some firms use /news/rss)

Honest User-Agent (FETCH-04, RESEARCH.md §Shared Patterns L1202):
```
LegalNewsletterBot/1.0 (+https://github.com/shlee87/law-firm-digest)
```

SUMMARY.md minimal shape (W4 — Task 1 writes this; Task 2 greps for `Selected URL`):
```markdown
# Plan 01-02 Summary — Cooley RSS Probe

## Cooley RSS Probe Result

- **Selected URL:** https://example-cooley-rss-url
- **HTTP status:** 200
- **Content-type:** application/rss+xml
- **Item count (grep `<item>`):** 12
- **robots.txt status:** allow (no `Disallow:` under `User-agent: *` covers this path)
- **Probed at:** 2026-04-17T09:15:00Z

## Probe Evidence

(curl output excerpts, first 3 item titles, robots.txt snippet, caveats)

## Downstream Hand-off

- Task 2 copies the Selected URL into `config/firms.yaml` (cooley entry `url:`).
- Task 3 (user checkpoint) confirms the URL before wave 2 unblocks.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Probe Cooley RSS endpoint, confirm working URL, and write probe SUMMARY (W4) (FIRST TASK OF PHASE)</name>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice/01-CONTEXT.md §decisions D-01 D-02 (seed firm and probe policy)
    - .planning/phases/01-foundation-vertical-slice/01-RESEARCH.md §Pitfall 9 (L723–737, honest UA + robots.txt requirement)
    - .planning/phases/01-foundation-vertical-slice/01-PATTERNS.md §Shared Patterns Honest User-Agent (L1200–1208)
  </read_first>
  <files>.planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md</files>
  <action>
    This is a PROBE + record task. Use `curl` (or equivalent) with the honest UA, visit each candidate URL in order, record which returns (a) HTTP 200 AND (b) valid RSS/Atom XML AND (c) at least one `<item>` or `<entry>` element, then write the findings into SUMMARY so Task 2 + Task 3 can consume them.

    Exact commands to run (bash):
    ```bash
    UA='LegalNewsletterBot/1.0 (+https://github.com/shlee87/law-firm-digest)'
    for U in \
      "https://cooley.com/feed" \
      "https://cooley.com/rss" \
      "https://cooley.com/alerts/feed" \
      "https://cooley.com/insights/feed" \
      "https://cooley.com/feed.xml" \
      "https://www.cooley.com/news/rss"; do
        echo "=== $U ==="
        curl -sS -L -A "$UA" -o /tmp/cooley_probe.xml -w "HTTP %{http_code}\nContent-Type: %{content_type}\nSize: %{size_download}\n" "$U" || true
        head -c 800 /tmp/cooley_probe.xml
        echo -e "\n"
    done
    ```
    Also probe robots.txt:
    ```bash
    curl -sS -A "$UA" https://cooley.com/robots.txt | head -60
    ```

    Selection rule (strict):
    1. Pick the FIRST candidate URL that returns HTTP 200, a content-type containing `xml` or `rss` or `atom`, and a body starting with `<?xml` or `<rss` or `<feed`.
    2. Count `<item>` or `<entry>` occurrences; MUST be ≥ 1.
    3. Verify robots.txt does not `Disallow: /<path_of_feed>` for `User-agent: *` (COMP-03 Pitfall #9).
    4. If NO candidate satisfies (1)+(2), STOP and escalate: record findings in SUMMARY with disposition "requires Phase 2 HTML tier for Cooley — seed firm must be re-selected OR Cooley deferred". Do NOT proceed to Task 2 until a firm with a working RSS is chosen (user checkpoint task 3 handles this).

    **W4 — Write SUMMARY at the end of this task.** After probe completes, confirm the selected URL, then CREATE `.planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md` containing at minimum:
    ```
    # Plan 01-02 Summary — Cooley RSS Probe

    ## Cooley RSS Probe Result

    - **Selected URL:** <url>
    - **HTTP status:** <status>
    - **Content-type:** <ct>
    - **Item count (grep `<item>`):** <n>
    - **robots.txt status:** <allow/deny + excerpt>
    - **Probed at:** <ISO timestamp>

    ## Probe Evidence

    - First 3 item titles: <titles>
    - robots.txt snippet (User-agent: * block): <excerpt>
    - Notes / caveats: <any>

    ## Downstream Hand-off

    - Task 2 copies the Selected URL into `config/firms.yaml` (cooley entry `url:`).
    - Task 3 (user checkpoint) confirms the URL before wave 2 unblocks.
    ```
    If all candidates fail, write a SUMMARY that clearly says so (with the failure disposition) — Task 3 user checkpoint will handle the escalation.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md &amp;&amp; grep -q "Selected URL" .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - At least one candidate URL returned HTTP 200 with XML content-type (OR a clear failure disposition is recorded in SUMMARY)
    - Selected URL body contains at least one `<item>` or `<entry>` element (OR failure disposition)
    - robots.txt for `cooley.com` was fetched and inspected for `Disallow` rules that would block the selected path
    - **W4**: `01-02-SUMMARY.md` exists on disk at the end of this task
    - **W4**: SUMMARY contains the literal string `Selected URL`
    - SUMMARY contains the chosen URL (even in the failure branch it records what was tried and rejected)
    - If probe failed for all candidates, a clear escalation note exists in SUMMARY recommending a different seed firm — in this branch Task 2 is blocked until Task 3 (checkpoint) resolves
  </acceptance_criteria>
  <done>Cooley RSS URL empirically confirmed AND recorded in SUMMARY (or failure clearly escalated with evidence in SUMMARY).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write config/firms.yaml with confirmed URL + non-dev comment header</name>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md (from Task 1 — read the Selected URL + robots.txt findings)
    - .planning/phases/01-foundation-vertical-slice/01-PATTERNS.md §config/firms.yaml (L948–983)
    - .planning/phases/01-foundation-vertical-slice/01-CONTEXT.md §specifics (L103–110) — non-dev comment template requirement
  </read_first>
  <files>config/firms.yaml</files>
  <action>
    Create `config/firms.yaml` by copying the template from the `<interfaces>` block above, substituting `<CONFIRMED_URL_FROM_TASK_1>` with the empirically confirmed Cooley RSS URL read from `01-02-SUMMARY.md`'s `Selected URL` line.

    Concrete required fields on the Cooley entry (matches zod FirmSchema from RESEARCH.md L913–929 — plan 03 implements the schema):
    - `id: cooley` (lowercase slug, will be used as state key — MUST NOT change later)
    - `name: Cooley`
    - `language: en`
    - `type: rss`
    - `url: <confirmed from Task 1 SUMMARY>` (must be a valid absolute URL)
    - `timezone: America/Los_Angeles` (Cooley is SF-based; this is the IANA zone required by CONF-05)
    - `enabled: true`

    The comment header MUST:
    - Be in Korean (CONF-07 비개발자 가이드)
    - Document each field (id, name, language, type, url, timezone, enabled)
    - Explicitly note that Phase 1 supports only `type: rss` (Phase 2 adds `html`, Phase 4 conditionally adds `js-render`)
    - Be at the very top of the file (before `firms:` block)

    Do NOT add `selectors:`, `user_agent:`, or `timeout_ms:` fields — Cooley uses RSS, none are needed (schema optional).
  </action>
  <verify>
    <automated>test -f config/firms.yaml &amp;&amp; grep -q "id: cooley" config/firms.yaml &amp;&amp; grep -q "type: rss" config/firms.yaml &amp;&amp; grep -q "timezone: America/Los_Angeles" config/firms.yaml &amp;&amp; grep -q "enabled: true" config/firms.yaml &amp;&amp; grep -qE "url: https?://[a-zA-Z0-9./-]*cooley" config/firms.yaml &amp;&amp; grep -qE "^#.*로펌" config/firms.yaml &amp;&amp; grep -q "Selected URL" .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `config/firms.yaml`
    - Contains literal `id: cooley`
    - Contains literal `type: rss`
    - Contains literal `timezone: America/Los_Angeles`
    - Contains literal `enabled: true`
    - Contains a `url:` line whose value matches Task 1's confirmed URL (regex `url: https?://[a-zA-Z0-9./-]*cooley`)
    - File begins with a `#` comment block containing the Korean strings `id`, `name`, `language`, `type`, `url`, `timezone`, `enabled` (one-line-per-field explanation, CONF-07)
    - File contains string `로펌` somewhere in the header (Korean context marker for CONF-07 acceptance)
    - Does NOT contain selectors or user_agent or timeout_ms fields (unneeded for RSS)
    - **W4 cross-task gate**: `01-02-SUMMARY.md` exists with `Selected URL` (left by Task 1)
  </acceptance_criteria>
  <done>config/firms.yaml is ready for plan 03's zod loader to parse without modification and for plan 05's RSS scraper to hit a live endpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User confirms Cooley probe result before the seed firm is locked</name>
  <files>config/firms.yaml, .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md</files>
  <what-built>
    - `config/firms.yaml` with empirically confirmed Cooley RSS URL
    - Probe evidence recorded in SUMMARY (HTTP status, item count, robots.txt excerpt) — written during Task 1 (W4)
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md` — note the selected URL, item count, and robots.txt summary
    2. Open the URL in a browser: does the RSS/XML render? Do the titles look like Cooley newsletter alerts?
    3. Open `config/firms.yaml`: does the entry look correct?
    4. Branch selection:
       - (A) URL works, Cooley has at least a few items with reasonable dates → approve, Phase 1 continues with Cooley.
       - (B) URL returns 200 but the feed looks stale / empty / wrong content → discuss seed firm alternative (user picks another English RSS-supporting firm, executor re-runs Task 1 with a new candidate list, updates firms.yaml).
       - (C) No candidate worked — ALL URL probes failed → escalate to user: options are (i) try `/feed.rss`, `/atom.xml`, `/blog/feed`, etc., (ii) pick a different seed firm, (iii) defer Cooley to Phase 2 HTML tier and use Latham (US) or Cooley's LinkedIn for Phase 1 seed.
  </how-to-verify>
  <action>Pause execution. Present the plan-02 SUMMARY and the selected Cooley URL to the user. Await approval before any wave-2 plan executes. This task is a pure user gate — no code changes, no file edits beyond what tasks 1+2 produced.</action>
  <verify>
    <automated>grep -qE "url: https?://[a-zA-Z0-9./-]*cooley" config/firms.yaml &amp;&amp; test -f .planning/phases/01-foundation-vertical-slice/01-02-SUMMARY.md</automated>
  </verify>
  <done>User has typed `approved` (or described a blocker). Phase 1 downstream scraper work is unblocked iff approval was received.</done>
  <resume-signal>
    Type `approved` to proceed (URL locked, downstream scrapers may now be coded). If not approved, describe the issue or name a replacement firm; Task 1+2 will re-run.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config file ↔ scraper code | User-editable YAML → executed HTTP request (potential SSRF, path traversal) |
| repo ↔ public firm site | Scraper identifies as `LegalNewsletterBot/1.0`; firm may honor, ignore, or block |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (SSRF) | `firm.url` in `config/firms.yaml` | mitigate | zod `z.string().url()` constraint (plan 03 schema) blocks non-URL strings. Plan 05 `robots.ts` parses `new URL(firm.url).origin` — malformed host will throw at parse time before fetch. Phase 1 URL is hand-set by developer, not user input. |
| T-02-02 | Information Disclosure | Probe User-Agent | accept | Honest UA `LegalNewsletterBot/1.0 (+https://github.com/shlee87/law-firm-digest)` is the compliant choice per Pitfall #9; reveals repo URL intentionally (takedown channel). |
| T-02-03 | Repudiation | robots.txt disregard | mitigate | Task 1 explicitly probes `/robots.txt` and records findings in SUMMARY; Task 3 checkpoint blocks progression if Disallow rules conflict. Plan 05 enforces at runtime. |
| T-02-04 | Denial of Service | aggressive probe | accept | Task 1 makes ~7 `curl` requests total to a single domain, far below any rate limit. Subsequent plans use `p-limit(3)` and 1 req/firm/day (FETCH-03). |
</threat_model>

<verification>
- `config/firms.yaml` contains a real, reachable Cooley RSS URL (not the `<CONFIRMED_URL_FROM_TASK_1>` placeholder)
- Comment header is Korean, CONF-07 compliant
- User has approved the selected URL via Task 3 checkpoint
- **W4**: SUMMARY captures probe evidence for audit trail AND is created by Task 1 (not by a separate "write output" step), so Task 2's grep sees a real file
</verification>

<success_criteria>
- CONF-01: Single YAML config for firms established
- CONF-02: File shape matches zod schema (validated by plan 03)
- CONF-05: `timezone` field present with IANA value
- CONF-07: Non-dev-friendly Korean comment template landed — Phase 2 developers (non-technical user) can follow it to add the remaining 11 firms
- W4: SUMMARY.md is produced inline with Task 1, closing the probe → record → consume loop on a single task
</success_criteria>

<output>
The SUMMARY is written DURING Task 1 (W4), not after the whole plan finishes. Task 3 just references it. The final SUMMARY body (recorded in Task 1) must contain:
  - Exact Cooley RSS URL selected
  - HTTP status code of probe
  - Approximate item count (from `grep -c '<item>' /tmp/cooley_probe.xml`)
  - First 3 item titles (as evidence the feed is live + relevant)
  - robots.txt relevant excerpt (whether Disallow rules affect this path)
  - Any caveats (e.g., "feed is 30 days old", "content-type is application/atom+xml not rss+xml")
</output>
