---
phase: 01-foundation-vertical-slice
plan: 09
type: execute
wave: 2
depends_on: [01, 03]
files_modified:
  - src/mailer/gmail.ts
autonomous: true
requirements:
  - EMAIL-03
  - EMAIL-06
  - OPS-06
tags: [mailer, gmail, smtp, fail-loud, dry-run]
must_haves:
  truths:
    - "sendMail(payload) uses nodemailer with service 'Gmail' + App Password"
    - "sendMail(payload) short-circuits when isDryRun() is true (check site #1 of 2)"
    - "sendMail(payload) THROWS on ANY SMTP error — never swallowed"
    - "On 535 auth failure, logs the literal GMAIL_AUTH_FAILURE marker plus App Password regenerate URL before throwing"
    - "The only catch block in the file is the 535-detecting one that rethrows; no additional catch(…) swallows errors (W2 fail-loud guarantee)"
  artifacts:
    - path: src/mailer/gmail.ts
      provides: "sendMail(payload): Promise<void> — DRY_RUN-aware, fail-loud, 535-detecting"
      contains: "GMAIL_AUTH_FAILURE"
  key_links:
    - from: src/main.ts (plan 11)
      to: src/mailer/gmail.ts
      via: "await sendMail(payload); errors bubble to top-level try/catch → exit 1"
      pattern: "sendMail"
---

<objective>
Gmail SMTP 발송 경계를 한 파일에 가둔다. DRY_RUN check site #1 (OPS-06), fail-loud 에러 전파 (EMAIL-06, Pitfall 6), nodemailer App Password (EMAIL-03), 그리고 535 명시적 감지 + 복구 URL 로그 (Pitfall 7/14)를 모두 포함한다. **W1 revision (2026-04-17):** wave 3 → 2. depends_on 이 [01, 03] 뿐이라 04/06/07/08 과 parallel 가능. **W2 revision:** acceptance-criteria 에 "catch 블럭은 정확히 하나 (535 감지 후 rethrow)" 를 추가해 silent-swallow regression 을 방지한다.

Purpose: SMTP 실패가 조용히 삼켜지는 것 (Pitfall 6)이 이 프로젝트의 가장 silent rot-prone 지점. 이 plan은 throw-on-any-error 계약을 코드 + 타입 검사 + grep 기반 acceptance 로 박아둔다. 별도 유닛 테스트 파일 없음 — Gmail SMTP 를 mock 으로 유의미하게 테스트하려면 실제 transporter 가드 레이어가 필요한데 Phase 1 budget 에 맞지 않음. 대신 grep acceptance 로 "catch 가 하나뿐이고 rethrow 한다" 를 static 하게 검증.
Output: 1개 파일, `src/mailer/gmail.ts`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-vertical-slice/01-RESEARCH.md
@.planning/phases/01-foundation-vertical-slice/01-PATTERNS.md
@src/types.ts
@src/env.ts
@src/util/logging.ts

<interfaces>
Canonical shape (PATTERNS.md §src/mailer/gmail.ts L693-733, RESEARCH.md §Code Examples L862-902).

src/mailer/gmail.ts:

```typescript
import nodemailer from 'nodemailer';
import { isDryRun } from '../env.js';
import { scrubSecrets } from '../util/logging.js';
import type { EmailPayload } from '../types.js';

export async function sendMail(payload: EmailPayload): Promise<void> {
  if (isDryRun()) {
    console.log('[DRY_RUN] Subject:', payload.subject);
    console.log('[DRY_RUN] HTML body:\n', payload.html);
    return;
  }

  const transporter = nodemailer.createTransport({
    service: 'Gmail',
    auth: {
      user: payload.from,
      pass: process.env.GMAIL_APP_PASSWORD,
    },
  });

  try {
    await transporter.sendMail({
      from: payload.from,
      to: payload.to,
      subject: payload.subject,
      html: payload.html,
    });
  } catch (err) {
    const anyErr = err as { responseCode?: number; response?: string; message?: string };
    if (
      anyErr.responseCode === 535 ||
      (typeof anyErr.response === 'string' && anyErr.response.includes('535'))
    ) {
      console.error('GMAIL_AUTH_FAILURE: App Password invalid or revoked.');
      console.error(
        'Regenerate at https://myaccount.google.com/apppasswords and update GMAIL_APP_PASSWORD secret.',
      );
    }
    throw new Error(`SMTP send failed: ${scrubSecrets(anyErr.message ?? String(err))}`);
  }
}
```

A4 note (PATTERNS.md L737-738): nodemailer 8.x may expose the SMTP response code under `responseCode` OR `code`; the defensive check covers both paths via `responseCode === 535 || response.includes('535')`. If Context7 `/nodemailer/nodemailer` shows a different field name in 8.0.5, update accordingly and document in SUMMARY.

D-08 reminder: no plaintext multipart. `transporter.sendMail({ html })` only; no `text` field.

Import `isDryRun` from '../env.js' — this file is DRY_RUN check site #1 of 2 (plan 10 state writer is #2). No other file in this phase may import `isDryRun`.

**W2 fail-loud invariant:** this file has EXACTLY ONE `catch (…)` block. That catch (a) logs the 535 recovery marker if applicable and (b) immediately `throw new Error(...)` — no silent recovery path. Any additional catch blocks would be a regression.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/mailer/gmail.ts with DRY_RUN gate + fail-loud + 535 detection</name>
  <read_first>
    - .planning/phases/01-foundation-vertical-slice/01-PATTERNS.md §src/mailer/gmail.ts (L688-739) including A4 note
    - .planning/phases/01-foundation-vertical-slice/01-RESEARCH.md §Pitfall 6 (L697-701) — fail-loud SMTP
    - .planning/phases/01-foundation-vertical-slice/01-RESEARCH.md §Pitfall 7 (L703-710) — 535 + App Password regen
    - .planning/phases/01-foundation-vertical-slice/01-RESEARCH.md §Pattern 2 (L442-464) — DRY_RUN check site policy
    - src/env.ts (isDryRun import source)
    - src/util/logging.ts (scrubSecrets import source)
    - src/types.ts (EmailPayload shape)
    - Optionally via Context7: `npx --yes ctx7@latest docs /nodemailer/nodemailer "createTransport Gmail service App Password"` to verify nodemailer 8.x API still supports `service: 'Gmail'` shortcut
  </read_first>
  <files>src/mailer/gmail.ts</files>
  <action>
    Create `src/mailer/gmail.ts` by copying the code block from `<interfaces>` verbatim.

    Implementation invariants (acceptance-testable):
    1. `export async function sendMail(payload: EmailPayload): Promise<void>` signature.
    2. **First thing**: `if (isDryRun()) { console.log('[DRY_RUN] Subject:', payload.subject); console.log('[DRY_RUN] HTML body:\\n', payload.html); return; }` — short-circuit BEFORE creating transporter (no SMTP connection, no auth use, no network call).
    3. `nodemailer.createTransport({ service: 'Gmail', auth: { user: payload.from, pass: process.env.GMAIL_APP_PASSWORD } })`.
    4. `transporter.sendMail({ from, to, subject, html })` — no `text`, no `attachments`.
    5. try/catch wrapper:
       - On any SMTP error → `throw new Error('SMTP send failed: ...')` AFTER logging.
       - Before the throw: check if `err.responseCode === 535` OR `err.response?.includes('535')` → emit exactly these two console.error lines:
         - `GMAIL_AUTH_FAILURE: App Password invalid or revoked.`
         - `Regenerate at https://myaccount.google.com/apppasswords and update GMAIL_APP_PASSWORD secret.`
       - Error message passed to scrubSecrets to prevent leaking GMAIL_APP_PASSWORD if it's embedded in the error.

    Critical: this function MUST throw. Under no circumstances should a `try/catch` swallow the error and return normally — that is the exact Pitfall 6 anti-pattern. The test for "this throws" is written in plan 11 main.ts (top-level catch sets exit code 1).

    **W2 invariant**: this file contains exactly ONE `catch (…)` block. Any future addition of a silent-recovery catch would be a Pitfall 6 regression. The grep acceptance below enforces this at plan-check time.

    Do NOT add retry in this plan. Phase 2 (EMAIL-07) will add p-retry for transient 5xx. Phase 1 fails immediately — loud is better than quiet.
  </action>
  <verify>
    <automated>pnpm typecheck &amp;&amp; grep -q "sendMail" src/mailer/gmail.ts &amp;&amp; grep -q "isDryRun()" src/mailer/gmail.ts &amp;&amp; grep -q "service: 'Gmail'" src/mailer/gmail.ts &amp;&amp; grep -q "GMAIL_APP_PASSWORD" src/mailer/gmail.ts &amp;&amp; grep -q "GMAIL_AUTH_FAILURE" src/mailer/gmail.ts &amp;&amp; grep -q "myaccount.google.com/apppasswords" src/mailer/gmail.ts &amp;&amp; grep -q "scrubSecrets" src/mailer/gmail.ts &amp;&amp; grep -q "throw new Error" src/mailer/gmail.ts &amp;&amp; grep -q "responseCode === 535" src/mailer/gmail.ts &amp;&amp; [ "$(grep -cE '^[[:space:]]*\} catch \(' src/mailer/gmail.ts)" = "1" -o "$(grep -cE 'catch \(' src/mailer/gmail.ts)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - File exists; exports `sendMail`
    - First executable statement is the DRY_RUN gate (verified by grep — `isDryRun()` appears before `createTransport`)
    - Uses `service: 'Gmail'` nodemailer shortcut (EMAIL-03)
    - Pass value is `process.env.GMAIL_APP_PASSWORD` (App Password, not account password)
    - Catch block contains explicit `responseCode === 535` OR `response.includes('535')` check
    - When 535 detected: emits literal `GMAIL_AUTH_FAILURE` marker string (Pitfall 7 + 14)
    - When 535 detected: emits the exact URL `https://myaccount.google.com/apppasswords`
    - Every catch path ends in `throw new Error('SMTP send failed: ...')` — fail-loud (EMAIL-06)
    - Error message passed through `scrubSecrets`
    - Does NOT contain `text:` field in sendMail call (D-08 no plaintext)
    - Does NOT contain any `return null`, `return undefined`, or silent recovery in catch
    - **W2 fail-loud invariant**: file contains EXACTLY ONE `catch (…)` block (`grep -cE 'catch \(' src/mailer/gmail.ts` returns `1`). Additional catches would indicate a silent-swallow regression.
    - `pnpm typecheck` exits 0
    - File is EXACTLY one of the two authorized DRY_RUN check sites (plan 10 is the other) — verified by cross-plan grep: `grep -rn "isDryRun" src/ | wc -l` should be 3 after plan 09 + 10 land (1 definition in env.ts + 2 imports = 3)
  </acceptance_criteria>
  <done>Mailer is DRY_RUN-aware, fail-loud, and surfaces Gmail auth issues distinctly for operator recovery. Catch-swallow regression blocked by W2 grep gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GMAIL_APP_PASSWORD env → nodemailer auth | Secret transported to SMTP library |
| EmailPayload.to → remote Gmail server | Recipient address treated as trusted (self-send) |
| SMTP error object → console.error log | Error messages may contain secret traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | GMAIL_APP_PASSWORD in error log | mitigate | `scrubSecrets` applied to `err.message ?? String(err)` before throw. Every log line touching error content is scrubbed. |
| T-09-02 | Spoofing | App Password revoked, workflow runs green | mitigate | EMAIL-06 fail-loud: catch branch ALWAYS throws; top-level main.ts catch sets exit 1; GHA shows red X. 535 explicitly surfaced with `GMAIL_AUTH_FAILURE` marker so operator can grep logs. W2 grep gate enforces single-catch-rethrow structure. |
| T-09-03 | Tampering | Malicious payload.html injecting client-side exploit | mitigate | Plan 08 composer escapes all user content via escapeHtml/escapeAttr. This mailer is a transport layer; it trusts payload.html to already be safe. The only producer of payload.html is `composeDigest`. |
| T-09-04 | Repudiation | DRY_RUN bypass bug sends real email | mitigate | Pattern 2 enforces single `isDryRun()` site in this file; plan 03 puts the predicate in `src/env.ts` as the single source. Scattered DRY_RUN checks are forbidden (see plan 03 verification grep: `grep -rn "DRY_RUN" src/` should only match env.ts + mailer/gmail.ts + state/writer.ts). |
| T-09-05 | Information Disclosure | recipient/from email in logs | accept | Email addresses are semi-public; recipient is already in `config/recipient.yaml` (committed) per D-03. |
| T-09-06 | Denial of Service | Slow SMTP hanging the run | accept | nodemailer has default internal timeout; combined with GHA 6h runner limit, DoS is out of scope. If observed, Phase 2 retry plan (EMAIL-07) adds p-retry with a cap. |
</threat_model>

<verification>
- `pnpm typecheck` clean
- Grep verification: `isDryRun()` is the very first condition checked in the function body (before any transporter creation)
- Grep verification: every `catch` block contains `throw`
- Grep verification: exactly ONE `catch (…)` block exists in the file (W2)
- Cross-plan invariant: `grep -rn "isDryRun" src/` total count equals 3 after plans 03 + 09 + 10 all exist
</verification>

<success_criteria>
- EMAIL-03 Gmail SMTP via nodemailer + App Password
- EMAIL-06 SMTP failure propagates (workflow goes red)
- OPS-06 DRY_RUN check site #1 (mailer prints, doesn't send)
- Pitfall 7/14 operator-friendly 535 recovery marker present
- W2 single-catch fail-loud invariant enforced
</success_criteria>

<output>
Create `.planning/phases/01-foundation-vertical-slice/01-09-SUMMARY.md` recording:
- nodemailer version resolved by pnpm install
- Context7 verification of `service: 'Gmail'` shortcut still supported in 8.x (or not)
- Confirmation that `grep -rn "isDryRun" src/` returns exactly 3 matches post-plan-10 (env.ts definition + mailer import + state writer import)
- Confirmation that `grep -cE 'catch \(' src/mailer/gmail.ts` returns 1 (W2)
</output>
