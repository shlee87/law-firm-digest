---
phase: 17
plan: "02"
wave: 1
depends_on: []
files_modified:
  - src/summarize/gemini.ts
  - test/summarize/gemini.test.ts
autonomous: true
requirements: [FAIL-UX-01]
---

# Plan 17-02: onFailedAttempt retryDelay honor — 429 burst 자동 회복

<objective>
`src/summarize/gemini.ts`의 `onFailedAttempt` 콜백(현재 line 166-172)에서 429 응답의 `retryDelay` 필드를 파싱해서 다음 재시도 전에 그 만큼 대기한다. 2026-05-27 production failure(`run 26512695491`)의 `retryDelay: "39.347610487s"` 같은 값을 honor해서 p-retry의 default backoff schedule보다 길게 대기 → flash + flash-lite가 같은 quota pool을 공유하더라도 다음 RPM window까지 기다리고 재시도 성공한다 (D-06). `test/summarize/gemini.test.ts`에 retryDelay honor 회귀 테스트 추가. SPEC.md 요구사항 5와 Acceptance(옵션 (a)/(b)) 만족.
</objective>

<must_haves>
- `onFailedAttempt` 콜백에서 429 error에 한해 error.message 또는 error.errorDetails에서 retryDelay 값을 파싱(초 단위 float). 파싱 실패 시 sleep 없이 기존 동작 유지.
- 파싱된 retryDelay가 양수(N > 0)이면 `await new Promise(r => setTimeout(r, N * 1000))` 형태로 sleep. 안전 상한 60초(SDK 응답이 비정상적으로 큰 값을 주는 경우 cap).
- 기존 동작은 모두 보존: (a) 429 + primary === flash → model = flash-lite 폴백, (b) ZodError → AbortError, (c) p-retry default backoff schedule 그대로(retryDelay parse 실패 케이스에서는 기존 동작 변경 없음), (d) `geminiCallCount` 증분 패턴 변경 없음.
- 콜백은 여전히 async 호환 — p-retry 8.x의 `onFailedAttempt`는 Promise를 반환할 수 있고 p-retry가 await한다.
- `console.error`/`scrubSecrets` 라인(line 181-183)은 일절 수정하지 않음 — operator signal 보존(D-08).
- 새 retry policy 도입 금지 — `retries: 3` 그대로, `factor`/`minTimeout`/`maxTimeout`을 추가하지 않음 (D-06: default schedule 유지하되 parsed retryDelay와 default 중 큰 값 효과를 sleep으로 합성).
- `test/summarize/gemini.test.ts`에 새 describe 블록 또는 it 케이스 — mock 429 응답에 retryDelay 30s 포함 + p-retry가 sleep 후 재시도 성공하는 시퀀스 검증.
- 다른 파일 일절 touch 금지 — `src/summarize/gemini.ts` + `test/summarize/gemini.test.ts` 두 파일만.
</must_haves>

<tasks>

<task id="17-02-T1">
<title>onFailedAttempt 콜백에 retryDelay parse + sleep 로직 추가</title>
<type>execute</type>
<read_first>
- src/summarize/gemini.ts (현재 onFailedAttempt 구현 — line 163-173, p-retry 호출 옵션과 model fallback 로직)
- .planning/phases/17-summary-failure-ux-cleanup/17-SPEC.md (Requirement 5 + Acceptance — 옵션 (b) "errorDetails 또는 메시지에서 retryDelay 파싱 → sleep")
- .planning/phases/17-summary-failure-ux-cleanup/17-CONTEXT.md (D-06 — parsed retryDelay와 default 중 더 큰 값 사용 / 같은 콜백에 append / SDK error shape 확인 후 결정)
- src/summarize/prompt.ts (참고: 현재 prompt builder 형태 — 호출자 인터페이스 변경 없음을 확인)
- node_modules/p-retry/index.d.ts (또는 node_modules/p-retry/index.js — onFailedAttempt context shape 확인. v8 API에서 RetryContext가 `{ error, attemptNumber, retriesLeft, retriesConsumed, retryDelay }` 형태인지 재확인. 이미 docstring line 18-20에 메모되어 있음.)
</read_first>
<action>
`src/summarize/gemini.ts`의 p-retry 호출 부분(현재 line 163-173)을 다음과 같이 확장한다. 함수 signature와 export는 변경하지 않는다.

**(1) retryDelay 파싱 헬퍼 함수 추가** — 파일 최상단(import 블록 아래, SummaryZ 위) 또는 `summarize()` 함수 내부에 다음 헬퍼를 추가:

```typescript
/**
 * Parse Gemini 429 error retryDelay from either error.errorDetails[].retryDelay
 * or the message body's "retryDelay":"39.347610487s" pattern.
 * Returns seconds as a number, or 0 if not present / unparseable / negative.
 * Capped at 60s to bound wall-clock impact (SPEC D-06 safety: SDK may report
 * inflated retryDelay during sustained outage).
 */
function parseRetryDelaySeconds(err: unknown): number {
  if (!err || typeof err !== 'object') return 0;
  const e = err as { errorDetails?: Array<{ retryDelay?: string }>; message?: string };

  // (a) Structured errorDetails (preferred — SDK 1.49.x error shape)
  if (Array.isArray(e.errorDetails)) {
    for (const d of e.errorDetails) {
      if (typeof d?.retryDelay === 'string') {
        const m = d.retryDelay.match(/^(\d+(?:\.\d+)?)s$/);
        if (m) {
          const n = parseFloat(m[1]);
          if (Number.isFinite(n) && n > 0) return Math.min(n, 60);
        }
      }
    }
  }

  // (b) Fallback: message-body regex (in case SDK only stringifies the error)
  if (typeof e.message === 'string') {
    const m = e.message.match(/"retryDelay"\s*:\s*"(\d+(?:\.\d+)?)s"/);
    if (m) {
      const n = parseFloat(m[1]);
      if (Number.isFinite(n) && n > 0) return Math.min(n, 60);
    }
  }
  return 0;
}
```

**(2) onFailedAttempt 콜백 수정** — 현재 sync arrow function을 async로 변경하고 retryDelay sleep을 model 폴백 결정 직후에 추가:

변경 전 (line 166-172):
```typescript
onFailedAttempt: ({ error }) => {
  const anyErr = error as unknown as { status?: number; name?: string; message: string };
  if (anyErr.status === 429 && model === (models?.primary ?? 'gemini-2.5-flash')) {
    model = models?.fallback ?? 'gemini-2.5-flash-lite';
  }
  if (anyErr.name === 'ZodError') throw new AbortError(anyErr.message);
},
```

변경 후:
```typescript
onFailedAttempt: async ({ error }) => {
  const anyErr = error as unknown as {
    status?: number;
    name?: string;
    message: string;
    errorDetails?: Array<{ retryDelay?: string }>;
  };
  if (anyErr.status === 429 && model === (models?.primary ?? 'gemini-2.5-flash')) {
    model = models?.fallback ?? 'gemini-2.5-flash-lite';
  }
  if (anyErr.name === 'ZodError') throw new AbortError(anyErr.message);
  // SPEC D-06: honor Gemini 429 retryDelay so the next attempt waits for the
  // quota window to reset. flash + flash-lite share the same gemini-2.5-flash
  // quota metric — model fallback alone does NOT unstick a 429 burst, so we
  // must actually sleep. 60s cap prevents a runaway value from blocking the
  // GHA job past its budget.
  if (anyErr.status === 429) {
    const delaySec = parseRetryDelaySeconds(anyErr);
    if (delaySec > 0) {
      await new Promise((r) => setTimeout(r, delaySec * 1000));
    }
  }
},
```

**(3) 호출자 contract 보존:**
- `summarize()` export signature, parameters, return type 변경 없음.
- `geminiCallCount++` 위치(line 131) 그대로.
- `console.error('[summarize] model=... FAILED: ...')` 라인(181-183) 그대로 — operator signal 보존.
- catch 블록의 `return { ..., summaryModel: 'failed', summaryError: scrubbed }` shape 그대로.

**금지:**
- `retries: 3` 옵션을 변경하지 않음 (현재 default — D-06 명시).
- `factor`/`minTimeout`/`maxTimeout`/`randomize`를 새로 추가하지 않음.
- p-retry 외에 새 dependency 추가 금지.
- `src/compose/templates.ts` 또는 `CLAUDE.md`를 본 plan에서 touch하지 않음 — 다른 plan 책임.
- console.log/console.warn 라인 추가 금지 — operator signal은 기존 console.error만 사용 (silent sleep으로 충분).
</action>
<acceptance_criteria>
- `grep -nE "parseRetryDelaySeconds" src/summarize/gemini.ts` → 2개 이상 매치(정의 + 호출)
- `grep -nE "errorDetails.*retryDelay" src/summarize/gemini.ts` → 매치 (헬퍼 구현부)
- `grep -nE "setTimeout.*1000" src/summarize/gemini.ts` → 매치 (sleep 호출)
- `grep -nE "onFailedAttempt:\s*async" src/summarize/gemini.ts` → 매치 (async로 변경됨)
- `grep -nE "retries:\s*3" src/summarize/gemini.ts` → 매치 (기존 옵션 유지)
- `grep -nE "Math\.min\(.*60\)" src/summarize/gemini.ts` → 매치 (60s cap 존재)
- `grep -nE "console\.error.*\[summarize\]" src/summarize/gemini.ts` → 1 매치 (operator signal 유지)
- `grep -nE "summaryModel:\s*'failed'" src/summarize/gemini.ts` → 1 매치 (catch path 유지)
- `pnpm tsc --noEmit` → 0 errors
- `git diff --name-only HEAD` → `src/summarize/gemini.ts` (다른 src/* 파일 touch 없음)
</acceptance_criteria>
</task>

<task id="17-02-T2">
<title>test/summarize/gemini.test.ts에 retryDelay honor 회귀 테스트 추가</title>
<type>execute</type>
<read_first>
- test/summarize/gemini.test.ts (line 1-50 — 기존 mock 패턴 `vi.hoisted` + `vi.mock('@google/genai')` 형태 + `baseItem` fixture)
- src/summarize/gemini.ts (수정 후 — `parseRetryDelaySeconds` 헬퍼 + onFailedAttempt async 동작)
- .planning/phases/17-summary-failure-ux-cleanup/17-SPEC.md (Requirement 5 Acceptance — "mock 429 응답 + retryDelay 30s → p-retry 25초+ 대기")
- .planning/phases/17-summary-failure-ux-cleanup/17-CONTEXT.md (D-06 fixture 데이터 — 2026-05-27 retryDelay 39.347610487s 패턴)
</read_first>
<action>
`test/summarize/gemini.test.ts` 파일 끝(또는 기존 describe 블록과 동일 레벨)에 새 describe 블록을 추가한다.

**Test setup — vitest fake timers 사용 (SPEC AC: 25초+ 대기를 실제로 sleep하면 테스트가 느려지므로 fake timers로 검증):**

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

// 기존 mocks/vi.mock 블록은 그대로. 본 describe는 같은 mock 인프라 재사용.

describe('summarize — retryDelay honor on 429 (FAIL-UX-01 / SPEC requirement 5)', () => {
  beforeEach(() => {
    mocks.generateContentMock.mockReset();
    process.env.GEMINI_API_KEY = 'test-stub';
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it('sleeps for the parsed retryDelay (errorDetails shape) before the next attempt', async () => {
    // 첫 호출: 429 with errorDetails[].retryDelay = "30s"
    const err429 = Object.assign(new Error('429 quota exceeded'), {
      status: 429,
      errorDetails: [{ retryDelay: '30s' }],
    });
    mocks.generateContentMock.mockRejectedValueOnce(err429);
    // 두 번째 호출: 성공 응답
    mocks.generateContentMock.mockResolvedValueOnce({
      text: JSON.stringify({ summary_ko: '요약입니다.', confidence: 'high' }),
    });

    const promise = summarize(baseItem, realArticleBody);

    // p-retry가 첫 실패 후 onFailedAttempt를 await하는 동안, retryDelay sleep이 active 상태여야 함.
    // fake timer를 25초 advance — 아직 30s 도달 전이므로 두 번째 generateContent 호출 안 됨
    await vi.advanceTimersByTimeAsync(25_000);
    expect(mocks.generateContentMock).toHaveBeenCalledTimes(1);

    // 추가로 10초 advance (총 35s) — 30s sleep + p-retry default backoff 통과 후 두 번째 호출 발생
    await vi.advanceTimersByTimeAsync(10_000);
    // 남은 micro-tasks가 모두 drain되도록
    await vi.runAllTimersAsync();

    const result = await promise;
    expect(mocks.generateContentMock).toHaveBeenCalledTimes(2);
    expect(result.summary_ko).toBe('요약입니다.');
    expect(result.summaryModel).not.toBe('failed');
  });

  it('parses retryDelay from message body fallback when errorDetails is absent', async () => {
    const err429 = Object.assign(
      new Error('{"error":{"code":429,"details":[{"retryDelay":"5s"}]}}'),
      { status: 429 },
    );
    mocks.generateContentMock.mockRejectedValueOnce(err429);
    mocks.generateContentMock.mockResolvedValueOnce({
      text: JSON.stringify({ summary_ko: '본문 요약', confidence: 'medium' }),
    });

    const promise = summarize(baseItem, realArticleBody);
    await vi.advanceTimersByTimeAsync(6_000);
    await vi.runAllTimersAsync();
    const result = await promise;
    expect(mocks.generateContentMock).toHaveBeenCalledTimes(2);
    expect(result.summaryModel).not.toBe('failed');
  });

  it('caps retryDelay at 60 seconds (does not block indefinitely on inflated SDK value)', async () => {
    const err429 = Object.assign(new Error('429'), {
      status: 429,
      errorDetails: [{ retryDelay: '3600s' }], // 1 hour — must be capped to 60s
    });
    mocks.generateContentMock.mockRejectedValueOnce(err429);
    mocks.generateContentMock.mockResolvedValueOnce({
      text: JSON.stringify({ summary_ko: 'OK', confidence: 'high' }),
    });

    const promise = summarize(baseItem, realArticleBody);
    // 65초 advance: 60s cap을 초과해야 재시도 발생
    await vi.advanceTimersByTimeAsync(65_000);
    await vi.runAllTimersAsync();
    const result = await promise;
    expect(mocks.generateContentMock).toHaveBeenCalledTimes(2);
    expect(result.summary_ko).toBe('OK');
  });

  it('does not sleep when 429 has no retryDelay field (preserves p-retry default backoff)', async () => {
    const err429 = Object.assign(new Error('429 no details'), { status: 429 });
    mocks.generateContentMock.mockRejectedValueOnce(err429);
    mocks.generateContentMock.mockResolvedValueOnce({
      text: JSON.stringify({ summary_ko: 'ok', confidence: 'low' }),
    });

    const promise = summarize(baseItem, realArticleBody);
    // p-retry default backoff schedule이 작동하도록 충분히 advance
    await vi.advanceTimersByTimeAsync(2_000);
    await vi.runAllTimersAsync();
    const result = await promise;
    expect(mocks.generateContentMock).toHaveBeenCalledTimes(2);
    expect(result.summaryModel).not.toBe('failed');
  });
});
```

**Import 라인:** 기존 file head의 imports 그대로 사용. `vi.useFakeTimers/useRealTimers`는 vitest 표준 API이므로 이미 import된 `vi`에서 접근 가능.

**금지:**
- 실제 시간 sleep 검증(real timer)을 사용하지 않음 — CI 비용 폭발.
- @google/genai mock 인프라(`vi.hoisted`, `vi.mock('@google/genai', ...)`)를 재정의하지 않음 — 기존 file-top 블록을 그대로 사용 (같은 파일 내 새 describe만 추가하면 mock은 자동 공유).
- 기존 describe 블록(Guard-02 fixture 등)을 수정하지 않음.
- `baseItem`, `realArticleBody` 같은 fixture 상수를 재정의하지 않음 — 파일 상단 기존 fixture 재사용.
</action>
<acceptance_criteria>
- `grep -nE "summarize — retryDelay honor" test/summarize/gemini.test.ts` → 1 매치 (새 describe 블록)
- `grep -cE "vi\.useFakeTimers|vi\.advanceTimersByTimeAsync|vi\.useRealTimers" test/summarize/gemini.test.ts` → 3개 이상 매치 (fake timer 사용)
- `grep -nE "retryDelay.*30s" test/summarize/gemini.test.ts` → 매치
- `grep -nE "Math\.min|3600s" test/summarize/gemini.test.ts` → 매치 (60s cap 검증)
- `grep -nE "errorDetails:" test/summarize/gemini.test.ts` → 2개 이상 매치 (fixture 구성)
- `grep -nE "expect\(mocks\.generateContentMock\)\.toHaveBeenCalledTimes\(2\)" test/summarize/gemini.test.ts` → 2개 이상 매치 (재시도 발생 검증)
- `pnpm vitest run test/summarize/gemini.test.ts` → 모든 테스트 pass (기존 + 신규 4개)
- `git diff --name-only HEAD` → `test/summarize/gemini.test.ts` (다른 test 파일 touch 없음)
</acceptance_criteria>
</task>

<task id="17-02-T3">
<title>Plan 17-02 commit 생성</title>
<type>execute</type>
<read_first>
- .planning/phases/17-summary-failure-ux-cleanup/17-02-PLAN.md (본 plan 변경 요약)
- .planning/phases/17-summary-failure-ux-cleanup/17-SPEC.md (Requirement 5)
</read_first>
<action>
T1 + T2의 변경을 단일 commit으로 묶는다:

```bash
git add src/summarize/gemini.ts test/summarize/gemini.test.ts
git commit -m "feat(17-02): honor Gemini 429 retryDelay in onFailedAttempt sleep

Parse retryDelay from either error.errorDetails[].retryDelay (SDK 1.49.x
shape) or the message-body \"retryDelay\":\"Ns\" fallback. Sleep that many
seconds (capped at 60) before p-retry's next attempt, so flash + flash-lite
quota-pool bursts wait for the actual RPM window instead of hammering the
default exponential backoff to exhaustion.

Preserves all existing behavior: retries: 3, ZodError → AbortError,
model fallback flash → flash-lite, console.error operator signal, and the
geminiCallCount increment pattern. parseRetryDelaySeconds returns 0 on
malformed input so non-429 retries proceed exactly as before.

Tests cover: structured errorDetails parse, message-body fallback parse,
60s cap (3600s SDK value clamped), 429 without retryDelay (default backoff
preserved). Uses vitest fake timers so the suite stays under 1s.

Requirement: FAIL-UX-01 (SPEC requirement 5, decided in-scope per D-06)
Phase: 17-summary-failure-ux-cleanup"
```

`git push` 본 plan에서 수행하지 않음.
</action>
<acceptance_criteria>
- `git log -1 --pretty=%s` → "feat(17-02): honor Gemini 429 retryDelay in onFailedAttempt sleep" 정확히 시작
- `git diff HEAD~1 --name-only` → 정확히 2개 라인: `src/summarize/gemini.ts`, `test/summarize/gemini.test.ts`
- `git log -1 --pretty=%B | grep -E "FAIL-UX-01"` → 매치
- `git log -1 --pretty=%B | grep -E "Phase: 17"` → 매치
- `git status` → working tree clean
</acceptance_criteria>
</task>

</tasks>

<verification>

**Goal-backward checks (Plan 17-02 → SPEC requirement 5 / D-06):**

1. **retryDelay parsing 정확성:** `pnpm vitest run test/summarize/gemini.test.ts -t "retryDelay honor"` → 4개 테스트 모두 pass.
   - errorDetails 구조 파싱
   - 메시지 본문 폴백 파싱
   - 60s cap 검증
   - retryDelay 없을 때 default 동작 보존

2. **operator signal 보존:** `grep -nE "console\.error.*\[summarize\]" src/summarize/gemini.ts` → 정확히 1 매치(기존 line 181-183 위치). `summaryModel: 'failed'` catch path도 그대로(SPEC requirement 3).

3. **외부 인터페이스 안정:** `summarize()` 시그니처 변경 없음. caller(`src/main.ts` 등)는 변경 없이 동작.

4. **Type safety:** `pnpm tsc --noEmit` → 0 errors. `parseRetryDelaySeconds` 헬퍼는 `unknown`을 입력으로 받아 타입 안전.

5. **Single-file 경계:** `git diff HEAD~1 --name-only` → 정확히 2개 파일. `src/compose/templates.ts`, `CLAUDE.md`, 그 외 어떤 src/test도 touch되지 않음.

6. **Regression — 기존 488 테스트:** `pnpm vitest run` 전체 실행 → 모두 green. `test/summarize/gemini.test.ts`의 기존 Guard-02 fixture, geminiCallCount 테스트 등이 깨지지 않아야 함.

**False-positive 방지:**
- fake timer로만 검증하면 실제 sleep이 안 일어나도 테스트가 pass할 수 있음. 그 위험을 줄이려면 (a) `mocks.generateContentMock.toHaveBeenCalledTimes` 호출 횟수를 시간 advance 전후로 비교 — sleep이 활성화된 동안에는 두 번째 호출이 발생하지 않음. (b) 60s cap 테스트는 `advanceTimersByTimeAsync(65_000)` 시 호출 발생, `advanceTimersByTimeAsync(50_000)`만 했을 땐 발생 안 함을 별도 검증할 수도 있으나 4번째 테스트가 'no retryDelay' 검증을 이미 커버하므로 충분.
- p-retry 내부 schedule이 v8에서 retryDelay context 필드를 자체적으로 가지는지 — 본 plan은 SDK retryDelay를 명시적으로 parsing해서 sleep하므로 p-retry 자체의 retryDelay 메커니즘과 무관(중복도 무해).

**보안/안전 노트 (threat model):**
- retryDelay value validation: 입력은 `unknown`으로 받아 type-narrow 후 regex로만 파싱. 60s cap이 무한 sleep을 차단. 음수/NaN/Infinity는 0으로 처리 → setTimeout 0 == no-op.
- 새 외부 호출 없음 — 기존 generateContent 경로 그대로.
- console.error scrubSecrets 체인(line 181-183) 보존 — sleep 도중 발생 가능한 추가 에러도 같은 경로로 흐름.

</verification>
</content>
