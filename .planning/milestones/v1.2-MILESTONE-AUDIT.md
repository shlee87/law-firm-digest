---
milestone: v1.2
milestone_name: Coverage & Closure
audited: 2026-05-28T00:30:00Z
status: tech_debt
scores:
  requirements: 7/7 (functionally satisfied)
  phases: 4/4 (all VERIFICATION.md present, 3 status=passed + 1 status=human_needed for time-delayed criterion)
  integration: 7/7 wired (0 critical, 1 tech-debt, 3 info)
  flows: e2e wired (daily → state/pending → weekly → compose → email)
  tests: 495/495 passing
nyquist: skipped (workflow.nyquist_validation = false)
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: project-traceability
    items:
      - "REQ checkbox drift in .planning/REQUIREMENTS.md:23 (CLOSURE-01 [ ]) — VERIFICATION+SUMMARY both PASS; flip to [x]"
      - "REQ checkbox drift in .planning/REQUIREMENTS.md:25 (CLOSURE-02 [ ]) — VERIFICATION+SUMMARY both PASS; flip to [x]"
      - "REQ checkbox drift in .planning/REQUIREMENTS.md:39 (FAIL-UX-01 [ ]) — VERIFICATION+SUMMARY both PASS; flip to [x]"
      - "Traceability table rows 61, 62, 65: status column still 'Pending' for CLOSURE-01, CLOSURE-02, FAIL-UX-01 — flip to 'Complete' with evidence pin"
  - phase: 14-scheduling-coverage
    items:
      - "Time-delayed roadmap SC #1 (Monday cron actually fires) requires post-deployment audit on or after 2026-06-03. Audit command pre-staged in 14-VERIFICATION.md human_verification block. Not a code gap — by-design deferred."
deferred:
  - "Phase 14 SC #1 weekly-cron-evidence audit — scheduled for 2026-06-03 via gh run list query (see 14-VERIFICATION.md)"
---

# Milestone v1.2 (Coverage & Closure) — Audit Report

**Audited:** 2026-05-28T00:30:00Z
**Status:** `tech_debt` — no critical blockers, 1 known traceability-drift class, 1 documented time-delayed audit deferred.
**Score:** 7/7 requirements functionally satisfied; 4/3 mechanical checkbox flips outstanding (see Tech Debt).
**Test suite:** `pnpm test` → 495/495 passing.

## Milestone Definition of Done (from ROADMAP.md)

> v1.1 close 과정에서 드러난 운영 footgun + 문서 메타데이터 누락 + 큐레이션 사각지대(월–화 48시간 fetch gap)를 한 번에 정리해 production 안정성과 artifact 정합성을 v1.1 shipping 수준에 맞춘다. No new user-facing features — pure operational + documentation tightening.

Achieved across 4 phases (14 → 17). Phase 17 added in mid-milestone via backlog promotion (999.1 → 17, 2026-05-27) after a real production observation in the 2026-05-27 daily digest.

## Phases

| Phase | Name | VERIFICATION status | Score | Notes |
|-------|------|---------------------|-------|-------|
| 14 | Scheduling Coverage | `human_needed` | 11/11 code-verifiable PASS; 1 time-delayed criterion deferred to 2026-06-03 audit | SC #1 (Monday cron fires) cannot be verified pre-deployment; audit-trigger anchor in STATE.md |
| 15 | v1.1 Phase Closure Backfills | `passed` | 3/3 must-haves, 13/13 SPEC ACs | Inline SDK-level verification per nested-Agent constraint |
| 16 | v1.1 Metadata Hygiene | `passed` | 7/7 requirements, 15/15 ACs | Inline SDK-level verification |
| 17 | Summary Failure UX Cleanup | `passed` | 5/5 must-haves, 0 gaps | Goal-backward smoke render against Latham EU ETS fixture PASS |

## Requirements (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION | SUMMARY frontmatter | REQUIREMENTS.md table | REQ definition line | Final |
|--------|-------|--------------|---------------------|------------------------|---------------------|-------|
| SCHED-01 | 14 | human_needed (code parts PASS) | 14-01: [SCHED-01] | `Complete` | `[x]` | **satisfied** (1 deferred audit) |
| SCHED-02 | 14 | human_needed (code parts PASS) | 14-02: [SCHED-02] | `Complete` | `[x]` | **satisfied** |
| CLOSURE-01 | 15 | passed | 15-01: [CLOSURE-01] | `Pending` ⚠ | `[ ]` ⚠ | **satisfied (update checkbox)** |
| CLOSURE-02 | 15 | passed | 15-02: [CLOSURE-02] | `Pending` ⚠ | `[ ]` ⚠ | **satisfied (update checkbox)** |
| META-01 | 16 | passed | 16-01,16-02: [META-01] | `Complete` | `[x]` | **satisfied** |
| META-02 | 16 | passed | 16-03: [META-02] | `Complete` | `[x]` | **satisfied** |
| FAIL-UX-01 | 17 | passed | 17-01,02,03: [FAIL-UX-01] | `Pending` ⚠ | `[ ]` ⚠ | **satisfied (update checkbox)** |

**Coverage:** 7/7 satisfied. 0 orphans. 0 unsatisfied. No FAIL-gate trigger.

**Drift items (`⚠`):** The 3 affected REQs all have phase-level VERIFICATION+SUMMARY agreement but the project-level `.planning/REQUIREMENTS.md` traceability table + REQ definition line still read `Pending` / `[ ]`. This is the same META-01-class drift v1.2 itself fixed for v1.1 — captured here as tech debt for resolution at `gsd:complete-milestone v1.2`.

## Integration (gsd-integration-checker findings)

**0 CRITICAL · 1 TECH_DEBT · 3 INFO**

### Wired (no findings)
- `.github/workflows/daily.yml` cron `0 12 * * 0-6` (KST 21:00 daily incl. Mon) + `weekly.yml` cron `0 21 * * 0` (KST Mon 06:00); both files declare `concurrency: digest-pipeline` lock with `cancel-in-progress: false`.
- Sync-schedule removal complete — `pnpm sync-schedule` not a script; no `toCron`/`sync-schedule` reference outside `.planning/`.
- CLAUDE.md three new sections coexist cleanly: P14 cron edit policy + P16 audit:firms freshness + P17 free-tier table observation note. No conflict.
- E2E flow: daily run → `pending.json` (atomic state) → weekly run reads + `composeDigest` → `renderArticle` (failed branch reachable) → email send. Wired end-to-end.
- 495/495 tests passing (P16 baseline 488 + P17 added 7).

### TECH_DEBT
- **Project-level `.planning/REQUIREMENTS.md` traceability drift** — same as Requirements table above. Affected REQs: CLOSURE-01, CLOSURE-02, FAIL-UX-01.

### INFO (by-design, no action)
- `renderArticle` failed branch reachable only via weekly pipeline. `runDaily.ts` does NOT compose email — daily writes `state/pending.json`; weekly reads, composes, sends. So a Tuesday 429 surfaces in HTML only on the next Monday. Correct per atomic-state design.
- `await sleep` in `onFailedAttempt` × cron windows — safe. 60s cap inside `parseRetryDelaySeconds`; daily/weekly cron slots are 15h apart; concurrency lock guarantees serialization.
- `config/settings.yaml:24–28` `schedule:` block retained as zod-compat placeholder per CLAUDE.md policy (P14 D-04). Consistent.

## E2E Flows

| Flow | Trigger | Path | Status |
|------|---------|------|--------|
| Daily fetch + state-write | `daily.yml` cron (Mon–Sun 21:00 KST) | `main.ts --mode=daily` → `runDaily.ts` → firms fetch → `summarize` → `appendPending` (state/pending.json) → git auto-commit | WIRED |
| Weekly compose + email | `weekly.yml` cron (Mon 06:00 KST) | `main.ts --mode=weekly` → `runWeekly.ts` → read pending → `composeDigest` → `renderArticle` (incl. **P17 failed branch**) → nodemailer SMTP → state clear + git auto-commit | WIRED |
| Gemini 429 retry-then-fallback | runtime | `summarize` → p-retry `onFailedAttempt` (**P17 parseRetryDelaySeconds + async sleep, 60s cap**) → model fallback flash → flash-lite → if all retries exhausted, `summaryModel: 'failed'` + `summary_ko = title` + `summaryError = scrubbed` | WIRED |
| Failed-item digest render | runtime | weekly compose path → `renderArticle()` sees `summaryModel === 'failed'` → (**P17 new branch**) emits `<a>title</a>` + `<div>⚠ 요약 일시 불가</div>` (muted-mono tag) + read-link, NO body `<p>`, NO error JSON | WIRED |
| Operator failure signal | runtime | `gemini.ts:181-183` `console.error('[summarize] model=... FAILED:...')` → GHA logs + step-summary recorder receives `summaryModel: 'failed'` + `summaryError` | WIRED |
| Cron edit policy | maintenance | CLAUDE.md `## Conventions > Workflow scheduling` describes single-source-of-truth edit ritual (P14 D-04) | WIRED |
| Audit-firms freshness | maintenance | CLAUDE.md `## Conventions > Audit freshness` lines 211-221 (P16 META-02) | WIRED |

## Nyquist Compliance

Skipped — `workflow.nyquist_validation = false` (per project config).

## Anti-Patterns / Stubs Found

None reported in any of the 4 VERIFICATION.md files. Phase 17 search of changed source confirmed no TODO/FIXME/placeholder strings in the new render branch or retryDelay parser.

## Time-Delayed Verification Tracking

| Phase | Test | Expected | When | Audit command |
|-------|------|----------|------|---------------|
| 14 | Monday cron fires from `schedule` (not workflow_dispatch) | `gh run list --workflow=daily.yml --created '>=2026-05-27' --json event,conclusion,createdAt` shows ≥1 `event: schedule` + `conclusion: success` entry per weekday in the week starting 2026-06-01 (incl. Mon 2026-06-01) | On or after 2026-06-03 KST | Pre-staged in `.planning/phases/14-scheduling-coverage/14-VERIFICATION.md` `human_verification:` block (verbatim) |

## Outstanding Items for `gsd:complete-milestone`

Mechanical, low-risk, ~3-min total:

1. `.planning/REQUIREMENTS.md` lines 23, 25, 39 → `- [ ]` → `- [x]` for CLOSURE-01, CLOSURE-02, FAIL-UX-01.
2. `.planning/REQUIREMENTS.md` lines 61, 62, 65 (traceability table) → Status column `Pending` → `Complete`. Evidence pin per row (commits + VERIFICATION.md paths) — pattern in v1.1-REQUIREMENTS.md after META-01 flip can be mirrored.
3. (Optional but recommended) Verify the Phase 14 time-delayed audit on 2026-06-03; if all pass, append a `re-verified: 2026-06-03` note to `14-VERIFICATION.md` and remove `status: human_needed` (flip to `passed`).

These are not blockers for milestone completion — they're the same drift class v1.2 just fixed for v1.1, and `gsd:complete-milestone` can perform the flips atomically.

---

*Audited: 2026-05-28 (Phase 17 last commit: be9879f)*
