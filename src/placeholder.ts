// Placeholder to satisfy tsconfig.json `include: ["src/**/*"]` until real source
// modules land in plan 01-02+. tsc emits TS18003 if include globs match zero files.
// This file will be removed once src/types.ts and src/env.ts exist.
export {};
